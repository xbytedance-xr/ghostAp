# 2026-07-13

## Agent Department completion audit 与 `/hire` 安全收口

### 审计结论

- 三个并行只读审计分别检查最终目标、`/hire` 生产调用链和 release gate，结论一致：整体目标仍 active。现有数据面、Context、Provisioning、Channel、Slash、Router、Response 和 Fire 多数是独立模块或内存脚手架，生产 composition 与真实租户证据缺失。
- 现场 `/hire` 没有调用一键创建 SDK；它仍路由到 `SlockHandler.create_role()`。首张工具卡丢失 `global_hire`，最终模型回调也未传回该标记；非 Slock 群被错误 gate 拦截，Slock 群则创建旧 `AgentIdentity` 并绿色假成功。带完整参数的 global 路径还会访问 `engine=None`。
- `AgentDepartmentBootstrap` 在 limit=0 或非 0 时都常量设置全部 readiness=True；Channel/Slash/Hire 测试全部使用 fake。acceptance manifest 77 项仍 pending，不能用本地测试代替真实 Bot、双租户、50 Bot soak 和故障注入。

### 更改与原因

- 将 `lark-oapi` 从 `1.6.5` 升级并锁定到官方 `1.7.1`；新增签名 contract，强制 `register_app/aregister_app` 支持 `app_preset`、`addons`、`create_only`、`app_id`。
- 新增 `LarkAppRegistrar`：通过官方 `aregister_app` 使用 minimal preset、版本化最小 scopes/events/callback 与 `create_only=True`；同步转发十分钟链接和状态，严格校验 credential result，错误与 repr 不暴露 secret。
- 新增 `EmployeeHireRequest/EmployeeHireService` 类型边界并注入 `HandlerContext`。`/hire` 的工具/模型/effort 标记全程保留，只允许配置管理员在主 Bot 私聊发起；没有 production service 时明确拒绝，绝不回退旧 global registry。`/new-role` 的 Slock 虚拟角色行为保持不变。
- `AgentDepartmentBootstrap` 新增 dormant 状态与六个具名 readiness probe；缺 probe、probe 异常或任一 unavailable 均 fail-close，`healthy` 同时检查 data/context/provisioning/channel/router/response。
- 更新 `docs/goals.md`，移除“数据 Task 1 未实现”等过时状态，明确局部模块证据与 production completion 的区别。

### TDD 与验证

- SDK/adapter RED：缺 `lark_app` 模块导致收集失败；GREEN：注册 capability、精确 manifest、callback、严格 credential/avatar 校验与 config contract 共 `25 passed`。
- `/hire` RED：首卡/终卡标记丢失、无 service 的 `NoneType` 崩溃、legacy registry 可达；GREEN：全局 hire 定向 `5 passed`，Slock/Card/Action 扩展回归 `148 passed`。
- Bootstrap RED：dormant 和 nonzero-limit 两条路径均伪报 healthy；GREEN：Fire/Bootstrap `9 passed`。
- 合并定向 `182 passed`；Autonomous 全量 `872 passed in 77.73s`；docs references `4 passed`、配置校验、scoped Ruff 与 `git diff --check` 通过。

### 后续风险与门禁

- 本轮只完成官方 SDK 能力和安全命令边界，不会也不能声称真实员工已可创建。`EmployeeHireService` 生产实现仍需 durable callback bridge、canonical Journal/Vault adapter、每员工 fresh-interpreter Channel、Slash GET/diff/POST/PATCH/DELETE/GET、VerificationRouter 和 Supervisor 恢复。
- `autonomous_visible_employee_limit` 必须保持 0。只有测试租户和生产租户 Provisioning、员工独立收发、主 Bot send count=0、1/10/50 Bot soak、故障注入和 release evidence 全部通过后才能启用。
- Thread Context 的真实 FeishuMessageSource/revision、真实 producer cutover、`/stop` 终态竞态、可恢复 `/fire` 与 Durable Outbox 仍是整体目标的阻断项。

## `/hire` 生产链路实现与安全门禁（保持关闭）

### 更改内容与原因

- 新增 `ProductionEmployeeHireService` 和冻结的 Hire 投影：以权威 tenant + message 生成稳定 intent，所有注册、Vault、Bot 绑定、Slash、Channel 和激活状态均写入 Journal；外部 Effect 在派发前完成 PREPARED/EXECUTING fsync+anchor，重启从 Journal 重放，未知远端结果进入 `ACTION_REQUIRED`，不盲目重建应用。
- 新增 Journal/Vault durable Saga 与 callback bridge：Vault receipt 可幂等收养，员工与 Bot principal 原子绑定；首次调度失败允许同一 intent 安全重投，关闭时停止 admission 并等待后台任务收敛。
- 新增每员工 fresh-interpreter Channel 子进程：secret 只经一次性 pipe，禁止出现在 argv/env/普通 IPC，协议为严格限长版本化 NDJSON；默认使用 bwrap 最小只读文件系统与 user/mount/PID namespace，attestation 在 secret 下发前验证项目凭证路径不可见，PID/generation fencing、发送身份回执、崩溃恢复和重新验证均由父进程监管。
- 新增官方 `lark-oapi Client.arequest(BaseRequest)` Slash adapter 与 GET→diff→POST/PATCH/DELETE→GET 精确 reconciliation；最终 observed set/hash 不一致则拒绝激活。
- 新增激活验证路由：一次性 nonce/TTL 与 tenant、agent、app、generation 绑定；只有真实员工 Bot DM `/status`、Slash/Channel 真实时间证据、员工回执中的 app/message/generation/connection 全匹配、且独立主 Bot 审计返回零发送，才可消费 nonce 并提交 `ACTIVE`。缺审计 provider 时 composition fail-close；员工 Channel 崩溃后 durable generation +1 并强制重新验证。
- 新增单一生产 composition 并接入 `FeishuWSClient`：权威 `tenant_key` 通过 TaskSpec/ContextVar 传递，runtime 启动恢复和关闭纳入 WS 生命周期；limit=0 的全新环境保持纯 dormant，已有 Journal 可离线重放但禁止新 admission 或无签名外部恢复。
- 新增 Employee 专用 17 项租户验收清单、追加式 fsync 哈希链、外部 checkpoint 及 Ed25519 独立 QA 签名；签名绑定 environment/release/commit/service/staging/prod tenant，最长 24 小时。该 bundle 仅作验收候选证据；因尚无不可变构建/工作负载证明、固定 QA trust root 与外部单调消费账本，runtime 对 config-only release 恒 fail-close，默认 CLI 只返回 `PENDING`。

### 验证与安全结论

- 最终 Autonomous 回归 `1019 passed, 2 skipped`；共享 WS/路由/配置回归 `248 passed`；`uv run python -m src.main --validate`、scoped Ruff 与 `git diff --check` 通过。两项 skip 分别是未授权真实租户验收和当前宿主未满足默认 bwrap 实际 attestation；两者均保持 fail-close。
- 现场配置确认 `autonomous_visible_employee_limit=0`，且未配置 Employee release 公钥；默认验收 CLI 返回 `PENDING`（exit 2）且不创建 evidence 文件。
- 第一轮独立审查发现的 unsigned release、默认 sandbox fail-open、Channel 崩溃不换代、硬编码证据时间、调度重投、关闭竞态和 Popen FD 泄漏均已修复并加入回归测试。
- 第二轮独立审查发现生产 factory 暴露布尔 release 绕过、重启后新 Channel 复用旧 generation/nonce；现已移除 bypass 参数，ACTIVE/READY 重启先 durable revalidation，再以 generation+1、新 connection 和新 challenge 回到待验证状态。
- 后续多轮 grill/mloop 审查继续修复：Slash generation+attempt crash window、Vault 失败后悬空 Effect、Channel failed-start 高水位、配置与恢复期 per-intent single-flight、长生命周期指数退避 recovery supervisor、公开 recover 提权，以及 hard-close 下既有 nonterminal intent 重投绕过。最终 Round13/14 连续两轮无当前可达 P0/P1。

### 尚未满足的发布条件

- 本轮没有真实测试/生产租户授权，因此没有执行真实 Provisioning、员工 Bot 收发、桌面/移动 Slash 传播、主 Bot send-count 外部核验或 1/10/50 Bot soak；独立 QA 必须在真实租户完成清单并签发 attestation 后才可能通过发布门禁。
- 生产启用还需要不可变 build/workload provenance、部署侧固定 QA trust root、外部单调 attestation ledger 和可续期 recovery capability，并把真实主 Bot 审计 provider 注入 WS composition；在这些外部信任组件缺失时，代码有意不可解锁。
- 本地 `FileAnchor` 可提供跨进程 CAS、no-follow、文件/目录 fsync 和 0600 权限，但不能单独提供独立系统防回滚证明；生产启用前仍需外部不可回滚 anchor/见证服务。
- 因上述阻断项，`autonomous_visible_employee_limit` 必须继续保持 0，不能宣称 Agent Department 已可生产使用。
