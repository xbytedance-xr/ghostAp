# 2026-07-17

## SMART Shell 路由与飞书热路径修复

### 现象与根因

- SMART、无项目、未管理聊天中，`pwd`、`ls -la`、`echo hi` 会先进入被动 Slock 分类并弹出“是否是任务”的澄清卡片；`git status`、`./restart.sh rr` 可能直接激活 Slock。提交 `a09bd51a` 引入的被动分类位于普通 intent/shell 识别之前，而 shell 快速队列只改变调度，不改变路由优先级。
- 澄清卡片发送后缺少 `return`，同一条消息还会继续进入普通 intent，造成重复处理。
- `logs.log` 中真实 `pwd` 请求先发送澄清卡，再执行 Contact API；Contact GET 返回 400 且一次耗时约 7 秒，随后普通 shell 路由仍继续执行。卡片回调还会在 `ActivationGuard` 中因生产配置的 `frozenset` 被当作字符串调用 `.split()` 而崩溃。
- 运行进程环境核查只发现 `NO_PROXY=127.0.0.1`，未发现 `http_proxy`/`https_proxy`；因此“生产进程继承代理并放大 open.feishu.cn 延迟”的假设不受当前进程证据支持。

### 修改

- 将 `IntentRecognizer.looks_like_shell()` 提升为可直接复用的类方法，`SystemHandler`、Slock handler 和 dispatcher 共用同一 shell 检测器，避免规则漂移。
- dispatcher 在被动/遗留 Slock 路由前识别 SMART shell 并直接保留给普通 shell intent；显式 Slock 命令、已激活上下文和话题引擎语义不变。澄清卡片发送后立即返回。
- 普通消息、卡片入口以及 Slock 角色/任务/升级回调全部改为非阻塞用户名解析；后台刷新使用进程级 single-flight 和最多 32 个并发刷新。飞书稳定权限错误族负缓存 1 小时，空响应、服务端错误、导入或网络等瞬态异常只负缓存 60 秒。
- `ActivationGuard` 对 `admin_user_ids` 同时接受逗号字符串和 `list`/`tuple`/`set`/`frozenset`，与生产 Settings 契约一致。
- 新增跨模块回归：SMART、无项目、未管理聊天中的 `pwd`、`ls -la`、`echo hi`、`git status`、`./restart.sh rr` 只能进入 shell；澄清卡片必须终止路由；卡片回调不得同步查询 Contact；Feishu handler 禁止带 API factory 调用同步用户名解析；生产 `frozenset` 管理员配置可正常鉴权。

### 验证

- 针对性路由/卡片/Slock 聚合：610 passed，0 failed。
- 下游 Contact handler 与升级路径：150 passed，0 failed。
- 统一 shell 检测器收口后的 handler/dispatcher 回归：272 passed，0 failed。
- 最终 Contact 权限/瞬态负缓存边界及卡片回调回归：100 passed，0 failed。
- 文档引用：4 passed。
- 主修复全量：12297 passed，9 skipped，45 subtests passed；1 条第三方 protobuf datetime 弃用警告。其后的两项小范围收口由上述 272 与 100 项回归覆盖。
- `ruff check`、`git diff --check`、`python -m src.main --validate` 均通过。

### 后续风险与交接

- 本轮未重启运行中的 GhostAP；线上进程在重启或部署前仍使用旧代码。
- 工作树进入本轮前已有 Autonomous、配置和 2026-07-16 Memory 等未提交修改；本轮未提交或推送，避免混入用户现有工作。

## 常驻员工、团队协调与知识工作目录改造方案

### 现状结论

- 当前真正常驻并可在重启后恢复的是每名员工的飞书 Channel 子进程；员工 ACP/CLI 会话仍由 Slock 在每个任务中创建、单次发送 prompt 后立即关闭。
- 可见 `/hire` 只把名字、工具、模型、Profile 和 Effort 传入正式 Hire request；领域模型已有的 role/persona/traits/capabilities/permissions 没有被 UI 完整固化。
- Team 模式不是主协调 Agent，而是按 `agent_id` 排序后固定执行“第一名 analysis、第二名 review、第一名 synthesis”。TeamRun 只持久化任务 digest，重启无法恢复原始 instruction。
- 本次错误 `context_unavailable` 的具体链路是：内部 Team assignment 仍要求员工 Bot 重新抓取并严格验证飞书 Thread/Group 排序；`ORDERING` 有界重试耗尽后被统一终止，模型尚未收到任务。

### 自动 Grill 决策

- “员工常驻”定义为可恢复的逻辑 Actor、durable mailbox、任务/会话 checkpoint 和知识目录；ACP 支持时保留热会话，不支持时允许从同一身份和 checkpoint 冷启动。
- Journal 与加密 Blob 保持唯一事实源；`AGENTS.md`、`IDENTITY.md`、`NOW.md` 和 Wiki 是可重建投影，员工不可通过文件修改身份、权限或任务状态。
- 借鉴 Karpathy LLM Wiki 的 immutable source / compiled wiki / schema 结构，并采用 ingest、query、lint；GhostAP 增加独立 runtime state 层，不首期引入向量库。
- 主 Bot 维护全群 durable event ledger，普通消息对协作系统可查但不自动唤醒所有模型；直接 @、Coordinator assignment、CAS claim 或显式职责规则才唤醒员工。
- 内部 Team assignment 使用已锚定的 canonical group context；远端 ordering/pagination/revision 失败降级为可披露的 partial context，scope/permission/current-message 等 authority 失败仍 fail-close。

### 文档交付

- 新增 `docs/2026-07-17-persistent-employee-runtime-and-knowledge-workspace-plan.md`，包含当前实现剖面、错误根因、员工目录布局、会话作用域、Wiki 合同、动态 Coordinator 合同和 12 个可逐项执行的 TDD 改造任务。
- 方案明确 Codex 使用 employee-specific `CODEX_HOME/AGENTS.md`，所有后端仍接收带 digest 的显式 bootstrap，避免错误假设 Coco/Traex/Claude/TTADK 都会自动读取同一目录规则。
- 本轮只产出方案，未修改生产行为、未运行 Python 测试、未提交或推送；验证仅做文档路径/占位语检查和 `git diff --check`。

## 常驻员工改造 Task 1：v1 缺口契约

### 修改与原因

- 新增员工运行时契约测试，明确连续两个员工 prompt 会分别创建并关闭两个 ACP session；飞书 Channel READY 不能被误读为模型 session READY。
- 将 Team 单测明确命名为 v1 固定 analysis/review/synthesis 流水线，并锁定重启后 `restart_instruction_unavailable` 的 Journal 终态。
- 新增真实 ordering 触发回归：群窗口中相同 `message_position` 对应不同消息时，`EmployeeThreadContext` 在 backend 调用前以 `ORDERING` 拒绝执行。
- 在 2026-07-12 部门设计中标记 Slock 单次 ACP 为 v1 过渡方案，并链接常驻 Actor/知识工作区实施计划，防止文档语义漂移。

### 验证

- `uv run python -m pytest tests/autonomous/contract/test_persistent_employee_runtime_contract.py tests/autonomous/integration/test_team_context_ordering_recovery.py tests/autonomous/unit/test_employee_team_service.py -q`：18 passed。
- `git diff --check`：通过。

### 后续风险

- 本阶段只冻结证据，没有改变生产行为；session 复用、durable payload/checkpoint 和 context quality 将在后续阶段逐项替换这些 v1 合同。

## 常驻员工改造 Task 2：完整 Hire 身份

### 修改与原因

- `EmployeeHireRequest` 新增冻结 tuple 字段 `personality_traits`、`capabilities`、`permissions`，并由版本化角色模板补齐空 role/persona/profile 字段。
- 建立 trait、capability、permission allowlist 和权限必须同时存在于 capability 的绑定规则；非法值在任何 Journal 写入或外部 App 提交前拒绝。
- `employee.created` schema v2 一次性持久化完整身份，`DurableHireState` 和 workforce projection 重放后保留相同字段，幂等匹配覆盖完整 profile。
- 可见 `/hire` 根据工具生成 coder/reviewer/writer 等最小权限模板，支持受控 `--role`/`--traits`，明确拒绝任意 `--prompt`；统一工具模型卡展示身份与权限摘要。
- 新增 `ux/employee-hire-profile.html`，先行沉淀资料卡视觉层级和推荐模板交互。

### 验证

- Hire、projection、可见 `/hire`、卡片集成：164 passed。
- 新增受控 profile 与任意 prompt 拒绝回归后，Slock role 创建：68 passed。
- 涉及文件 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- 当前角色模板是代码内版本化常量；后续知识工作区会把选定结果投影为只读 `IDENTITY.md`/`AGENTS.md`，但不会改变 Journal 权威。

## 常驻员工改造 Task 3：知识工作区投影

### 修改与原因

- 新增 `src/autonomous/workspace/`，以冻结 `EmployeeWorkspaceSource` 生成 `AGENTS.md`、`IDENTITY.md`、`NOW.md`、purpose/schema、Wiki 骨架、任务索引、source manifest 和 Codex global AGENTS。
- projector 逐组件使用 dir-fd 与 `O_NOFOLLOW`，文件通过唯一临时名、短写循环、`fsync`、同目录 rename 原子替换；目录固定 `0700`、文件固定 `0600`。
- workspace lint 校验必需文件、8 KiB AGENTS 上限、Codex 指令 digest 一致、symlink/权限漂移，以及 secret、token、`.env`、隐藏推理标记等禁入内容。
- `EmployeeIdentityMaterializer` 同步生成工作区；data composition 的全量恢复从 Journal Projection 重建全部员工目录，不读取 Markdown 反推事实。
- 生产 Hire service 在人工/自动/恢复路径进入 ACTIVE 前先 rebuild+verify workspace；投影失败时拒绝激活。

### 验证

- Workspace、workforce identity 和 data composition：56 passed。
- Workspace 与完整 Hire composition：145 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- 当前 Wiki 内容仅是确定性骨架，knowledge ingest/query/lint 的来源校验和发布流程在 Task 7 实现；当前不会把原始执行正文复制到 workspace。

## 常驻员工改造 Task 4：跨后端 Bootstrap 与路径隔离

### 修改与原因

- 新增冻结 `EmployeeSessionKey`/`EmployeeSessionBootstrap`，从已 lint 的 workspace AGENTS 构造 backend-neutral 指令 envelope，并固定 identity、project、backend/model/profile 和 instruction digest。
- Slock 员工 session 在创建后端进程前读取并注入同一 bootstrap；缺失或超限 AGENTS fail-close。原有 factory/ACP adapter 已支持显式 env 一次性捕获，继续复用而不修改全局 `os.environ`。
- `build_employee_process_env` 允许显式 employee-specific `CODEX_HOME`，并拒绝继承主进程同名值；Coordinator 仅对 Codex 员工注入 `runtime/codex-home`。
- 工具过滤拆为 read roots 与 write roots：project 依权限可写、workspace 控制投影只读；shell/git cwd 必须位于 write root，`.env`、Vault、Journal 和其他员工目录默认拒绝。
- legacy Slock 仍保留原 workspace 行为；严格读写分离只作用于 `employee_v1`，避免改变旧虚拟角色合同。TTADK 员工仍由现有门禁拒绝 ACP 启动。

### 验证

- 新增 backend/bootstrap、CODEX_HOME 和路径隔离合同，加现有 Slock 安全测试：35 passed。
- `ruff check`：通过。

### 后续风险

- 本阶段仍是一次任务一次 session；Task 5 将把相同 bootstrap/key 用于可复用 lease，并在 identity digest 变化时回收热 session。

## 常驻员工改造 Task 5：可恢复 Actor 与 Session Lease

### 修改与原因

- 新增每员工串行 `EmployeeActor` 和进程内 `EmployeeRuntimeSupervisor`：重复 assignment 幂等，同员工串行、不同员工并行，取消、超时和关闭都只形成一个终态。
- mailbox、Effect PREPARED/EXECUTING 和终态写入 Journal；正文只保存 digest/ref。后端 session 创建和 prompt 发送前必须完成 Effect 锚定。
- 重启可通过受控 payload loader 重新派发；正文无法安全恢复时写入具体 `employee_recovery_payload_unavailable`，不重复不确定的外部 effect。
- 相同 `EmployeeSessionKey` 复用健康后端 session；模型、Profile、项目或身份键变化时关闭旧 lease；idle TTL 回收后状态回到 `READY_COLD`。
- Slock 新增 employee_v1 可复用 session lease，并保持现有 tool filter、显式环境和取消边界；Actor prompt 总是带 Task 4 的 bootstrap digest。
- Gateway 增加 `legacy_one_shot|shadow|actor` 显式模式，actor 失败不自动回退。生产默认仍为 `legacy_one_shot`，等待真实租户证据后再切换。

### 验证

- Actor、复用、崩溃恢复及完整 Gateway 集成：102 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- 当前 actor recovery 对已有 ingress envelope 尚未接入解密 loader，因此进程重启后的不确定 prompt 会进入明确 action-required；Task 10 将按共享 projection/generation fence 接入完整恢复顺序。
- 默认模式尚未切到 actor；真实租户的 ACP session 复用、idle recycle 和取消证据属于 Task 12 发布门禁。

## 常驻员工改造 Task 6：Canonical Group Ledger 与上下文降级

### 修改与原因

- 新增 `GroupContextLedger`，将群消息正文写入加密 Blob，Journal 只保存租户/群/线程/消息、transport principal、sequence、payload ref、dedup key 和 causal ID。
- main Bot dispatcher 与员工 Bot durable ingress 都发布 canonical group event；同一飞书消息跨 Bot 观察按稳定键去重，后到 payload 不能覆盖首次锚定的 transport authority。
- `AssembledContext` 新增 `COMPLETE|CANONICAL_PARTIAL` 质量和类型化 warning；完整上下文保持原合同，降级快照按 Journal sequence 截取本地窗口并严格验证当前消息与 requester。
- Team 的 ordering/pagination/revision/deadline/source 失败改走 ledger；scope/permission/current-message 等 authority failure 仍直接拒绝。降级派发在同一事务记录 `context.warning.recorded`。
- Team ledger 不可用时使用具体 `canonical_context_unavailable`，不再把内部 assignment 终止为模糊 `context_unavailable`；普通员工任务仍保留原有有界重试语义。

### 验证

- Group ledger、Team ordering、Gateway、Router 和 dispatcher：144 passed。
- Context models/service/ACL/credentials/chaos：120 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- 当前 group ledger 使用全文窗口与 Journal sequence，不做向量检索；这是首期明确非目标。
- partial warning 已进入 Journal/运行时模型，用户可见卡片文案在 Task 11 完成。

## 常驻员工改造 Task 7：来源可追溯的知识 Wiki

### 修改与原因

- canonical data 新增 knowledge page/index/review 三类加密文档；员工 Markdown 只物化已锚定文档，仍可从 Journal+Blob 全量重建。
- 新增异步 knowledge ingest：执行 history 的 payload hash 形成稳定 ingest ID，同源幂等；compile effect 在调用前记录 prepared/executing，异常形成无敏感正文的 review item，已完成任务状态不回滚。
- 编译器先做 secret、PII、prompt injection 和 hidden-reasoning 脱敏，再输出冻结 schema 的 claim/source link；所有 claim 必须精确引用已授权 history source。
- 查询严格先授权并读取 committed index，再读取候选 page；使用全文 token 与 wikilink 邻接扩展，结果始终携带 page/source citation。
- Wiki lint 检测 frontmatter、断链、孤儿页、缺失/错 hash 来源、重复 page ID、矛盾来源、陈旧索引和超大 AGENTS；source manifest 与知识日志是可恢复投影。
- Gateway 在已完成 execution history 发布后异步排入知识摄入，不让知识编译失败改变原任务终态。

### 验证

- knowledge compiler/query/security/recovery：8 passed。
- canonical data、Blob policy、read ports、composition、commit boundaries 与 Slock gateway：166 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- 首期编译器为可替换的确定性安全实现，尚未启用向量库；这是方案明确约束。未来接入模型编译器时仍必须经过同一 schema/source/脱敏门禁。
- 知识发布以文档级 commit 组成，不提供跨 page/index 的单帧事务；index 在每次摄入末尾发布，崩溃后由 pending ingest 恢复并最终收敛。

## 常驻员工改造 Task 8：持久 TeamRun 图与动态 Coordinator

### 修改与原因

- 新增冻结 `TeamRunV2`、Assignment、Decision 和纯 replay projection；任务、指令、贡献只写加密 Blob，Journal 保存引用、状态图、bounds、coordinator session key 和 effect disposition。
- `TeamCoordinatorActor` 按 tenant/chat/project 串行驱动 created→planning→dispatching→reviewing/revising→terminal；终态前拒绝 unresolved effect。
- 候选按显式 @、职责/能力匹配、READY 状态和 mailbox load 排序；role/capability 为空、BUSY/DEGRADED 的员工不会被硬分配，claim 使用 Journal CAS，fanout 上限 4。
- 新增可复用 `SessionCoordinatorDecisionProvider`，生产 coordinator 模式使用配置的 tool/model/profile/effort 和项目 cwd 启动主协调会话；严格解析冻结 JSON decision，拒绝虚构员工、伪造完成和越界图。
- 原 `EmployeeTeamService` 成为 `legacy_pipeline|coordinator` 显式 facade；默认仍为 legacy，真实租户证据前不静默切流。
- planning、assignment running、contribution committed、final notify executing 四个中断点均从 Blob+Journal 继续；最终回复使用稳定飞书 idempotency key 收敛。
- 修复并发 Team 写入暴露的 Router 两处 Journal head CAS 缺少 transaction guard，以及无 group ledger 的兼容 harness 在受理后误终止的问题。

### 验证

- Coordinator unit/contract/chaos：14 passed（含动态 fanout 与四点恢复）。
- Team legacy、配置合同和 stop-control：118 passed。
- Hire composition + stop-control：107 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- 默认模式仍是 `legacy_pipeline`；Task 12 必须以真实租户 main coordinator、员工 ACP session 和幂等通知证据作为切换门禁。
- Coordinator 当前将模型决策用于首轮任务分派，后续 review/revise 图由受控确定性状态机执行；如扩展为每轮模型决策，必须继续复用同一 schema/bounds 和来源门禁。

## 常驻员工改造 Task 9：群协作路由与选择性唤醒

### 修改与原因

- 新增冻结 `GroupRouteRequest/RouteDecision`，优先级固定为单员工直接 @、显式 Team task、已绑定 collaboration event、ambient chat；Bot 文本永远不能创建新任务。
- managed 群高置信闲聊只保留在 durable group ledger，dispatcher/Slock 不调用任何模型；多员工 @ 进入 Team，单员工 @ 通过普通 durable ingress 只投递该员工邮箱。
- Membership 增加 tenant/chat/bot principal 的唯一 ACTIVE 成员绑定，作为员工 Bot contribution 的入口权限；跨 tenant、非成员 Bot、伪造 assignment 一律拒绝。
- Coordinator 对 collaboration event 校验 live run、已完成 assignment、agent ownership 和唯一 causal ID；重复/过期/终态/越界事件不推进任务，也不再次唤醒模型。
- Outbox 增加 contribution provenance 事件，只有已终态且已绑定真实 employee app/generation 的快照可发布；最终结论仍由 Coordinator 的 main Bot 幂等 transport 发送。
- 直接 @ 的生产适配器生成普通 `im.message.receive_v1` employee ingress，不伪装 Team assignment，因此不依赖 Team effect，也不广播到其他员工。

### 验证

- group collaboration、direct mention 与 Bot loop guard：8 passed。
- Outbox、上述集成合同及 Feishu dispatcher：35 passed。
- Coordinator/legacy Team/Slock collaboration 回归：47 passed；Slock 路由专项：91 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- contribution provenance 已提供 production Outbox API 和 Coordinator loop guard；飞书侧未来若新增可验证的 Bot-message callback，只能携带该 causal metadata 调用，不能从可见正文反推 assignment。
- 默认 employee runtime/team runtime 仍未切到 actor/coordinator；发布门禁继续留到 Task 12。

## 常驻员工改造 Task 10：恢复、迁移与生命周期收口

### 修改与原因

- 新增统一 `EmployeeLifecycleSupervisor`，把恢复顺序固定为 Journal/data/workspace/group ledger/actor/Team/Channel/admission，并以完全相反的顺序关闭；任一阶段异常立即 fail-close，不开放入口。
- 生产组合根改为先恢复 canonical data、workspace 和群账本，再恢复员工 mailbox 与 Team coordinator；Channel 恢复完成后才标记 hire runtime ready，恢复轨迹可用于启动诊断。
- `EmployeeRuntimeSupervisor` 增加退休 fence：Fire 在撤权和归档前先取消活动 assignment、关闭 mailbox/session，并阻止同一员工 Actor 被意外重建。
- 新增旧员工 workspace v1 迁移器，只接受 canonical projection 作为输入；忽略 legacy mutable 文件，重复执行幂等，symlink 和冲突路径 fail-close。
- 组合层停机先关闭 admission/coordinator/actor，再 drain Outbox、关闭 Channel/data/Journal；对应启动、Fire 和 1/10/50 员工冷启动矩阵均有回归覆盖。

### 验证

- Task 10 启动顺序、重启矩阵、Fire authority/effects 与 hire composition：111 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- 当前重启矩阵在进程内构造 1/10/50 个冷 Actor，并验证 pending knowledge/Team/Channel/Fire 阶段不可跳过；真实租户的进程级重启与长连接恢复仍属于 Task 12 灰度门禁。
- 新 supervisor 提供统一生命周期合同；旧通用 Supervisor 尚未移除，删除兼容路径要等 actor/coordinator 默认切流后的真实证据。

## 常驻员工改造 Task 11：运行时可观察性与恢复动作

### 修改与原因

- 新增员工运行时状态卡和 HTML 预览，独立展示 Bot Channel、Actor/session、接单资格/mailbox、identity/knowledge generation、active run/assignment/checkpoint 与 context quality，避免把飞书连接在线误报为模型热驻留。
- `/roster` 通过 runtime facade 获取 secret-free 视图，显示 `Bot READY / Agent READY_COLD` 等双状态，并提供单员工详情入口；原 manifest 证据和原地授权操作保持不变。
- partial context 显示可继续但不完整的来源警告；action-required 卡保留 run/assignment、稳定 error code、已完成贡献与恢复建议，不再只返回笼统 `context_unavailable`。
- 新增管理员私聊动作：回收模型 session、从 canonical projection 重建 workspace、lint knowledge、重试已锚定 review item；handler 不读取 Actor/projector 内部结构，非管理员默认拒绝。
- knowledge review retry 新增 Journal 事件并复用同一 source/tenant/agent 边界；workspace lint 增加只读 issue report；Team renderer 保持每名员工一张任务卡并对长输出创建续接卡。

### 验证

- runtime cards、Slock status/roster、CardSession、knowledge retry、workspace lint、session reuse 与 hire composition：257 passed。
- 涉及模块 `ruff check` 与 `git diff --check`：通过。

### 后续风险

- UI 已接生产 facade，但真实飞书租户的卡片 schema、按钮回调和长卡续接仍需 Task 12 灰度证据。
- legacy one-shot 模式会如实显示 `Agent LEGACY_ONE_SHOT`；只有 actor 默认切流后才显示 READY_COLD/READY_WARM，不提前伪造常驻状态。

## 常驻员工改造 Task 12：自动灰度门禁（真实切流待验收）

### 修改与原因

- `shadow` employee runtime 在不增加模型调用的前提下构造与 Actor 相同的 bootstrap 输入，锚定 context/instruction/input/output digest 和 mismatch code；审计异常或 mismatch 不改变 legacy 结果。
- SlockEngine 暴露 canonical employee one-shot 调用计数，Team service 暴露显式 runtime mode，供真实租户证明 actor/coordinator 模式无 fallback。
- 真实租户 release manifest 新增 persistent Actor、direct mention、Team Coordinator、partial context、selective wake 和 persistent Fire 六组签名证据门禁，并继续要求 1/10/50 soak。
- 配置和 ADR 明确 shadow、actor、coordinator 语义；当前默认保持 `legacy_one_shot` / `legacy_pipeline`，回滚仍为恢复这两个模式并重启。

### 验证

- `uv run python -m pytest tests/autonomous/ -q`：2188 passed、1 skipped；跳过项是需要真实飞书租户证据的 selector。
- `uv run python -m pytest tests/test_slock*.py tests/test_feishu*.py tests/test_card_session.py -q`：2622 passed。
- `uv run ruff check src/autonomous/ src/agent_session/ src/slock_engine/ src/feishu/`、`uv run python -m src.main --validate`、文档引用测试和 `git diff --check` 全部通过。
- 修复两项测试基建漂移：UNKNOWN NLI 回退用例改用任务型输入；异步 Slock 测试不再在收集阶段向 `sys.modules` 永久注入假的已安装 SDK，消除跨文件顺序污染。
- grill-me 复审发现 shadow 原实现会用同一构造函数自我比较；现改为从 legacy one-shot 生产路径获取纯 prompt 预览并与 Actor bootstrap 输入比对，新增漂移回归测试，仍不会启动第二个 session 或增加模型调用。
- 一次将两套大型回归并行执行时，Channel bridge 的 10 秒子进程事件窗口超时；该无关用例单独连续 3 次通过，Autonomous 全量随后串行 2188/2188 通过，确认为并行资源争用而非产品回归。

### 未完成与风险

- 没有可用的真实飞书 staging/production 租户和独立签名 evidence bundle，因此不能勾选真实租户验收、删除固定 Team/one-shot rollback 路径或宣称整体完成。
- 本地自动测试只能证明实现合同，不能替代员工 Bot 身份、移动端 Slash、群内贡献、断线重连和 1/10/50 长时间 soak 的外部观察。

## 常驻员工默认切流与可执行验收入口

### 修改与原因

- `Settings` 与 `.env.example` 默认切到 `actor` / `coordinator`；生产组合根遇到旧 settings 对象缺少模式字段时也选择持久模式，不再静默留在 v1。
- Actor 和 Coordinator 仍保持失败不自动回退；`legacy_one_shot` / `legacy_pipeline` 只作为显式紧急回滚模式保留，等待真实租户签名证据后删除实现。
- `validate_employee_tenant.py --template-out` 现在以 exclusive、0600 文件生成 tenant-bound 验收清单；全部 required assertion 初始为 false，soak 数值为 0/None，不能误作为通过证据。
- 运行日志审计确认用户看到的 `context_unavailable` 来自 2026-07-17 03:20 启动、尚未加载本轮修复的旧进程；代码提交后必须受控重启并以新 `[RESTART]` 标记后的日志复核。

### 当前验证

- 默认切流与生产组合根 TDD：先观察 3 个预期失败，再实现后通过。
- `uv run python -m pytest tests/autonomous/ -q`：2189 passed、1 个真实租户 selector skipped。
- `uv run python -m pytest tests/test_slock*.py tests/test_feishu*.py tests/test_card_session.py -q`：2622 passed。
- 配置/文档验证：35 passed；Ruff、配置校验和 `git diff --check` 通过，校验摘要明确显示 `actor` / `coordinator`。

### 后续风险

- 本机不存在 `GHOSTAP_EMPLOYEE_*` release binding、evidence bundle 或 checkpoint，无法在本轮自动制造真实租户验收结果。
- `1297e279` 推送后已受控重启；新 PID 建立飞书 WebSocket、恢复两个 Slock 群，重启后的窗口尚未出现 `context_unavailable`。

## Lark WebSocket 连接日志凭据泄露修复

### 修改与原因

- 部署核查发现主 Bot 把 `ObservedLarkWSClient` 配为 DEBUG；SDK 会在 INFO 打印带 `access_key` 和 `ticket` 的完整连接 URL，并因自带 handler + root propagation 写入两次日志。
- 主 Bot WebSocket SDK 日志级别改为 WARNING。连接健康不依赖 SDK DEBUG 文本，而是继续使用 `ObservedLarkWSClient` lifecycle hooks，因此断线监控能力不受影响。
- 员工 Channel 原本已经固定 ERROR，无需修改。

### 验证

- 回归测试先证明主连接收到 DEBUG 而失败，修改后证明所有重连实例均收到 WARNING。
- WebSocket/飞书相关测试：198 passed；Ruff 通过。

### 部署验证

- 安全修复 `d3456b04` 推送后已使用 `GHOSTAP_LOG_MODE=truncate ./restart.sh restart` 受控重启，旧凭据日志已轮换清除。
- 新进程 PID 1200855 正常运行并有一条已建立的出站连接；启动恢复两个 Slock 群，且运行环境没有覆盖 runtime mode，配置校验确认 `actor` / `coordinator` 生效。
- 新日志没有 `access_key=`、`ticket=`、`context_unavailable`、Traceback、CRITICAL 或 runtime composition error。
- 真实租户的 direct mention、Team 协调和 1/10/50 soak 仍属于独立验收门禁；本次健康检查不替代外部签名证据。

## 员工 Channel 重连卡死自愈

### 修改与原因

- 部署后进程级核查发现员工沙箱 worker 仍存活，但已经没有 TCP/socket fd；Canonical Workforce 与 Journal 仍保留员工 ACTIVE，导致员工无法继续实时监听。
- 根因是 SDK 发出 `reconnecting` 后 supervisor 会把 Channel 改为 `STARTING/channel-reconnecting`，但 runtime monitor 只处理 CRASHED、FAILED、STOPPED，永久忽略卡死的 STARTING generation。
- supervisor 现在在 reconnecting 时撤销旧 connection metadata 并记录 `reconnecting_at`；若 180 秒仍未收到新的 observed READY，monitor 会持久进入 channel revalidation 并启动下一 generation。
- 仍在 180 秒窗口内的 SDK 正常自动重连不受影响；新 READY 会替换 reconnect metadata，避免无谓重启。

### 验证

- TDD 先观察两个预期失败：reconnecting 状态缺少开始时间、stalled reconnect 不触发 revalidation；实现后两项通过。
- Channel/Hire/Gateway/隔离相关回归：222 passed；Ruff 与 `git diff --check` 通过。
- `uv run python -m pytest tests/autonomous/ -q`：2190 passed、1 skipped；唯一跳过项仍是真实租户 selector。

### 后续验证

- 推送并重启后必须确认员工 worker 拥有已建立的 socket，Journal generation 恢复到 ACTIVE，且观察窗口内不再重新落入无 socket 的假活状态。
