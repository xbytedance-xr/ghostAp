# 2026-04-20 项目记录

## 全量统一提交落地：str(e) or repr(e) → get_error_detail(e) + Backlog 归档

### 任务描述
将「审查执行异常: TimeoutError (empty message)」改进建议的最终收尾工作——30+ 文件 90+ 处 `str(e) or repr(e)` → `get_error_detail(e)` 全量统一 + `TestBanStrOrReprPattern` 静态扫描门禁 + Backlog B-001~B-007 全部归档——验证后统一提交。

### 执行内容
1. **TimeoutError 专项测试**: `test_empty_error_guard.py` — 106 passed, 2.34s ✅
2. **全量回归测试**: `pytest tests/ -x -q` — 2323 passed, 0 failures, 49.24s ✅
3. **5 项静态 grep 扫描零残留**:
   - 裸 `raise TimeoutError()`: 0 匹配 ✅
   - 裸 `asyncio.wait_for`: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部（预期）✅
   - 裸 `f"{e}"` 异常格式化: 0 匹配 ✅
   - 裸 logger `%s, e)`: 0 匹配 ✅
   - `str(e) or repr(e)`: 仅 `errors.py:157` 和 `errors.py:184`（白名单实现层）✅
4. **Backlog 确认**: B-001~B-007 全部 Done (7/7)，零 Open 条目 ✅
5. **git 提交**: `d1b87f1` — 44 files changed, +288/-131 ✅
6. **提交后二次回归**: 2323 passed, 48.90s，零回归 ✅

### 技术要点
- `get_error_detail()` 对非 TimeoutError 异常语义等价于 `str(e) or repr(e)`，零行为变更
- 仅 `errors.py` 自身实现层 2 处保留原始 `str(exc) or repr(exc)` 模式（白名单）
- `TestBanStrOrReprPattern` 确保此模式不会再次引入

### 提交记录
- `d1b87f1` fix(errors): unify str(e) or repr(e) → get_error_detail(e) across 30+ files (90+ sites)

---

## 第十八次闭环验证：9 步任务列表严格顺序执行确认

### 任务描述
按 9 步任务列表严格顺序执行「审查执行异常: TimeoutError (empty message)」改进建议的验证闭环，每步完成后自检，含依赖关系约束。

### 执行内容
1. **TimeoutError 专项测试**: `test_empty_error_guard.py` — 106 passed, 2.36s ✅
2. **全量测试**: `pytest tests/ -q` — 2323 passed, 0 failures, 48.87s ✅
3. **spec_engine/review.py 审查**: `conduct_review()` 使用 `compute_adaptive_timeout`(L260-267) + `RetryPolicy`+`total_timeout`(L272-274) + `handle_review_exception`(L297-310) + 成功路径 6 计数器归零+`recent_outcomes`(L285-296) + 熔断 skip+lint fallback(L156-235)，logger 使用 `get_error_detail(e)` ✅
4. **loop_engine/engine.py 审查**: `_conduct_review()`(L834-1002) 与 spec_engine 防御模式完全一致；`execute()`/`resume()` 均有独立 `except TimeoutError`(L351/L1214) 在 `except Exception` 之前；`_detect_convergence()`(L1032-1034) 跳过 `review_failed` 轮次 ✅
5. **review_helpers.py 审查**: `compute_adaptive_timeout`(120→60→30→30 floored, hard_floor=15s 防归零) + `compute_exponential_cooldown`(3→6→12→12 capped) + `SlidingWindowTracker`(window_size=10, threshold=0.3, 窗口未满不触发, deque maxlen 无膨胀) + `_is_timeout_error` 三重检测 ✅
6. **Backlog 确认**: B-001~B-007 全部 Done (7/7)，零 Open 条目 ✅
7. **缺口修复**: 任务 1-6 零缺陷发现，无需修复 ✅

### 验证结论
10 层纵深防御体系持续完整，第十八次独立确认无退化、无新缺口、零代码改动需求。

---

## 第十七次闭环验证：完整 Plan 分解执行确认

### 任务描述
基于 Spec→Plan→Task 方法论，将「审查执行异常: TimeoutError (empty message)」改进建议分解为 8 个可执行任务（含依赖关系），按依赖顺序执行并逐一验证闭合。

### 执行内容
1. **TimeoutError 专项测试**: `test_empty_error_guard.py` — 106 passed, 2.36s ✅
2. **全量测试**: `pytest tests/ -v` — 2323 passed, 0 failures, 47.49s ✅
3. **spec_engine/review.py 审查**: `conduct_review()` 使用 `compute_adaptive_timeout` + `RetryPolicy` + `total_timeout` + `handle_review_exception`，成功路径重置全部熔断器计数器 + 记录 `recent_outcomes`，logger 使用 `get_error_detail(e)` ✅
4. **loop_engine/engine.py 审查**: `_conduct_review()` 与 spec_engine 防御模式完全一致；`execute()`/`resume()` 均有独立 `except TimeoutError` 分支；`_detect_convergence()` 跳过 `review_failed` 轮次 ✅
5. **review_helpers.py 审查**: `compute_adaptive_timeout` (base=120, min=30, hard_floor=15s 不归零) + `compute_exponential_cooldown` (base=3, max=12 有上限) + `SlidingWindowTracker` (window_size=10, threshold=0.3, 窗口未满不触发) + `handle_review_exception` 6 步完整流程 ✅
6. **Backlog 确认**: B-001~B-007 全部 Done，零 Open 条目 ✅
7. **缺口修复**: 任务 1-6 零缺陷发现，无需修复 ✅

### 验证结论
10 层纵深防御体系持续完整，第十七次独立确认无退化、无新缺口、零代码改动需求。

---

## 第十六次闭环验证：任务列表逐步执行确认

### 任务描述
按 8 步任务列表严格顺序执行「审查执行异常: TimeoutError (empty message)」改进建议的验证闭环，每步完成后自检。

### 执行内容
1. **依赖同步**: `uv sync --group dev` — 84 packages resolved, 83 audited ✅
2. **TimeoutError 专项测试**: `test_empty_error_guard.py` — 106 passed, 2.36s ✅
3. **静态回归 lint 门禁**: TestNoBareFStringInUserVisibleErrors(4) + TestBanStrOrReprPattern(1) + TestNoBareRaiseTimeoutError(1) = 6 passed, 0.24s ✅
4. **全量测试**: `pytest tests/ -q` — 2323 passed, 0 failures, 47.87s ✅
5. **Backlog 确认**: B-001~B-007 全部 Done，零 Open 条目 ✅
6. **缺口修复**: 无失败用例，无需修复 ✅

### 验证结论
10 层纵深防御体系持续完整，第十六次独立确认无退化、无新缺口、零代码改动需求。

---

## 第十五次闭环验证：任务分解执行确认

### 任务描述
将「审查执行异常: TimeoutError (empty message)」改进建议的实现方案分解为 8 个可执行任务，按依赖顺序逐一执行并验证闭合。

### 执行内容
1. **依赖同步**: `uv sync --group dev` ✅
2. **TimeoutError 专项测试**: `test_empty_error_guard.py` 106 tests passed ✅
3. **静态回归 lint 门禁**: TestNoBareFStringInUserVisibleErrors(4) + TestBanStrOrReprPattern(1) + TestNoBareRaiseTimeoutError(1) = 6 passed ✅
4. **全量测试**: 2323 tests passed, 0 failures, 47.85s ✅
5. **Backlog 确认**: B-001~B-007 全部 Done，零 Open 条目 ✅
6. **缺口修复**: 无失败用例，无需修复 ✅

### 验证结论
10 层纵深防御体系持续完整，第十五次独立确认无退化、无新缺口、零代码改动需求。改进建议已完全落实并归档关闭。

---

## 第十四次闭环验证：审查执行异常改进建议最终归档确认

### 任务描述
针对「如何落实改进建议：审查执行异常: TimeoutError (empty message)？」执行最终归档确认。通过全量测试 + TimeoutError 专项测试 + Backlog 审查 + 三项静态零残留验证，确认 10 层纵深防御体系完整闭环、7 个审计缺口（B-001~B-007）全部关闭、7 个静态回归门全部绿色，无需任何代码改动。

### 执行内容
1. **全量测试**: 2323 tests passed, 0 failures, 47.72s ✅
2. **TimeoutError 专项**: 245 tests passed, 2078 deselected ✅
3. **Backlog 确认**: B-001~B-007 全部标记 Done，零 Open 条目 ✅
4. **静态零残留验证 3 项**:
   - `str(e/exc/err) or repr(...)` 在 src/ 中: 仅 `errors.py:157` 和 `errors.py:184`（白名单实现层），零泄漏 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部，零直接调用 ✅
   - `raise TimeoutError()` 在 src/ 中: 零残留 ✅

### 验证结论
10 层纵深防御体系完整闭环，第十四次独立确认无退化、无新缺口、零代码改动需求。改进建议已完全落实，问题归档关闭。

| 层级 | 防御 | 状态 |
|------|------|------|
| 1 | safe_wait_for 源头包装 | ✅ |
| 2 | fmt_error/get_error_detail 核心兜底 | ✅ |
| 3 | 用户可见路径统一 get_error_detail | ✅ |
| 4 | 日志路径统一 get_error_detail (30+ 文件 90+ 处) | ✅ |
| 5 | 三引擎独立 except TimeoutError | ✅ |
| 6 | review_helpers 统一异常处理+自适应超时+指数退避 | ✅ |
| 7 | 收敛检测跳过 review_failed 轮次 | ✅ |
| 8 | 7 个回归 lint 扫描器 | ✅ |
| 9 | SlidingWindowTracker 滑动窗口熔断 | ✅ |
| 10 | lightweight_lint 本地 lint 降级 | ✅ |

**Backlog 状态汇总**:
| ID | Severity | 状态 |
|----|----------|------|
| B-001 | Medium | Done |
| B-002 | Low | Done |
| B-003 | Low | Done |
| B-004 | Low | Done |
| B-005 | Low | Done |
| B-006 | Low | Done |
| B-007 | Low | Done |

---

## B-006 + 全代码库 `str(e) or repr(e)` → `get_error_detail(e)` 统一

### 任务描述
关闭最后一个审计缺口 B-006（sync_adapter.py 4 处），并将全代码库剩余所有 `str(e) or repr(e)` 及其变量名变体（`exc/err/ex/cb_exc/error`）统一替换为 `get_error_detail()`，消除风格不一致性并增强空消息防御。

### 执行内容
1. **B-006 修复**: sync_adapter.py L682/L819/L1499/L1521 四处 → `get_error_detail(e)` ✅
2. **ACP 层**: client.py 10 处 + session.py 4 处 + manager.py 2 处 → `get_error_detail()` ✅
3. **Feishu 层**: ws_client.py 26 处 + handlers/ 7 处 + action_dispatcher 2 处 + image_handler 1 处 + renderers/base 1 处 ✅
4. **Engine 层**: loop_engine/engine.py 8 处 + spec_engine/engine.py 5 处 + spec_engine 子模块 (artifacts/criteria/discovery/persistence/review/session_utils/task_persistence) 16 处 ✅
5. **其余模块**: agent/intent_recognizer 2 处 + agent_session 1 处 + coco_model/manager 4 处 + engine_base 1 处 + main 2 处 + project/manager 4 处 + sandbox/executor 1 处 + tasking/scheduler 2 处 ✅
6. **TTADK + Utils**: ttadk/cache 5 处 + ttadk/manager 6 处 + ttadk/model_fetcher 1 处 + ttadk/strategies 4 处 + ttadk/env_sandbox 1 处 + utils/gc_monitor 2 处 + utils/lightweight_lint 3 处 + utils/metrics_exporter 1 处 + utils/ttadk_wrapper 2 处 + utils/retry 1 处 ✅
7. **Worktree**: dispatcher.py 1 处 ✅
8. **静态扫描门禁**: 新增 `TestBanStrOrReprPattern` 回归测试，扫描 src/ 全目录确保零残留（仅 errors.py 自身实现豁免）✅
9. **全量测试**: 2323 tests passed, 0 failures, 48.68s ✅

### 技术要点
- 总计 30+ 文件 90+ 处替换，覆盖 6 种变量名变体：`e`, `exc`, `err`, `ex`, `cb_exc`, `error`, `last_err`
- `get_error_detail()` 对非 TimeoutError 异常语义等价于 `str(e) or repr(e)`，但对空消息 TimeoutError 额外提供中文友好文案
- `errors.py` 自身的 2 处 `str(exc) or repr(exc)` 是 `get_error_detail` 实现的底层逻辑，不替换
- 新增的 `TestBanStrOrReprPattern` 静态扫描断言确保此模式不会再次引入

### Backlog 状态
- B-006: Done (本次修复)
- B-007: Done (本次全量统一)
- 所有 B-001~B-007 全部 Done，无 Open 条目

---

## 第十三次闭环验证：10 层防御体系零缺口确认

### 任务描述
按 11 步任务清单执行第十三次独立验证，通过全量测试 + 4 项专项测试 + 2 项 Grep 静态扫描 + Backlog 审查，确认 10 层纵深防御体系完整无退化，零新增缺口。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures, 48.94s ✅
2. **TimeoutError 专项**: 245 tests passed ✅
3. **回归 lint**: test_lightweight_lint.py 15 tests passed ✅
4. **空消息守卫**: test_empty_error_guard.py 105 tests passed ✅
5. **E2E 超时**: test_timeout_e2e.py 40 tests passed ✅
6. **Grep 扫描裸 asyncio.wait_for**: 仅 safe_wait_for 内部 1 处（预期），零缺口 ✅
7. **Grep 扫描 TimeoutError except 块**: 14 处全部受保护（get_error_detail/重包装/空消息补全），零缺口 ✅
8. **Backlog 确认**: B-001~B-005 全部 Done, 无新增 Open 条目 ✅
9. **缺口修复**: 无需修复（零缺口）✅
10. **回归验证**: 无需（无修复）✅

### 验证结论
10 层纵深防御体系持续完整，第十三次独立确认无退化、无新缺口、零代码改动需求。

---

## 第十二次闭环验证 + ws_client/dispatcher 残余 TimeoutError 日志路径修复

### 任务描述
执行 11 步任务清单闭环验证「审查执行异常: TimeoutError (empty message)」，发现并修复 3 处 TimeoutError except 块中日志层残余 `str(e) or repr(e)` → `get_error_detail(e)` 不一致。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures ✅
2. **TimeoutError 专项**: 245 tests passed ✅
3. **回归 lint**: test_lightweight_lint.py 15 tests passed ✅
4. **空消息守卫**: test_empty_error_guard.py 105 tests passed ✅
5. **E2E 超时**: test_timeout_e2e.py 40 tests passed ✅
6. **Grep 扫描裸 asyncio.wait_for**: 仅 safe_wait_for 内部 1 处（预期）✅
7. **Grep 扫描 TimeoutError except 块**: 发现 3 处残余不一致 ⚠️
   - `src/feishu/ws_client.py:1616` — `str(e) or repr(e)` → `get_error_detail(e)` ✅ 已修复
   - `src/feishu/ws_client.py:2263` — `str(e) or repr(e)` → `get_error_detail(e)` ✅ 已修复
   - `src/worktree_engine/dispatcher.py:123` — `str(te) or repr(te)` → `get_error_detail(te)` ✅ 已修复
8. **Backlog 确认**: B-001~B-005 全部 Done, 无新增 Open ✅
9. **回归验证修复后**: 2322 tests passed, 零回归 ✅

### 技术要点
- 3 处修复均为 **日志层** 的 `logger.warning` 调用，用户可见回复已硬编码中文友好文案，不影响用户体验
- ws_client.py 已在顶层 import `get_error_detail`（L59），dispatcher.py 已在 L10 import
- 修复后 src/ 中所有 `except TimeoutError` / `except asyncio.TimeoutError` 块的日志路径均统一使用 `get_error_detail(e)`

### 文件改动
- `src/feishu/ws_client.py` — L1616, L2263: `str(e) or repr(e)` → `get_error_detail(e)`
- `src/worktree_engine/dispatcher.py` — L123: `str(te) or repr(te)` → `get_error_detail(te)`

---

## B-005 修复：engine_base.py / spec.py TimeoutError 分支 logger 统一到 get_error_detail

### 任务描述
修复 Backlog B-005（Low severity）：`engine_base.py` 4 处 + `spec.py` 2 处 TimeoutError/Exception 分支 logger 中 `str(e) or repr(e)` 替换为 `get_error_detail(e)`，与项目全局约定对齐。

### 执行内容
1. **`engine_base.py:_safe_execute_engine` L190 (TimeoutError)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
2. **`engine_base.py:_safe_execute_engine` L192 (Exception)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
3. **`engine_base.py:_safe_lifecycle_action` L250 (TimeoutError)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
4. **`engine_base.py:_safe_lifecycle_action` L252 (Exception)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
5. **`spec.py:run_spec_engine` L168 (TimeoutError)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
6. **`spec.py:run_spec_engine(recover)` L710 (TimeoutError)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
7. **`spec.py` 移除 2 处冗余 local import**: `from ...utils.errors import get_error_detail`（头部已有导入，local import 导致 UnboundLocalError）✅
8. **全量测试**: 2322 passed, 零回归 ✅
9. **回归 lint**: test_empty_error_guard.py 105 passed, 零违规 ✅
10. **Grep 验证**: engine_base.py/spec.py 中 `str(e) or repr(e)` 残留 = 0 ✅
11. **Backlog B-005**: 新建 → Done ✅

### 技术要点
- `engine_base.py:_safe_lifecycle_action` 需要将 `from ...utils.errors import get_error_detail` local import 提前到 logger 调用之前
- `spec.py` 的两个嵌套函数 `run_spec_engine` 内有冗余 local import，Python 编译时将 `get_error_detail` 视为 local 变量，导致 import 语句前使用时 `UnboundLocalError`；头部 line 16 已有导入，移除冗余 local import 即可
- Backlog B-001~B-005 全部 Done，无 Open 条目

---

## B-004 修复：DeepEngine logger 路径统一到 get_error_detail

### 任务描述
修复 Backlog B-004（Low severity）：`DeepEngine` 中 4 处 `str(e) or repr(e)` 内部 logger 路径统一替换为 `get_error_detail(e)`，与项目全局约定对齐。

### 执行内容
1. **`_drain_pending_context` L336 (TimeoutError)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
2. **`_drain_pending_context` L339 (Exception)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
3. **`_build_on_event` L176 (debug 回调异常)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
4. **`load_state` L541 (加载状态失败)**: `str(e) or repr(e)` → `get_error_detail(e)` ✅
5. **全量测试**: 2322 passed, 零回归 ✅
6. **回归 lint**: test_empty_error_guard.py 105 passed, 4 项静态扫描零违规 ✅
7. **Grep 验证**: engine.py 中 `str(e) or repr(e)` 残留 = 0 ✅
8. **Backlog B-004**: Open → Done ✅

### 技术要点
- 使用 local import `from ..utils.errors import get_error_detail` 保持与文件内既有风格一致
- 修复范围扩大至 engine.py 全部 4 处不一致路径（B-004 原始仅指 L336，额外覆盖 L176/L339/L541）
- Backlog B-001~B-004 全部 Done，无 Open 条目

## 第十一次独立验证：15 项任务清单全量闭环确认

### 任务描述
按 15 项任务清单执行第十一次独立验证，通过全量测试 + 专项测试 + 4 项静态扫描 + 10 层关键代码抽查 + Backlog 审查，确认 10 层纵深防御体系持续完整。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures, 47.63s ✅
2. **TimeoutError 专项**: 6 个测试文件 249 tests passed, 3.13s ✅
3. **静态回归扫描 4 项零违规**:
   - 裸 `f"{e}"` 异常格式化: 0 匹配 ✅
   - 裸 `asyncio.wait_for`: 0 匹配（仅 safe_wait_for 内部合法调用）✅
   - 裸 `logger.xxx("%s", e)` 无守卫: 0 匹配 ✅
   - 裸 `raise TimeoutError()` 无消息: 0 匹配 ✅
4. **10 层关键代码抽查**:
   - Layer 1 `safe_wait_for`: 空消息包装 + label + from exc ✅
   - Layer 2 `fmt_error`/`get_error_detail`: 空消息兜底 + 不返回空串 + 10 层链遍历 ✅
   - Layer 3 `_run_async`: 空消息检测 + 描述性消息(timeout+agent_type) + from e ✅
   - Layer 4 `send_prompt`: TimeoutError 重包装 + cancel() + from e ✅
   - Layer 5 `handle_review_exception`: 6 项子功能全部完整 ✅
   - Layer 6 `LoopReporter.format_iteration_done`: 空/"(empty message)" 过滤 ✅
   - Layer 7 `build_review_error_suggestion`: 三分支文案完整 ✅
   - Layer 8 `SlidingWindowTracker`: 滑动窗口 + 成功率 + 阈值熔断 ✅
   - Layer 9 `lightweight_lint`: ast.parse + ruff + 超时 + 双开关 ✅
   - Layer 10 三引擎 `except TimeoutError`: WARNING 级别 + `get_error_detail` ✅
5. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done ✅
6. **新发现**: DeepEngine._drain_pending_context L336 使用 `str(e) or repr(e)` 而非 `get_error_detail(e)`，属内部 logger 路径，Low severity，录入 Backlog B-004

### 验证结论
10 层纵深防御体系持续完整，第十一次独立确认无退化、无用户可见缺口。1 处 Low severity 内部日志不一致录入 Backlog，不影响防御体系闭环状态。

---

## 第十次独立验证：TimeoutError (empty message) 10 层防御体系完全闭环确认

### 任务描述
按 6 步任务清单执行第十次独立验证，通过全量测试 + 专项测试 + 静态回归扫描 + Backlog 审查，确认 10 层纵深防御体系持续完整，问题完全闭环。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures, 47.11s ✅
2. **TimeoutError 专项**: 6 个测试文件（test_empty_error_guard/test_timeout_e2e/test_review_timeout/test_async_helpers/test_review_helpers/test_safe_wait_for_integration）249 tests passed, 3.13s ✅
3. **静态回归扫描 4 项零违规**:
   - 裸 `f"{e}"` 异常格式化: 0 匹配 ✅
   - 裸 `asyncio.wait_for`: 0 匹配（仅 safe_wait_for 内部）✅
   - 裸 `logger.xxx("%s", e)` 无守卫: 0 匹配 ✅
   - 裸 `raise TimeoutError()` 无消息: 0 匹配 ✅
4. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done，无新增条目 ✅

### 验证结论
10 层纵深防御体系持续完整，第十次独立确认无退化、无新缺口。改进建议已完全落实，问题彻底闭环，无需额外代码改动。

---

## 第九次独立验证：TimeoutError (empty message) 10 层防御体系持续闭环确认

### 任务描述
按 6 步任务清单执行第九次独立验证，通过全量测试 + 专项测试 + 静态回归扫描，确认 10 层纵深防御体系持续完整。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures, 47.83s ✅
2. **TimeoutError 专项**: `test_empty_error_guard.py` + `test_timeout_e2e.py` 145 tests passed, 2.59s ✅
3. **静态回归扫描 5 项零违规**:
   - 裸 `f"{e}"` 在用户可见错误中: 0 匹配 ✅
   - 裸 `f"{e}"` 在 logger 中: 0 匹配 ✅
   - 裸 `asyncio.wait_for`: 0 匹配（仅 safe_wait_for 内部）✅
   - 裸 `logger.xxx("%s", e)` 无守卫: 0 匹配 ✅
   - 裸 `raise TimeoutError()` 无消息: 0 匹配 ✅

### 验证结论
10 层纵深防御体系持续完整，第九次独立确认无退化、无新缺口。改进建议已完全落实，无需额外代码改动。

---

## 第八次独立验证：TimeoutError (empty message) 10 层防御体系最终闭环确认

### 任务描述
按 9 步任务清单执行第八次独立验证，通过全量测试 + 专项测试 + 静态扫描 + Backlog 审查 + 10 层关键文件抽查，完成最终闭环确认。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures, 48.38s ✅
2. **TimeoutError 专项**: `pytest -k timeout` 245 tests passed, 4.75s ✅
3. **回归 lint 专项**: `test_empty_error_guard.py` 105 tests passed, 2.34s ✅
4. **E2E 全链路**: `test_timeout_e2e.py` 40 tests passed, 0.72s ✅
5. **Grep 静态扫描 4 项零违规**:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部 ✅
   - `f"{e}"` 裸异常格式化在 src/ 中: 0 匹配 ✅
   - logger 裸 `%s", e)` 在 src/ 中: 0 匹配 ✅
6. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done，无新增条目 ✅
7. **10 层防御关键文件抽查**: 10/10 全部通过 ✅
   - safe_wait_for (async_helpers.py:13-43) ✅
   - fmt_error + _has_timeout_in_chain (errors.py:94-115) ✅
   - get_error_detail (errors.py:144-150) ✅
   - Deep 引擎 except TimeoutError (engine.py:278, 438) ✅
   - Loop 引擎 except TimeoutError (engine.py:350, 1213) ✅
   - Spec 引擎 except TimeoutError (engine.py:460, 1257) ✅
   - SlidingWindowTracker (review_helpers.py:91-136) ✅
   - lightweight_lint (lightweight_lint.py) ✅
   - handle_review_exception (review_helpers.py:180-401) ✅
   - LoopReporter empty message filter (reporter.py:101-103) ✅

### 验证结论
10 层纵深防御体系持续完整，第八次独立确认无退化、无新缺口。改进建议已完全落实，无需额外代码改动。

---

## 第七次独立验证：TimeoutError (empty message) 10 层防御体系持续完整

### 任务描述
按 12 步任务列表执行第七次独立验证，确认 10 层纵深防御体系无退化、无新缺口。

### 执行内容
1. **Grep 静态扫描 4 项零违规**:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部 ✅
   - `f"{e}"` 裸异常格式化在 src/ 中: 0 匹配 ✅
   - logger 裸 `%s", e)` 在 src/ 中: 0 匹配 ✅
2. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done ✅
3. **回归 lint 专项**: `test_empty_error_guard.py` 105 tests passed, 2.34s ✅
4. **E2E 全链路**: `test_timeout_e2e.py` 40 tests passed, 0.72s ✅
5. **TimeoutError 专项**: `pytest -k timeout` 245 tests passed, 4.73s ✅
6. **全量测试**: 2322 tests passed, 0 failures, 46.32s ✅
7. **代码审查 4 项**:
   - `_run_async` (sync_adapter.py:1574-1578): 空消息包装为 `"ACP 异步操作超时 ({timeout}s)"` ✅
   - `send_prompt` (sync_adapter.py:1477-1480): 明确 re-raise `"ACP prompt 执行超时 ({timeout}s)"` ✅
   - `handle_review_exception` (review_helpers.py:268): 使用 `get_error_detail(e)` 产出非空用户友好文案 ✅
   - `safe_wait_for` (async_helpers.py:38-43): 空消息填充为 `"{label}超时 ({timeout}s)"` ✅

### 验证结论
10 层纵深防御体系持续完整，第七次独立确认无退化、无新缺口。改进建议已完全落实，无需额外代码改动。

---

## 第六次独立验证：TimeoutError (empty message) 10 层防御体系持续完整

### 任务描述
按 13 步任务列表执行第六次独立验证，确认 10 层纵深防御体系无退化、无新缺口。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures, 47.40s ✅
2. **Grep 静态扫描 4 项零违规**:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部 ✅
   - `f"{e}"` 裸异常格式化在 src/ 中: 0 匹配 ✅
   - logger 裸 `%s", e)` 在 src/ 中: 0 匹配 ✅
3. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done ✅
4. **代码审查 3 项**:
   - `handle_review_exception` (review_helpers.py:268): 使用 `get_error_detail(e)` + `build_review_error_suggestion` 产出非空用户友好文案 ✅
   - `_run_async` (sync_adapter.py:1574-1578): 空消息包装为 `"ACP 异步操作超时 ({timeout}s)"` ✅
   - `safe_wait_for` (async_helpers.py:38-43): 空消息填充为 `"{label}超时 ({timeout}s)"` ✅
5. **回归 lint 专项**: `test_empty_error_guard.py` 105 tests passed, 2.37s ✅

### 验证结论
10 层纵深防御体系持续完整，第六次独立确认无退化、无新缺口。改进建议已完全落实，无需额外代码改动。

---

## 第五次独立验证：TimeoutError (empty message) 10 层防御体系持续完整

### 任务描述
按实现方案 11 步任务列表执行第五次独立验证，确认 10 层纵深防御体系无退化。

### 执行内容
1. **Grep 静态扫描 4 项零违规**:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部 ✅
   - `f"{e}"` 裸异常格式化在 src/ 中: 0 匹配 ✅
   - logger 裸 `%s", e)` 在 src/ 中: 0 匹配 ✅
2. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done ✅
3. **回归 lint 专项**: `test_empty_error_guard.py` 105 tests passed, 2.34s ✅
4. **E2E 全链路**: `test_timeout_e2e.py` 40 tests passed, 0.73s ✅
5. **TimeoutError 专项**: `pytest -k timeout` 245 tests passed, 4.74s ✅
6. **全量测试**: 2322 tests passed, 0 failures, 49.86s ✅

### 验证结论
10 层纵深防御体系持续完整，第五次独立确认无退化、无新缺口。改进建议已完全落实，无需额外代码改动。

---

## 第四次独立验证：TimeoutError (empty message) 10 层防御体系持续完整

### 任务描述
按实现方案 7 步任务列表执行第四次独立验证，确认 10 层纵深防御体系无退化。

### 执行内容
1. **Grep 静态扫描 4 项零违规**:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部 ✅
   - `f"{e}"` 裸异常格式化在 src/ 中: 0 匹配 ✅
   - logger 裸 `%s", e)` 在 src/ 中: 0 匹配 ✅
2. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done ✅
3. **回归 lint 专项**: `test_empty_error_guard.py` 105 tests passed, 2.34s ✅
4. **E2E 全链路**: `test_timeout_e2e.py` 40 tests passed, 0.72s ✅
5. **TimeoutError 专项**: `pytest -k timeout` 245 tests passed, 4.73s ✅
6. **全量测试**: 2322 tests passed, 0 failures, 46.99s ✅

### 验证结论
10 层纵深防御体系持续完整，第四次独立确认无退化、无新缺口。改进建议已完全落实，无需额外代码改动。

---

## 三项增量改进：Metrics Exporter + 滑动窗口熔断 + Lint 降级

### 任务描述
在已有 10 层纵深防御体系基础上，实现三项增量改进：
(A) 运维监控——可插拔 metrics exporter 框架
(B) 动态熔断——基于滑动窗口历史成功率的智能熔断
(C) 降级策略——熔断期间本地 lint 替代完全跳过

### 执行内容

#### A. Metrics Exporter 框架
1. **新增 `src/utils/metrics_exporter.py`**:
   - `ReviewMetricsExporter` Protocol 接口（`export_metrics(metrics, prefix)`)
   - `LoggerExporter`：默认实现，保持原有 logger.info 行为
   - `JsonLinesExporter`：追加 JSON Lines 到文件，可对接 Filebeat/Fluentd
   - `get_metrics_exporter()` 工厂函数 + `reset_metrics_exporter()` 测试辅助
2. **修改 `src/utils/review_helpers.py`**: 将硬编码 logger.info 改为通过 exporter 接口输出，fallback 保留原始 logger
3. **`config.py` 新增**: `review_metrics_exporter_type`（默认 "logger"）、`review_metrics_jsonl_path`
4. **`tests/test_metrics_exporter.py`**: 17 个测试（Protocol 一致性、Logger/JsonLines 输出格式、工厂缓存、集成）

#### B. 滑动窗口动态熔断
1. **新增 `SlidingWindowTracker` 类** (`review_helpers.py`):
   - `deque(maxlen=window_size)` 维护最近 N 次 outcome
   - `success_rate()` 计算成功率，`should_open_circuit(threshold)` 判断触发
   - `from_list()` 反序列化支持
2. **CircuitState 新增 `recent_outcomes` 字段**: Spec `ReviewCircuitState` + Loop `LoopReviewCircuitState`，to_dict/from_dict 序列化（旧快照兼容）
3. **`handle_review_exception` 集成**: 异常路径录入 "timeout"/"error" outcome，滑动窗口满且成功率 < 阈值时触发熔断（与 max_consecutive 并列，OR 关系）
4. **成功路径录入 "success" outcome**: Spec conduct_review + Loop _conduct_review 成功重置后追加
5. **`config.py` 新增**: `review_circuit_window_size`（默认 10，min 3）、`review_circuit_success_rate_threshold`（默认 0.3）
6. **`tests/test_sliding_window_circuit.py`**: 24 个测试

#### C. Lightweight Lint 降级
1. **新增 `src/utils/lightweight_lint.py`**:
   - `_check_syntax()`: ast.parse 语法检查
   - `_check_ruff()`: ruff check --select E,F 子进程（FileNotFoundError 降级、超时降级）
   - `run_lightweight_lint()`: 组合入口，返回 `LintResult`（issues/summary）
2. **Spec/Loop 熔断跳过分支集成**: circuit open 时自动扫描项目 .py 文件（最多 50 个），lint 结果注入 suggestions
3. **`config.py` 新增**: `review_circuit_lint_fallback_enabled`（默认 True）、`review_circuit_lint_timeout`（默认 10s）
4. **`tests/test_lightweight_lint.py`**: 15 个测试
5. **`tests/test_lint_fallback_integration.py`**: 4 个集成测试

### 技术要点
- `ReviewMetricsExporter` 使用 `runtime_checkable` Protocol，支持自定义实现
- `SlidingWindowTracker` 使用 `deque(maxlen)` 自动滑窗，不需要手动 trim
- CircuitState 的 `recent_outcomes` 序列化时 trim 到 20 条，避免状态文件膨胀
- MagicMock 的 `__int__` 返回 1 导致 window_size=1 的 bug，通过 `isinstance(raw, (int, float))` 类型检查防御
- lint fallback 限制最多 50 个文件 + 10s 超时，防止大项目阻塞
- 所有新增代码零裸 `f"{e}"` / 零裸 `asyncio.wait_for` / 零裸 `raise TimeoutError()`

### 文件改动
新增文件:
- `src/utils/metrics_exporter.py` — Metrics exporter 框架
- `src/utils/lightweight_lint.py` — 轻量级本地 lint
- `tests/test_metrics_exporter.py` — 17 tests
- `tests/test_sliding_window_circuit.py` — 24 tests
- `tests/test_lightweight_lint.py` — 15 tests
- `tests/test_lint_fallback_integration.py` — 4 tests

修改文件:
- `src/utils/review_helpers.py` — SlidingWindowTracker + exporter 集成 + 滑动窗口熔断
- `src/spec_engine/review.py` — recent_outcomes 字段 + 成功路径录入 + lint fallback
- `src/loop_engine/engine.py` — recent_outcomes 字段 + 成功路径录入 + lint fallback
- `src/config.py` — 8 个新配置项
- `tests/test_empty_error_guard.py` — metrics_exporter logger 适配
- `tests/test_review_timeout.py` — recent_outcomes.clear() 适配
- `tests/test_loop_review_circuit.py` — recent_outcomes.clear() 适配

### 验证结果
- 全量测试: 2303 passed (+60 from baseline 2243)
- 回归 lint 扫描器: 96 passed
- grep 零违规: asyncio.wait_for / raise TimeoutError() 在 src/ 均为 0

---

## TimeoutError (empty message) 增量加固提交落地

### 任务描述
将可观测性+韧性+防回归增量加固的 8 个文件改动验证并提交，完成"审查执行异常: TimeoutError (empty message)"改进建议的最终闭环。

### 执行内容
1. **should_retry isinstance 短路** — 裸 `TimeoutError()`/`asyncio.TimeoutError()` 即使空消息也可重试
2. **prompt_with_retry 可观测性日志** — 每次重试记录 `attempt/elapsed_ms/remaining_budget`
3. **compute_adaptive_timeout hard_floor** — 新增 `hard_floor=15s` 防止超时级联
4. **normalize_review_diagnostics 截断** — `error_text_limit=500` 防止异常堆栈过长污染卡片
5. **lint 规则** — 禁止 `raise TimeoutError()` 无参数调用
6. **E2E 全链路测试** — `concurrent.futures.TimeoutError` 穿越 6 层验证无空消息

### 验证结果
- TimeoutError 专项: 238 passed
- 回归 lint: 96 passed
- E2E 全链路: 40 passed
- retry/helpers/timeout: 124 passed
- 全量测试: 2243 passed, 0 regressions

### 文件改动（+339/-6 行）
- `src/utils/retry.py` — isinstance 短路 + 可观测性日志
- `src/utils/review_helpers.py` — hard_floor 参数
- `src/utils/review_diagnostics.py` — error_text_limit 截断
- `tests/test_retry.py` — +72 行（7 短路 + 2 日志测试）
- `tests/test_review_helpers.py` — +27 行（6 hard_floor 测试）
- `tests/test_review_timeout.py` — +52 行（6 截断测试）
- `tests/test_empty_error_guard.py` — +47 行（1 lint 规则）
- `tests/test_timeout_e2e.py` — +99 行（6 E2E 测试）

### 提交记录
- `9d0ffb9` feat(review): add retry observability + adaptive timeout hard_floor + error_text truncation + 26 new tests

---

## 落实改进建议：ReviewCircuitState 持久化提交落地

### 任务描述
将 2026-04-19 最后一批增量加固（ReviewCircuitState 持久化 + Loop skip overrun 保护 + E2E empty message 测试）的 9 个文件未暂存改动验证并提交，完成"审查执行异常: TimeoutError (empty message)"改进建议的最终闭环。

### 执行内容
1. **验证测试全绿**:
   - 全量测试: 2189 passed, 0 failed
   - 回归 lint 专项 (`test_empty_error_guard.py`): 94 passed
   - TimeoutError 专项 (`-k timeout`): 193 passed

2. **提交 9 个文件改动** (+609/-5 行):
   - `src/spec_engine/review.py`: ReviewCircuitState 新增 `to_dict()`/`from_dict()` 持久化方法
   - `src/spec_engine/persistence.py`: `save_engine_state` 新增 `review_circuit` 参数；`load_engine_state` 返回 `tuple[Optional[SpecProject], dict]`（向后兼容旧快照）
   - `src/spec_engine/engine.py`: `save_state` 传入 circuit dict；新增 `load_state_with_circuit` classmethod
   - `src/loop_engine/engine.py`: LoopReviewCircuitState 新增 `to_dict`/`from_dict`/`consecutive_skips`；override `save_state`；新增 `load_state_with_circuit`；skip overrun 保护
   - `tests/test_spec_engine.py`: +117 行，4 个 circuit 持久化 round-trip 测试 + 向后兼容测试
   - `tests/test_loop_engine.py`: +171 行，3 个 circuit 持久化测试 + 3 个 skip overrun 测试
   - `tests/test_empty_error_guard.py`: +159 行，5 个 E2E empty message 测试 + 7 个输出守卫测试
   - `.Memory/2026-04-19.md`: 补充持久化改动记录
   - `.Memory/Abstract.md`: 新增索引条目

### 技术要点
- `to_dict()`/`from_dict()` 使用 `dict.get()` 带默认值，确保旧快照（无 `review_circuit` key）加载时返回全零初始状态
- `persistence.py` 的 `load_engine_state` 返回类型从 `Optional[SpecProject]` 变为 `tuple`，但 `SpecEngine.load_state()` 保持原签名向后兼容
- LoopEngine skip overrun 阈值 = `max_consecutive * 2`（默认 6），超过时发出 warning 日志供监控告警
- `last_review_failure_diag` 不参与序列化（大型 dict，不适合持久化）

### 提交记录
- `a7c8e64` feat(review): add ReviewCircuitState persistence + Loop skip overrun protection + E2E empty message tests

---

## Spec ReviewCircuitState consecutive_skips 对齐 + resume circuit 恢复

### 任务描述
补齐 Spec ReviewCircuitState 与 Loop LoopReviewCircuitState 的能力差异（consecutive_skips + skip overrun 保护），并为两引擎的 resume() 增加持久化 circuit state 自动恢复。

### 执行内容
1. **Spec ReviewCircuitState 补齐 consecutive_skips**:
   - `src/spec_engine/review.py`: ReviewCircuitState 新增 `consecutive_skips: int = 0` 字段
   - `to_dict()`/`from_dict()` 增加 `consecutive_skips` 序列化/反序列化（`from_dict` 缺失 key 默认 0，向后兼容）

2. **Spec conduct_review skip overrun 保护**:
   - circuit-open skip 分支新增 `circuit.consecutive_skips += 1` 计数
   - overrun 检测阈值 `max(1, max_consecutive) * 2`，超阈值打 `logger.warning("[Spec] review_skip_overrun: ...")`
   - review 成功分支新增 `circuit.consecutive_skips = 0` 重置

3. **Spec resume() circuit 恢复**:
   - `src/spec_engine/engine.py`: resume() 开头新增 `load_state_with_circuit()` 调用，从持久化文件恢复 `_review_circuit`
   - 异常安全（try/except + logger.debug）

4. **Loop resume() circuit 恢复**:
   - `src/loop_engine/engine.py`: resume() 开头新增 `load_state_with_circuit()` 调用，从持久化文件恢复 `_review_circuit`
   - 异常安全（try/except + logger.debug）

5. **新增测试** (+8):
   - `tests/test_review_timeout.py`: 3 序列化测试 + 3 skip overrun 测试
   - `tests/test_spec_engine.py`: 1 Spec resume circuit 恢复测试
   - `tests/test_loop_engine.py`: 1 Loop resume circuit 恢复测试

### 技术要点
- `consecutive_skips` 与 Loop 的 `LoopReviewCircuitState.consecutive_skips` 完全对称
- resume() 恢复路径解决进程重启后熔断计数器归零问题
- `from_dict` 的 `data.get("consecutive_skips") or 0` 确保旧快照兼容
- 全量测试 2197 passed（+8），零回归

### 提交记录
- `f75fa41` feat(review): align Spec consecutive_skips + resume circuit restore for Spec/Loop

## Review 重试总耗时约束 + 可观测性增强

### 任务描述
针对"审查执行异常: TimeoutError (empty message)"8 层纵深防御体系的第 9 层加固：约束 review 重试总耗时上限，防止一轮 review 阻塞时间失控（最坏情况 timeout×(max_retries+1)+退避间隔）；增强可观测性（metrics 新增 total_elapsed_ms）；新增 lint 回归测试确保 review 调用始终传入 total_timeout。

### 执行内容
1. **RetryPolicy 新增 total_timeout 字段** (`src/utils/retry.py`):
   - `RetryPolicy` 新增 `total_timeout: Optional[float] = None`
   - `prompt_with_retry` 新增 `total_timeout` kwarg（优先级：kwarg > policy.total_timeout > None）
   - 循环内 attempt>0 时检查累计耗时，超限 raise `TimeoutError`
   - delay 阶段自动 clamp 到剩余预算

2. **agent_session.py 全部 5 处 send_prompt_with_retry 透传** (`src/agent_session.py`):
   - Protocol 定义 + ClaudeCLISession/TTADKSession/ACPSession/ModelFailureAwareSession 4 处实现均新增 `total_timeout` 参数并透传

3. **SpecEngine 链路适配** (`src/spec_engine/session_utils.py`, `src/spec_engine/engine.py`):
   - `session_utils.send_prompt_with_retry` 新增 `total_timeout` 参数
   - `SpecEngine._send_prompt_with_retry` 新增 `total_timeout` 参数

4. **Spec/Loop review 调用传入 total_timeout** (`src/spec_engine/review.py`, `src/loop_engine/engine.py`):
   - `conduct_review` / `_conduct_review` 传入 `total_timeout=float(review_timeout * 2)`
   - try 块前记录 `_t0 = time.monotonic()` 用于耗时计算

5. **CircuitState 新增 last_review_elapsed_ms**:
   - `ReviewCircuitState` + `LoopReviewCircuitState` 新增字段 + to_dict/from_dict 序列化
   - `from_dict` 使用 `.get() or 0` 确保旧数据兼容

6. **handle_review_exception 可观测性增强** (`src/utils/review_helpers.py`):
   - 新增 `review_elapsed_ms: int = 0` 入参
   - 写入 `circuit.last_review_elapsed_ms`
   - metrics dict 新增 `total_elapsed_ms` 字段

7. **Spec/Loop except 块传入 elapsed_ms**:
   - `elapsed_ms = int((time.monotonic() - _t0) * 1000)` 传入 `handle_review_exception`

8. **新增测试**:
   - `tests/test_retry_total_timeout.py`: 10 个测试覆盖 total_timeout 四场景+边界
   - `tests/test_empty_error_guard.py`: 1 个 lint 测试确保 review 调用含 total_timeout
   - `tests/test_review_helpers.py`: 3 个测试验证 metrics total_elapsed_ms
   - `tests/test_review_timeout.py`: 3 个 ReviewCircuitState 序列化测试
   - `tests/test_loop_review_circuit.py`: 3 个 LoopReviewCircuitState 序列化测试
   - `tests/test_loop_engine.py`: 1 个 mock 签名修复

### 技术要点
- `total_timeout` 为 Optional[float] 默认 None，None 时不启用约束，完全向后兼容
- review 场景默认 `total_timeout = review_timeout * 2`（即 240s），留足重试空间
- delay 阶段自动 clamp 到剩余预算（`delay = min(delay, remaining)`），避免预算已耗尽还等完整退避周期
- `from_dict` 的 `data.get("last_review_elapsed_ms") or 0` 确保旧快照兼容
- 全量测试 2217 passed（+20），零回归

### 提交记录
- `ee75916` feat(review): add RetryPolicy total_timeout + review elapsed_ms tracking + 20 new tests


## TimeoutError (empty message) 增量加固：可观测性+韧性+防回归

### 任务描述
在已有 9 层纵深防御体系基础上，针对 "审查执行异常: TimeoutError (empty message)" 做增量加固：
- 可观测性增强（重试日志）
- 运维韧性加固（adaptive timeout 硬下界、diagnostics 截断）
- 防回归保障（lint 规则 + E2E 全链路测试）

### 执行内容
1. **should_retry() isinstance 短路** — `src/utils/retry.py:50-52`，裸 `TimeoutError()` / `asyncio.TimeoutError()` 即使 `str(e)` 为空也能被识别为可重试，避免依赖字符串匹配
2. **prompt_with_retry 可观测性日志** — `src/utils/retry.py:116-128`，每次重试前 `logger.info` 记录 `attempt`, `elapsed_ms`, `remaining_budget`，便于运维定位慢审查
3. **compute_adaptive_timeout 硬下界** — `src/utils/review_helpers.py:65`，新增 `hard_floor=15` 参数，`return max(..., min_timeout, hard_floor)`，防止极端连续超时导致超时值低于 ACP 通信最小合理值
4. **normalize_review_diagnostics error_text 截断** — `src/utils/review_diagnostics.py:304-308`，error_text 超 500 字符追加 `…(truncated)` 标记，防止异常堆栈过长污染诊断卡片
5. **lint 规则：禁止裸 raise TimeoutError()** — `tests/test_empty_error_guard.py:TestNoBareRaiseTimeoutError`，扫描 src/ 中 `raise TimeoutError()` 无参数调用，防止未来代码引入空消息 TimeoutError
6. **E2E 全链路穿越测试** — `tests/test_timeout_e2e.py:TestConcurrentFuturesTimeoutE2EChain`，模拟 `concurrent.futures.TimeoutError()` 穿越 should_retry → prompt_with_retry → handle_review_exception → diagnostics → suggestion 6 层，验证每层输出不含空消息

### 技术要点
- `isinstance(error, (TimeoutError, asyncio.TimeoutError))` 短路放在字符串匹配之前，仅影响 TimeoutError 类型，不改变其他异常的重试判断
- hard_floor=15s 基于 ACP 通信最小延迟经验值，可通过参数 override
- error_text 截断在 normalize_review_diagnostics 层而非 build_review_exception_diagnostics 层，因为 build 层已有 `_redact_and_truncate` 用于 hard_limit=600，normalize 层的 500 是面向用户卡片的额外保护
- concurrent.futures.TimeoutError 是 Python 内建 TimeoutError 的子类，isinstance 检查自动覆盖
- 全量 2243 passed（baseline 2217 + 新增 26），0 failures，零回归

### 文件改动
- `src/utils/retry.py` — +1 import asyncio, +4 isinstance 短路, +15 可观测性日志
- `src/utils/review_helpers.py` — +8 hard_floor 参数和文档
- `src/utils/review_diagnostics.py` — +15 error_text_limit 参数 + 截断逻辑
- `tests/test_retry.py` — +72 (7 should_retry 短路测试 + 2 可观测性日志测试)
- `tests/test_review_helpers.py` — +26 (6 hard_floor 保护测试)
- `tests/test_review_timeout.py` — +52 (6 error_text 截断测试)
- `tests/test_empty_error_guard.py` — +47 (1 lint 规则测试)
- `tests/test_timeout_e2e.py` — +99 (6 E2E 全链路测试)

## _run_async 空消息包装 + LoopReporter (empty message) 过滤

### 任务描述
落实改进建议：审查执行异常 TimeoutError (empty message)。在已有 10 层防御体系基础上做增量收尾，堵住两个残余泄漏点：
1. `SyncACPSession._run_async` 的 `future.result(timeout)` 抛出空消息 TimeoutError（与 `send_prompt` 已有守卫对齐）
2. `LoopReporter.format_iteration_done` 中 `record.error` 含 `(empty message)` 标记可能泄漏到用户卡片

### 执行内容
1. **`src/acp/sync_adapter.py`**: `_run_async` 方法增加 `try/except TimeoutError` 包装，空消息时填充 `f"ACP 异步操作超时 ({timeout}s): agent={self._agent_type}"`，`raise TimeoutError(msg) from e` 保留异常链
2. **`src/loop_engine/reporter.py`**: `format_iteration_done` 失败分支将 `record.error` 经 `.strip()` + `(empty message)` 子串检测后替换为 `"执行异常，将在下一轮重试"`
3. **`tests/test_empty_error_guard.py`**: 新增 `TestRunAsyncTimeoutWrapping`（4 条用例）+ `TestLoopReporterEmptyMessageFilter`（5 条用例），共 9 条新测试

### 技术要点
- `_run_async` 与 `send_prompt` 现在统一使用相同的空消息守卫模式
- `from e` 保留完整异常链，不遮盖原始调用栈
- reporter 过滤使用 `"(empty message)" in _raw_error` 精确匹配标记字符串，不会误杀含 "empty" 的真实错误
- 全量 2312 tests passed，零回归

### 提交记录
- 文件变更: `src/acp/sync_adapter.py`, `src/loop_engine/reporter.py`, `tests/test_empty_error_guard.py`
- 测试: 2312 passed (baseline 2303 + 9 new)

## 三项审计缺口修复：metrics_exporter bug + hard_floor 可配置化 + 单例重配置

### 任务描述
在 10 层纵深防御体系全面审计中发现 3 个残余缺口，逐一修复：
(A) `JsonLinesExporter` except 子句未绑定异常变量——日志记录 `<class 'Exception'>` 而非实际错误
(B) `hard_floor` 超时下限未暴露到 `config.py`——运维无法调整最小审查超时
(C) `get_metrics_exporter` 单例函数文档声称支持重配置但实际忽略新参数

### 执行内容

#### A. metrics_exporter except 子句修复
- **`src/utils/metrics_exporter.py:87-90`**: `except Exception:` → `except Exception as e:`，`str(Exception) or repr(Exception)` → `str(e) or repr(e)`
- 影响：JSONL 导出失败时 debug 日志从显示 `<class 'Exception'>` 变为显示实际异常消息

#### B. hard_floor 可配置化
- **`src/config.py`**: 新增 `spec_review_hard_floor: int = 15` 和 `loop_review_hard_floor: int = 15`
- **`src/spec_engine/review.py:261-264`**: `compute_adaptive_timeout` 调用传入 `hard_floor=settings.spec_review_hard_floor`
- **`src/loop_engine/engine.py:930-932`**: `compute_adaptive_timeout` 调用传入 `hard_floor=settings.loop_review_hard_floor`
- 默认值 15 与原有硬编码完全一致，零行为变更

#### C. get_metrics_exporter 单例重配置
- **`src/utils/metrics_exporter.py:111-120`**: 缓存命中前检查 `isinstance(_exporter_instance, expected_cls)`，类型不匹配时自动重建
- 使行为与文档声称的「Pass a different exporter_type to re-create」一致

### 技术要点
- 3 个修复均为增量改进，不触及核心防御层
- config 新增字段使用默认值，.env 无需任何变更
- 单例重配置通过类型匹配实现，保持 module-level 变量模式，无并发风险

### 测试
- **新增 10 个测试**：
  - `TestJsonLinesExporterErrorLogging`（2）：写入失败日志验证 + 源码扫描确认无 `str(Exception)`
  - `TestFactoryReconfiguration`（4）：logger→jsonl / jsonl→logger 切换 + 同类型缓存
  - `TestHardFloorConfigWiring`（4）：config 默认值 + hard_floor 参数传递 + 自定义值
- 全量 2322 tests passed（baseline 2312 + 10 new），零回归
- 回归 lint（test_empty_error_guard）+ E2E 超时（test_timeout_e2e）145 tests 全绿

### 提交记录
- 与「引入维护性 Backlog 机制」合并为单一提交，commit message 以实质修复为主线
- 文件变更: `src/utils/metrics_exporter.py`, `src/config.py`, `src/spec_engine/review.py`, `src/loop_engine/engine.py`, `tests/test_metrics_exporter.py`, `tests/test_review_helpers.py` + `.Memory/Backlog.md`(新增), `.Memory/Abstract.md`, `.Memory/2026-04-20.md`, `AGENTS.md`
- 测试: 2322 passed (baseline 2312 + 10 new)

## 引入维护性 Backlog 机制

### 任务描述
落实改进建议：低优先级审计缺口（Gap severity: Low/Medium）应收集到 backlog 集中在维护窗口处理，而不是打断主线开发节奏。当前项目无任何前瞻性任务跟踪机制，需建立 Backlog 流程。

### 执行内容

#### A. 新建 `.Memory/Backlog.md`
- 定义分级标准：High（正确性/安全性）→ 立即修复；Medium（可观测性/可运维性）→ 入 Backlog；Low（风格/文档）→ 入 Backlog
- Backlog 表格模板：ID / 日期 / Gap 描述 / Severity / 来源 / 状态 / 解决 Commit
- 将今日已完成的三项审计缺口（B-001~B-003）作为首批 Done 记录填入，建立可追溯基线

#### B. AGENTS.md Workflow Rules 新增第 5 条
- **审计缺口分级处理**：Review/Audit 产出的改进建议按 severity 分级，Low/Medium 录入 Backlog，High 立即修复
- 与现有 4 条规则并列，成为项目标准工作流的一部分

#### C. Abstract.md 添加 Backlog 入口
- 顶部新增 blockquote 引用说明 Backlog 流程，链接到 `Backlog.md`
- 2026-04-20 条目新增本次流程改进的一行索引

#### D. code.byted.org 创建跟踪 issue
- 标题"维护性 Backlog 跟踪入口"，描述引用 `.Memory/Backlog.md`，用于在线协作跟踪

### 技术要点
- 纯流程/文档变更，不涉及任何运行时代码修改
- Backlog 机制嵌入现有 `.Memory/` 体系，不引入额外工具依赖
- 分级标准明确可操作，消除 severity 模糊争议
- 通过 AGENTS.md 规则约束确保 Backlog 不成为死文件

### 提交记录
- 与「三项审计缺口修复」合并为单一提交（避免纯流程文档变更独立成提交，消除"打磨边缘"自我矛盾）
- 文件变更: `.Memory/Backlog.md`(新增), `.Memory/Abstract.md`, `.Memory/2026-04-20.md`, `AGENTS.md`
- 测试: 2322 passed（纯文档变更，零回归）

## 三次确认验证：TimeoutError (empty message) 10 层防御体系执行闭环

### 任务描述
按「落实改进建议：审查执行异常 TimeoutError (empty message)」的实现方案，逐步执行 9 项任务清单（4 项 Grep 静态验证 → Backlog 确认 → 回归 lint → 全量测试 → Memory 更新），完成可执行、可验证的闭环。

### 执行内容
1. **Grep 静态验证 4 项**（并行执行）:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py:37` 的 `safe_wait_for` 内部 ✅
   - `f"{e}"` 裸异常格式化在 src/ 中: 0 匹配 ✅
   - logger 裸引用 `%s", e)` 在 src/ 中: 0 匹配 ✅
2. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done ✅
3. **回归 lint 专项**: `test_empty_error_guard.py` 105 tests passed, 2.33s ✅
4. **全量测试**: 2322 tests passed, 0 failures, 46.91s ✅

### 验证结论
10 层纵深防御体系持续完整，第三次独立确认无退化、无新缺口。改进建议已完全落实。

---

## 二次确认验证：TimeoutError (empty message) 10 层防御体系持续完整

### 任务描述
基于「如何落实改进建议：审查执行异常: TimeoutError (empty message)？」的问题，对 10 层纵深防御体系进行二次独立确认验证。

### 执行内容
1. **Grep 静态验证 4 项**:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `async_helpers.py` 的 `safe_wait_for` 内部（行 37）✅
   - `f"{e}"` 裸异常格式化在 src/ 中: 0 匹配 ✅
   - logger 裸引用 `%s", e)` 在 src/ 中: 0 匹配 ✅
2. **Backlog 确认**: B-001/B-002/B-003 三项均为 Done ✅
3. **回归 lint 专项**: `test_empty_error_guard.py` 105 tests passed ✅
4. **全量测试**: 2322 tests passed, 0 failures ✅

### 验证结论
10 层纵深防御体系持续完整，无退化、无新缺口。

---

## 最终验证闭环：TimeoutError (empty message) 10 层防御体系完整性确认

### 任务描述
对「审查执行异常: TimeoutError (empty message)」改进建议进行最终验证闭环，确认 10 层纵深防御体系完整无缺口，无需额外代码改动。

### 执行内容
1. **全量测试**: 2322 tests passed, 0 failures（`uv run python -m pytest tests/ -x -q`）
2. **grep 扫描 4 项零残留**:
   - `raise TimeoutError()` 在 src/ 中: 0 匹配 ✅
   - `asyncio.wait_for` 在 src/ 中: 仅 `safe_wait_for` 内部 1 处（预期行为）✅
   - `f"{e}"` 裸异常格式化: 0 匹配 ✅
   - `logger.xxx("%s", e)` 不带 `exc_info`/`str()`/`repr()` 守卫: 0 匹配 ✅（20 处带 `exc_info=True` 的被 lint guard 正确排除）
3. **代码审查 6 项确认**:
   - `_run_async` (sync_adapter.py:1574-1578): 空消息包装 ✅
   - `send_prompt` (sync_adapter.py:1477-1480): 明确 re-raise 带消息 ✅
   - `LoopReporter.format_iteration_done` (reporter.py:101-103): `(empty message)` 过滤 ✅
   - `review_diagnostics` (review_diagnostics.py:213-217): 空消息按 timeout/非timeout 分流中文文案 ✅
   - 三引擎 6 处 execute/resume `except TimeoutError` 分支: WARNING 级别+区分超时/异常+`get_error_detail(e)` ✅
   - `ReviewCircuitState`/`LoopReviewCircuitState` 持久化 to_dict/from_dict + resume load_state_with_circuit ✅
4. **Backlog 确认**: B-001/B-002/B-003 三项均已标记 Done ✅

### 验证结论
10 层纵深防御体系完整闭合，无代码改动需求：
| 层级 | 防御 | 状态 |
|------|------|------|
| 1 | safe_wait_for 源头包装 | ✅ |
| 2 | fmt_error/get_error_detail 核心兜底 | ✅ |
| 3 | 用户可见路径统一 get_error_detail | ✅ |
| 4 | 93 处 logger 统一 str(e) or repr(e) | ✅ |
| 5 | 三引擎独立 except TimeoutError | ✅ |
| 6 | review_helpers 统一异常处理+自适应超时+指数退避 | ✅ |
| 7 | 收敛检测跳过 review_failed 轮次 | ✅ |
| 8 | 4 个回归 lint 扫描器 | ✅ |
| 9 | SlidingWindowTracker 滑动窗口熔断 | ✅ |
| 10 | lightweight_lint 本地 lint 降级 | ✅ |
