# 2026-04-20 项目记录

## 落实改进建议：ReviewCircuitState 持久化提交落地

### 任务描述
将 2026-04-19 最后一批增量加固（ReviewCircuitState 持久化 + Loop skip overrun 保护 + E2E empty message 测试）的 9 个文件未暂存改动验证并提交，完成"审查执行异常: TimeoutError (empty message)"改进建议的最终闭环。

### 执行内容
1. **验证测试全绿**:
   - 全量测试: 2189 passed, 0 failed
   - 回归 lint 专项 (`test_empty_error_guard.py`): 94 passed
   - TimeoutError 专项 (`-k timeout`): 193 passed

2. **提交 9 个文件改动** (+609/-5 行):
   - `src/spec_engine/review.py`: ReviewCircuitState 新增 `to_dict()`/`from_dict()` 持久化方法
   - `src/spec_engine/persistence.py`: `save_engine_state` 新增 `review_circuit` 参数；`load_engine_state` 返回 `tuple[Optional[SpecProject], dict]`（向后兼容旧快照）
   - `src/spec_engine/engine.py`: `save_state` 传入 circuit dict；新增 `load_state_with_circuit` classmethod
   - `src/loop_engine/engine.py`: LoopReviewCircuitState 新增 `to_dict`/`from_dict`/`consecutive_skips`；override `save_state`；新增 `load_state_with_circuit`；skip overrun 保护
   - `tests/test_spec_engine.py`: +117 行，4 个 circuit 持久化 round-trip 测试 + 向后兼容测试
   - `tests/test_loop_engine.py`: +171 行，3 个 circuit 持久化测试 + 3 个 skip overrun 测试
   - `tests/test_empty_error_guard.py`: +159 行，5 个 E2E empty message 测试 + 7 个输出守卫测试
   - `.Memory/2026-04-19.md`: 补充持久化改动记录
   - `.Memory/Abstract.md`: 新增索引条目

### 技术要点
- `to_dict()`/`from_dict()` 使用 `dict.get()` 带默认值，确保旧快照（无 `review_circuit` key）加载时返回全零初始状态
- `persistence.py` 的 `load_engine_state` 返回类型从 `Optional[SpecProject]` 变为 `tuple`，但 `SpecEngine.load_state()` 保持原签名向后兼容
- LoopEngine skip overrun 阈值 = `max_consecutive * 2`（默认 6），超过时发出 warning 日志供监控告警
- `last_review_failure_diag` 不参与序列化（大型 dict，不适合持久化）

### 提交记录
- `a7c8e64` feat(review): add ReviewCircuitState persistence + Loop skip overrun protection + E2E empty message tests

---

## Spec ReviewCircuitState consecutive_skips 对齐 + resume circuit 恢复

### 任务描述
补齐 Spec ReviewCircuitState 与 Loop LoopReviewCircuitState 的能力差异（consecutive_skips + skip overrun 保护），并为两引擎的 resume() 增加持久化 circuit state 自动恢复。

### 执行内容
1. **Spec ReviewCircuitState 补齐 consecutive_skips**:
   - `src/spec_engine/review.py`: ReviewCircuitState 新增 `consecutive_skips: int = 0` 字段
   - `to_dict()`/`from_dict()` 增加 `consecutive_skips` 序列化/反序列化（`from_dict` 缺失 key 默认 0，向后兼容）

2. **Spec conduct_review skip overrun 保护**:
   - circuit-open skip 分支新增 `circuit.consecutive_skips += 1` 计数
   - overrun 检测阈值 `max(1, max_consecutive) * 2`，超阈值打 `logger.warning("[Spec] review_skip_overrun: ...")`
   - review 成功分支新增 `circuit.consecutive_skips = 0` 重置

3. **Spec resume() circuit 恢复**:
   - `src/spec_engine/engine.py`: resume() 开头新增 `load_state_with_circuit()` 调用，从持久化文件恢复 `_review_circuit`
   - 异常安全（try/except + logger.debug）

4. **Loop resume() circuit 恢复**:
   - `src/loop_engine/engine.py`: resume() 开头新增 `load_state_with_circuit()` 调用，从持久化文件恢复 `_review_circuit`
   - 异常安全（try/except + logger.debug）

5. **新增测试** (+8):
   - `tests/test_review_timeout.py`: 3 序列化测试 + 3 skip overrun 测试
   - `tests/test_spec_engine.py`: 1 Spec resume circuit 恢复测试
   - `tests/test_loop_engine.py`: 1 Loop resume circuit 恢复测试

### 技术要点
- `consecutive_skips` 与 Loop 的 `LoopReviewCircuitState.consecutive_skips` 完全对称
- resume() 恢复路径解决进程重启后熔断计数器归零问题
- `from_dict` 的 `data.get("consecutive_skips") or 0` 确保旧快照兼容
- 全量测试 2197 passed（+8），零回归

### 提交记录
- `f75fa41` feat(review): align Spec consecutive_skips + resume circuit restore for Spec/Loop

## Review 重试总耗时约束 + 可观测性增强

### 任务描述
针对"审查执行异常: TimeoutError (empty message)"8 层纵深防御体系的第 9 层加固：约束 review 重试总耗时上限，防止一轮 review 阻塞时间失控（最坏情况 timeout×(max_retries+1)+退避间隔）；增强可观测性（metrics 新增 total_elapsed_ms）；新增 lint 回归测试确保 review 调用始终传入 total_timeout。

### 执行内容
1. **RetryPolicy 新增 total_timeout 字段** (`src/utils/retry.py`):
   - `RetryPolicy` 新增 `total_timeout: Optional[float] = None`
   - `prompt_with_retry` 新增 `total_timeout` kwarg（优先级：kwarg > policy.total_timeout > None）
   - 循环内 attempt>0 时检查累计耗时，超限 raise `TimeoutError`
   - delay 阶段自动 clamp 到剩余预算

2. **agent_session.py 全部 5 处 send_prompt_with_retry 透传** (`src/agent_session.py`):
   - Protocol 定义 + ClaudeCLISession/TTADKSession/ACPSession/ModelFailureAwareSession 4 处实现均新增 `total_timeout` 参数并透传

3. **SpecEngine 链路适配** (`src/spec_engine/session_utils.py`, `src/spec_engine/engine.py`):
   - `session_utils.send_prompt_with_retry` 新增 `total_timeout` 参数
   - `SpecEngine._send_prompt_with_retry` 新增 `total_timeout` 参数

4. **Spec/Loop review 调用传入 total_timeout** (`src/spec_engine/review.py`, `src/loop_engine/engine.py`):
   - `conduct_review` / `_conduct_review` 传入 `total_timeout=float(review_timeout * 2)`
   - try 块前记录 `_t0 = time.monotonic()` 用于耗时计算

5. **CircuitState 新增 last_review_elapsed_ms**:
   - `ReviewCircuitState` + `LoopReviewCircuitState` 新增字段 + to_dict/from_dict 序列化
   - `from_dict` 使用 `.get() or 0` 确保旧数据兼容

6. **handle_review_exception 可观测性增强** (`src/utils/review_helpers.py`):
   - 新增 `review_elapsed_ms: int = 0` 入参
   - 写入 `circuit.last_review_elapsed_ms`
   - metrics dict 新增 `total_elapsed_ms` 字段

7. **Spec/Loop except 块传入 elapsed_ms**:
   - `elapsed_ms = int((time.monotonic() - _t0) * 1000)` 传入 `handle_review_exception`

8. **新增测试**:
   - `tests/test_retry_total_timeout.py`: 10 个测试覆盖 total_timeout 四场景+边界
   - `tests/test_empty_error_guard.py`: 1 个 lint 测试确保 review 调用含 total_timeout
   - `tests/test_review_helpers.py`: 3 个测试验证 metrics total_elapsed_ms
   - `tests/test_review_timeout.py`: 3 个 ReviewCircuitState 序列化测试
   - `tests/test_loop_review_circuit.py`: 3 个 LoopReviewCircuitState 序列化测试
   - `tests/test_loop_engine.py`: 1 个 mock 签名修复

### 技术要点
- `total_timeout` 为 Optional[float] 默认 None，None 时不启用约束，完全向后兼容
- review 场景默认 `total_timeout = review_timeout * 2`（即 240s），留足重试空间
- delay 阶段自动 clamp 到剩余预算（`delay = min(delay, remaining)`），避免预算已耗尽还等完整退避周期
- `from_dict` 的 `data.get("last_review_elapsed_ms") or 0` 确保旧快照兼容
- 全量测试 2217 passed（+20），零回归

### 提交记录
- `ee75916` feat(review): add RetryPolicy total_timeout + review elapsed_ms tracking + 20 new tests
