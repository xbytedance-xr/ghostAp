# 2026-03-18 项目记录

## Spec 模式崩溃防御与失败任务持久化强化

### 任务描述
2026-03-18 02:01:29
修复 Spec 模式在异常场景下的崩溃风险，确保失败任务仍能本地落盘以供查询与恢复，同时服务不中断。

### 执行内容
1. 为 SpecEngine 回调增加安全包装，回调异常只记录日志不影响执行主流程。
2. 记录最后 cycle/phase 并在异常路径中兜底保存失败任务，避免异常中断导致无法恢复。
3. 强化任务持久化：失败任务保存支持 fallback 目录，读/删/列操作全量覆盖双目录并去重。
4. 生成 spec 文件落盘改为 best-effort，落盘失败不再向上抛异常。
5. 更新任务持久化与 SpecEngine 测试用例，适配 fallback 目录新逻辑。

### 技术要点
- 回调安全封装防止 UI/渲染异常拖垮引擎循环。
- 持久化 fallback 目录提升磁盘异常或权限异常时的可用性。
- 失败路径仍尽力保存任务快照，保障 /spec_recover 可用。

### 提交记录
- 未提交
