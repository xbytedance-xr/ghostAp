# 2026-04-28 项目记录

## 修复静默异常吞没（except Exception: pass → logger.debug）

### 任务描述
全项目范围内将 `except Exception: pass`（及 `except OSError: pass`、`except ValueError: pass`）替换为 `logger.debug("...", exc_info=True)`，消除静默异常吞没问题，提升可观测性。

### 执行内容
修改了 32 个源文件，共约 96 处静默异常（分三批并行执行）：

**第一批：ws_client.py（19 处）：**
- `src/feishu/ws_client.py` — 19 处（close/engine stop/session cleanup/reply error/sticky message/card action 等）

**第二批：核心引擎文件（22 处）：**
- `src/main.py` — 5 处（SIGTERM/SIGHUP/reply error/preheat/close client）
- `src/acp/session.py` — 4 处（plan_update/grace wait/tool_calls/history）
- `src/feishu/handlers/system.py` — 4 处（lock holder/admin/InteractionMode/link task）
- `src/feishu/chat_lock_gate.py` — 4 处（cleanup thread/lock reply ×2/reaction）
- `src/loop_engine/engine.py` — 3 处（lint/circuit breaker/cancel）
- `src/deep_engine/engine.py` — 1 处（cancel）
- `src/engine_base.py` — 2 处（cancel in stop/cleanup）

**第三批：其他文件（约 55 处）：**

**主要文件（多处修改）：**
- `src/feishu/handlers/diagnostics.py` — 9 处（lock 过期状态、消息映射、链接回复、streaming 更新/关闭、链接任务）
- `src/feishu/handlers/spec.py` — 5 处（加载 engine、保存状态 pause/stop、获取 target_project）
- `src/worktree_engine/dispatcher.py` — 4 处（3 个 on_unit_update 回调 + session.close）
- `src/tasking/scheduler.py` — 6 处（executor shutdown ×2、deque remove ×2、listener 回调）+ 新增 logger 导入
- `src/acp/provider.py` — 6 处（LRU move_to_end ×4、cache pop ×2）
- `src/utils/review_helpers.py` — 5 处（elapsed_ms、recent_outcomes、sliding window、diagnostics 字段、metrics 日志）

**需要新增 logger 导入的文件（7 个）：**
- `src/tasking/scheduler.py`
- `src/card/builders/system.py`
- `src/card/builders/deep.py`
- `src/card/builders/core.py`
- `src/worktree_engine/reporter.py`
- `src/acp/providers/__init__.py`

**其他文件（各 1-2 处）：**
- `src/card/streaming.py`（thread_id 获取、content 长度回退）
- `src/sandbox/executor.py`（strict_lock_mode、best-effort sentinel）
- `src/feishu/renderers/deep_renderer.py`（running engine 获取）
- `src/feishu/renderers/base.py`（summary 提取）
- `src/feishu/handlers/programming.py`（TTADK degrade 通知）
- `src/feishu/handlers/loop.py`（target_project 获取）
- `src/card/builders/diagnostics.py`（项目名获取）
- `src/card/builders/deep.py`（结构化内容降级）
- `src/card/builders/core.py`（thread_id 获取）
- `src/worktree_engine/tool_discovery.py`（TTADK 工具发现）
- `src/worktree_engine/reporter.py`（索引转字母）
- `src/repo_lock.py`（handlers 列表移除）
- `src/utils/lock_order.py`（held 列表移除）
- `src/utils/registry.py`（cleanup_fn 调用）
- `src/utils/retry.py`（before_retry 回调）
- `src/utils/metrics_exporter.py`（日志导出）
- `src/acp/providers/__init__.py`（清除缓存）
- `src/acp/telemetry.py`（遥测记录）
- `src/project/manager.py`（删除临时文件 OSError）
- `src/spec_engine/task_persistence.py`（删除临时文件 OSError）

### 技术要点
- 所有替换使用 `logger.debug("...", exc_info=True)` 保持最低日志级别，不影响正常运行
- 描述信息使用英文且简短
- 对于缺少 logger 的文件，添加了 `import logging` 和 `logger = logging.getLogger(__name__)`
- 不改变任何业务逻辑，仅将 `pass` 替换为 debug 日志
- 全量 3857 个测试零回归

## 合并 ws_client.py 与 ws_health.py 重复的 WebSocket 健康检查逻辑

### 任务描述
`ws_client.py` 中的 `FeishuWSClient` 内联了一整套 WebSocket 健康检查逻辑（约 120 行，8 个方法 + 7 个状态字段），与 `ws_health.py` 中的 `WSHealthMonitor` 类完全重复。将内联逻辑删除，改为委托给 `WSHealthMonitor` 实例。

### 执行内容
**修改文件：**
- `src/feishu/ws_client.py` — 核心重构
  - 新增 `from .ws_health import WSHealthMonitor` 导入
  - `__init__` 中 7 个状态字段（`_ws_health_lock`, `_ws_last_connect_at`, `_ws_last_frame_at`, `_ws_last_pong_at`, `_ws_reconnect_requested_at`, `_ws_watchdog_stop`, `_ws_watchdog_thread`）替换为 `self._ws_health_monitor = WSHealthMonitor(self, self.settings)`
  - 8 个重复方法删除（`_record_ws_activity`, `_get_ws_watchdog_interval`, `_get_ws_stale_timeout`, `_trigger_ws_disconnect`, `_check_ws_health_once`, `_ws_watchdog_loop`, `_start_ws_watchdog`, `_stop_ws_watchdog`）
  - 3 个调用点替换：`_start_ws_watchdog()` → `_ws_health_monitor.start_watchdog()`、`_stop_ws_watchdog()` → `_ws_health_monitor.stop_watchdog()`、`on_activity=self._record_ws_activity` → `on_activity=self._ws_health_monitor.record_activity`
- `tests/test_ws_client_patch.py` — 2 个 watchdog 测试更新为通过 `_ws_health_monitor` 访问
- `tests/test_ws_client_reconnect.py` — monkeypatch 目标从 `FeishuWSClient._start/_stop_ws_watchdog` 改为 `WSHealthMonitor.start_watchdog/stop_watchdog`

### 技术要点
- `WSHealthMonitor.__init__` 接受 `(client_instance, settings)` 两个参数，通过 `getattr(self._client_instance, "_client", None)` 访问底层 SDK client
- `WSHealthMonitor` API 保持不变：`record_activity(kind)`, `check_health_once(now)`, `start_watchdog()`, `stop_watchdog()`
- 净减约 120 行重复代码
- `test_ws_health_monitor.py` 6 个测试全部通过
- 模块导入验证通过

## 提取 ws_client.py 硬编码值到 Settings 配置类

### 任务描述
将 `src/feishu/ws_client.py` 中的 6 处关键硬编码值提取到 `src/config.py` 的 `Settings` 类中，使其可通过 `.env` 环境变量配置。

### 执行内容
**config.py 新增 10 个字段**（放在 `feishu_ws_watchdog_interval` 之后，新建 "Feishu WebSocket client runtime parameters" 分组）：
- `message_expire_seconds: int = 30` — 消息过期时间
- `message_cache_ttl: int = 300` — 消息去重缓存 TTL
- `message_cache_max_size: int = 1000` — 消息去重缓存最大容量
- `card_action_dedup_ttl: int = 1` — 卡片动作去重 TTL
- `card_action_dedup_max_size: int = 5000` — 卡片动作去重最大容量
- `system_command_concurrency: int = 10` — 系统命令并发数
- `spec_rate_limit_capacity: int = 100` — Spec 引擎限流容量
- `spec_rate_limit_fill_rate: float = 50.0` — Spec 引擎限流填充速率
- `spec_circuit_breaker_threshold: int = 10` — Spec 引擎熔断阈值
- `spec_circuit_breaker_recovery: float = 5.0` — Spec 引擎熔断恢复超时

**ws_client.py 6 处修改**：
1. 移除 `MESSAGE_EXPIRE_SECONDS = 30` 类常量，改为 `self.settings.message_expire_seconds`
2. `MessageCache(ttl=300, max_size=1000, ...)` → `MessageCache(ttl=self.settings.message_cache_ttl, max_size=self.settings.message_cache_max_size, ...)`
3. `MessageCache(ttl=1, max_size=5000, ...)` → `MessageCache(ttl=self.settings.card_action_dedup_ttl, max_size=self.settings.card_action_dedup_max_size, ...)`
4. `system_concurrency=10` → `system_concurrency=self.settings.system_command_concurrency`
5. `RateLimiter(capacity=100, fill_rate=50.0)` → 从 settings 读取
6. `CircuitBreaker(failure_threshold=10, recovery_timeout=5.0)` → 从 settings 读取

**测试文件更新**（8 个文件，~35 处 mock_settings 补全新字段）：
- `test_ws_client_patch.py`（31 处 + 4 处 thread_programming 变体）
- `test_button_gate_and_dedupe.py`（2 处）
- `test_worktree_command_routing.py`（3 处）
- `test_spec_interaction.py`（1 处）
- `test_loop_interaction.py`（1 处）
- `test_exit_deferred_behavior.py`（2 处）
- `test_ws_client_reconnect.py`（1 处 SimpleNamespace）

### 技术要点
- `FeishuWSClient.__init__` 中已有 `self.settings = get_settings()`，所有新配置直接通过 `self.settings.xxx` 读取
- 默认值与原硬编码值完全一致，零行为变更
- 测试使用 `MagicMock()` 时，未显式设置的属性返回 `MagicMock` 对象，传入 `int` 参数位置会导致 `TypeError`，因此所有 mock_settings 必须补全新字段
- 2689 tests passed，1 unrelated failure（test_spec_engine.py 与本次修改无关）

## 重构：Extract Method 拆解 SpecEngine._run_cycle_loop

### 任务描述
将 `src/spec_engine/engine.py` 中超长的 `_run_cycle_loop` 方法（约 397 行）通过 Extract Method 重构拆解为多个独立的阶段方法，降低圈复杂度，提升可读性。纯重构，不改变外部行为。

### 执行内容
将 `_run_cycle_loop`（约 397 行）拆解为协调器（约 85 行）+ 9 个 helper 方法：

1. **`_prepare_cycle_input`** — 确定 SPEC 阶段输入（首轮原始需求 / work_item spec 文件 / refinement 输入）
2. **`_run_spec_phase`** — SPEC 阶段：执行 spec prompt / 直接加载 spec 文件、产物持久化、work_item 标记、acceptance_criteria 合并
3. **`_run_plan_phase`** — PLAN 阶段：执行 plan prompt、产物持久化、解析 plan_artifact
4. **`_run_task_phase`** — TASK 阶段：执行 task prompt、解析任务列表、产物持久化
5. **`_run_build_phase`** — BUILD 阶段：执行 build prompt、产物持久化
6. **`_run_review_phase`** — REVIEW 阶段（条件执行）：审查编排、诊断持久化、返回 review_passed
7. **`_check_cycle_pause`** — 暂停检查：检测引擎状态、标记 cycle 失败、持久化
8. **`_handle_cycle_exception`** — 异常处理：暂停检测、错误记录、连续失败保护、session 重建
9. **`_finalize_successful_cycle`** — 成功后处理：criteria 评估、发现+spec 生成、metrics 快照、终止检查、session 重建

**关键设计决策**：
- `persist_every_phase` 放在各阶段方法内部（而非统一的 pause check），保持 SPEC 阶段 persist 在 work_item 处理前的原始时序
- 控制流耦合（break/continue）保持在 `_run_cycle_loop` 主方法中，helper 方法通过返回值传达决策
- 删除了 `_run_cycle_loop` 中不再使用的 `requirement` 局部变量

### 技术要点
- `_run_cycle_loop` 从约 397 行缩减到约 85 行（协调逻辑）
- 9 个 helper 方法总计约 310 行（包含原始逻辑一行不差）
- 方法签名保持不变，外部行为完全一致
- 3857 tests passed，零回归

## 修复内存泄漏：全局字典无限增长

### 任务描述
修复 3 个内存泄漏风险点：`_working_dirs` 无限增长、`_ttadk_flow_start_times` 泄漏路径、`_ttadk_flow_last_duration_ms` 只增不减。

### 执行内容
**问题 1: `_working_dirs`（ws_client.py + handlers/base.py）**
- `_working_dirs` 从 `dict` 改为 `OrderedDict` + `_WORKING_DIRS_MAX_SIZE = 500` 容量限制
- `set_working_dir` 写入后检查容量，超出时 `popitem(last=False)` 淘汰最旧条目

**问题 2: `_ttadk_flow_start_times`（handlers/system.py）**
- `_report_ttadk_flow_duration` 中添加 10 分钟（600s）过期丢弃
- `handle_ttadk_command` 的 error 分支添加 `pop(chat_id, None)` 清理

**问题 3: `_ttadk_flow_last_duration_ms`（handlers/system.py）**
- 从 `dict` 改为 `OrderedDict` + `_TTADK_FLOW_DURATION_MAX_SIZE = 200` 容量限制
- 写入后检查容量，超出时淘汰最旧条目

### 技术要点
- 使用 `OrderedDict` + 容量限制是轻量级 LRU 的常用模式
- 不改变业务逻辑，仅添加边界保护
- 3857 tests passed，零回归

## 修复 card 层反向依赖（safe_truncate_markdown 下沉到 utils）

### 任务描述
`src/card/streaming.py` 通过延迟导入依赖 `src/feishu/message_formatter.py` 的 `safe_truncate_markdown` 静态方法，形成 card→feishu 反向依赖。

### 执行内容
1. **新建** `src/utils/markdown.py` — 提取 `safe_truncate_markdown` 为独立模块级函数
2. **修改** `src/card/streaming.py` — 延迟导入替换为顶层 `from ..utils.markdown import safe_truncate_markdown`
3. **修改** `src/feishu/message_formatter.py` — 原方法改为委托调用 `src.utils.markdown.safe_truncate_markdown`，保持向后兼容

### 技术要点
- `safe_truncate_markdown` 是纯函数（无状态），适合放在 utils 层
- 依赖方向从 card→feishu（反向）变为 card→utils（正向）
- feishu 层保持向后兼容（委托调用）
- 3857 tests passed，零回归
