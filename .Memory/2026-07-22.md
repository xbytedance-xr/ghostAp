# 2026-07-22

## 编程卡片执行流时间线

### 更改内容

- 普通 Coco/Claude/Aiden/Codex/Gemini/Traex/TTADK 编程卡不再把所有 `AgentThoughtChunk` 回填到一个固定“过程摘要”面板；`ProgrammingCardSession` 和共享 `ACPStreamBridge` 在真实工具开始事件上结束并退役当前 reasoning block，工具后的下一段分析获得单调新 block ID，因此 `CardState.blocks` 保持“分析 → 工具 → 分析 → 工具”的真实到达顺序。
- 新增 `src/card/render/execution_flow.py`，将协议层仍然独立的 reasoning/tool block 投影为统一执行流：运行中只显示一个蓝色“当前动作”行，已发生步骤进入折叠的“执行记录 · N 步”；分析保留 provider 原文，工具只展示名称和输入目标，不泄露或重复工具输出。
- 执行记录最多展示最近 12 步，较早步骤以明确计数折叠；失败、仍运行和已完成分别显示 `❌`、`⏳`、`✅`，避免并行场景把较早的活动工具误标为完成。
- 普通编程卡运行中将当前动作和执行记录置于正文前，完成后移除当前动作并把回答正文提升到折叠执行记录之前；Deep/Spec/Worktree/Workflow 等 `engine_type` 非空卡片继续使用原 reasoning/activity 渲染合同，不改变既有分页与紧凑模式。
- 删除 `ProgrammingCardSession` 中只计算却从未进入渲染链路的 `_turn_snapshots`/私有 `ACPEventRenderer` 状态，卡片顺序直接由权威 `CardState` 事件流表达。
- 重做 `ux/programming-reasoning-summary.html`，并排展示“运行中”和“已完成”两态：任务/当前动作即时可见、历史按需展开、完成结论优先。高频卡片不增加装饰动画，只保留状态与层级反馈。

### 设计原因

- 原实现按内容类型聚合而不是按发生时间组织：后来的分析不断改写卡片前部同一面板，工具调用在后方另行聚合，导致用户看到“卡片不动，只有过程摘要更新”，也破坏了分析与命令调用之间的因果关系。
- 协议层必须继续区分回答、可展示分析摘要和真实工具事件；展示层则应面向人的任务心智模型按时间统一。当前动作、审计历史和最终交付分别承担即时反馈、可追溯性和结果阅读，不再争夺同一视觉层级。

### 验证

- 新增/更新 chronology、运行中当前分析、活动命令、折叠历史、完成态答案优先、12 步上限、provider 原文保真、工具输出隐藏、并行活动状态和引擎隔离回归；测试均按红—绿顺序执行。
- 卡片/会话/ACP/Deep/Spec 针对性回归：`392 passed`。
- `uv run python -m pytest tests/ -q -m "not slow"`：首次完整运行仅因临时 `docs/superpowers/` 计划目录触发仓库文档瘦身守卫；删除该一次性目录并用 `.Memory` 承接事实后，复跑为 `12255 passed, 9 skipped, 100 deselected, 45 subtests passed`。
- `uv run ruff check src/card/ ...`：`All checks passed!`。
- `uv run python -m src.main --validate`：`GhostAP v0.2.1 配置校验通过`。
- `git diff --check`：通过。

### 后续风险

- 尚未向真实飞书租户投递；本地结构化 payload、分页和 CardKit/session 回归已覆盖，但移动端实际行高、折叠面板点击区域与长命令视觉密度仍需部署后验收。
- 测试仍报告 `lark-channel-sdk==1.1.0` 的两条既有 Python 3.13 `DeprecationWarning`，与本次执行流改动无关，继续由 Backlog B054 跟踪。

## 普通编程模式迁移到官方 Lark Channel SDK

### 更改内容

- 主 Bot 入站 WebSocket 从 `lark-oapi` WebSocket 客户端迁移到 `lark-channel-sdk`，保留既有事件分发、活动观测、重连健康检查和优雅关闭语义；普通 REST 消息能力仍使用官方 `lark-oapi`。
- 新增 `LarkChannelCardAPIClient`，普通 Coco/Claude/Aiden/Codex/Gemini/TraeX/TTADK 编程卡片通过共享 `FeishuChannel` 使用 CardKit：创建流式实体、发送卡片引用、文本元素增量更新、结构化实体更新和终态 `finish`。
- 保留 `CardSession`/`CardDelivery` 为卡片状态唯一事实源，因此项目、工具/模型、任务清单、当前进展、工具调用、子代理独立卡、总执行时长和分页信息继续由原渲染管道提供。
- 普通编程卡从首帧开始预分配流式实体；纯文本增量使用 element update，结构变化使用 entity update。所有 CardKit 操作使用单调 sequence，历史分页冻结前和终态均显式 finish。
- 出站审计在创建远端实体之前执行；Channel 路径默认拒绝退回普通内联卡片，引用发送失败时回收孤立实体，避免未审计写入或把 `om_*` 消息 ID 当作 CardKit 实体 ID。
- 首卡不可见、终态投递失败时中止剩余重试并以文本兜底；主卡和全部子代理卡完成终态投递后任务才退出，确保共享 Channel 不会先于投递线程池关闭。
- 修正 stale `expected_conn` 断连误报，并让健康监控读取 Channel SDK 的事件循环。

### 设计原因

- 用户要求普通模式使用官方 Channel SDK 的流式能力，同时不能丢失现有富卡片信息。复用现有纯渲染和交付状态机，仅替换普通编程模式的传输适配层，可以保持执行策略与传输维度分离。
- CardKit 实体和消息引用是两阶段操作；审计、孤儿回收、sequence 冲突处理及终态屏障必须在适配器和交付层显式建模，不能依赖 SDK 隐式重试。
- Deep/Spec/Worktree/Workflow 和静态文本/媒体未纳入本次传输切换，避免扩大共享路由改动范围。

### 验证

- `uv run python -m pytest tests/ -q -m "not slow"`：`12248 passed, 9 skipped, 100 deselected, 45 subtests passed`。
- Channel/CardKit、普通编程终态、WebSocket 重连/健康和卡片会话针对性回归：`87 passed`。
- `uv run ruff check`（全部本次变更文件）：通过。
- `git diff --check`：通过。
- `uv run python -m src.main --validate`：通过。
- 未重启服务，未向真实飞书租户发送消息或卡片。

### 后续风险

- `lark-channel-sdk==1.1.0` 在 Python 3.13 导入时仍产生两条上游 `DeprecationWarning`（protobuf 的 naive UTC 时间及无当前事件循环时调用 `get_event_loop()`）；未使用 warning filter 掩盖，跟踪于 Backlog B054。
- 本地测试使用协议级 fake Channel 锁定 payload、sequence、finish 和失败恢复契约；真实租户权限、网络和视觉行为仍需部署方在受控环境验收。

## 编程卡片过程摘要与工具活动收敛

### 更改内容

- 修复普通编程卡在每次 `TOOL_STARTED` 时强制结束 reasoning block 的行为；同一来源在一次任务内复用稳定的过程摘要块，工具调用不再把短摘要切成多张“思考完成”面板。
- 共享 `ACPStreamBridge` 仍在工具边界切分正文流，但不再切断 reasoning；Deep、Spec 等共享桥接调用因此保持正文顺序，同时获得一致的摘要聚合语义。
- reducer 支持对既有 reasoning block 重新激活，避免复用稳定 block ID 时追加重复块。
- 将“深度思考中 / 思考完成”改为“正在分析 / 过程摘要”，准确表达模型提供的是可展示摘要而非原始思维链；不对模型原文进行启发式翻译或改写。
- 连续工具调用仍完整保留在活动审计中，并由现有 digest 聚合为单个探索/工具面板。
- 新增 `ux/programming-reasoning-summary.html`，对照展示修复前后的卡片层级和信息密度。

### 根因

- 旧实现为规避共享 block ID 导致内容覆盖，在工具开始事件上同时关闭正文和 reasoning，并递增 reasoning 边界序号；渲染器又会在任意非工具 atom 前冲刷待聚合工具，因此形成“单个工具面板 + 单句思考面板”的交替瀑布。
- 事件本身没有重复；问题是边界建模过度、标题语义不准确和视觉层级过强。

### 验证

- `uv run python -m pytest`（reasoning/stream/render/session、Channel 投递、活动 digest、Deep/Spec renderer 与 orchestrator）：`338 passed`。
- `uv run ruff check`（全部本次变更 Python 文件）：通过。
- `git diff --check`：通过。
- `uv run python -m src.main --validate`：通过。

### 后续风险

- 模型生成的过程摘要可能仍使用英文；这是 provider 内容而非卡片系统伪造或重复。本次只收敛展示边界与标签，未做可能扭曲含义的自动翻译。
- 尚未向真实飞书租户投递；本地 HTML 预览和结构化渲染测试已覆盖聚合数量、标题与终态状态。

## 过程摘要折叠面板列表化

### 更改内容

- 保留单个“过程摘要” `collapsible_panel`，将面板内部的非空摘要段落统一渲染为 Markdown `- ...` 列表，与工具活动折叠面板的详情层级保持一致。
- 普通编程 `ProgrammingCardSession` 和 Deep/Spec 共用 `ACPStreamBridge` 都以 `source_id` 隔离追踪工具边界：工具调用后的下一段摘要会开启新列表项，同一段的流式 chunk 仍连续拼接，不按标点启发式拆句。
- 更新 `ux/programming-reasoning-summary.html`，修复目标预览改为折叠面板内部无序列表。

### 设计原因

- 外层折叠结构已符合次级信息定位，问题在于内容是一整段 Markdown，多个过程节点难以扫读。
- 工具边界是已有的真实语义分段信号；在事件适配层保留它，比在渲染层根据中英文标点猜测句子边界更稳定。

### 验证

- 新增的渲染与工具边界用例先红后绿；普通编程和共享流桥接均覆盖。
- 卡片渲染/会话/流桥接/activity digest 相邻回归：`190 passed`。
- `uv run python -m pytest tests/ -q -m "not slow"`：`12249 passed, 9 skipped, 100 deselected, 45 subtests passed`。
- 相关 Python 文件 `ruff check` 与 `git diff --check` 通过。

### 后续风险

- 尚未向真实飞书租户投递；本地结构化测试和 HTML 预览已锁定列表层级，真实移动端的行距与缩进仍需部署后视觉验收。
