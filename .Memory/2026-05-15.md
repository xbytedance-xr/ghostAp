# 2026-05-15

## 任务列表假折叠箭头修复

- 问题：任务列表内部的 `▶ 进行中` 和当前任务行 `▶ 🔄 ...` 只是 markdown 文本，不是飞书可交互折叠控件；线上表现为看起来能展开但点击无效。
- 修复：`render_task_list_panel()` 去掉任务列表内部的三角展开符号，只保留外层 `collapsible_panel` 作为真实飞书折叠能力；当前任务继续通过加粗和状态图标高亮。
- 验证：
  - 红灯回归先复现 `▶` 残留导致的失败。
  - `uv run python -m pytest tests/test_render_task_list.py tests/test_task_list_v2.py tests/test_sticky_head.py tests/test_card_orchestrator.py::TestAgentTaskRouting tests/test_card_renderer.py::TestPagination::test_section_layout_renders_sticky_task_list_once_per_page -q` → 33 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过

## Feishu 自动 reaction 收敛到 Typing

- 问题：用户消息触发 Deep/Spec/普通编程时，dispatcher、handler 和卡片完成 hook 会叠加发送 `OK`、`OnIt`、`Fire`、`PARTY` 等多个飞书表情；用户期望只保留“敲键盘/Typing”反馈，避免消息下方出现一排无意义 reaction。
- 修复：`EmojiReaction` 新增 `should_send()` 作为自动表情白名单，目前只允许 `EmojiType.TYPING`；`BaseHandler.add_reaction()` 在 handler 出口过滤非 Typing，`FeishuIMClient.add_reaction()` 再兜底过滤直接 IM client 调用，保留现有调用点但不再真正发送其它表情。
- 验证：
  - `uv run python -m pytest tests/test_emoji_reactions.py tests/test_emoji_hook_empty_msgid.py tests/test_feishu_dispatcher.py tests/test_ws_client_routing.py -q` → 54 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过

## Feishu reaction 保留任务生命周期反馈

- 问题：上一轮把自动 reaction 白名单收敛到仅 `Typing`，误伤了“收到任务”的 `OnIt` 和“任务完成”的 `PARTY`；用户仍希望这两类关键反馈保留。
- 修复：`EmojiReaction._ALLOWED_AUTO_REACTIONS` 调整为 `Typing / OnIt / PARTY`，`BaseHandler.add_reaction()` 与 `FeishuIMClient.add_reaction()` 继续统一调用 `should_send()` 过滤噪音；日志文案从“非输入中表情”改为“非保留自动表情”。
- 同步：多任务卡片路由已是当前实现，计划项在 `TaskOrchestrator.on_plan_received()` 阶段作为一等卡片创建，`task/agent/subagent` 后续事件绑定到对应任务卡；本轮只修正过期注释，不改路由行为。
- 验证：
  - 新增红灯先确认 `OnIt` / `PARTY` 被旧白名单拦截。
  - `uv run python -m pytest tests/test_emoji_reactions.py -q` → 3 passed
  - `uv run python -m pytest tests/test_card_orchestrator.py::TestOnPlanReceived tests/test_card_orchestrator.py::TestAgentTaskRouting tests/test_deep_renderer_orchestrator.py::TestDeepRendererMultiCard::test_multi_task_plan_creates_n_plus_1_cards tests/test_deep_renderer_orchestrator.py::TestDeepRendererMultiCard::test_agent_tool_call_creates_independent_child_card tests/test_programming_card_session.py::TestProgrammingCardSession::test_task_tool_opens_independent_card -q` → 17 passed

## 任务卡准并行独立更新落地

- 问题：虽然 Deep/普通编程已经能把计划任务和 `task/agent/subagent` 路由到独立任务卡，但 `TaskRegistry` 状态变化仍会触发 `_do_broadcast()` 给所有活跃任务卡发送 `TASK_LIST_UPDATED`；任一任务状态变化都会拉着其他任务卡一起 PATCH，视觉上接近 lockstep，不符合“准并行独立更新”。
- 修复：`TaskOrchestrator` 新增 pending task id 队列，`_on_registry_status_change(task_id, status)` 只调度受影响 task；`_do_broadcast()` 把 pending task id 映射到对应可见任务卡或 overflow 承载卡，只刷新这些目标 session。每张任务卡仍保留完整任务列表快照，但普通任务状态变化不再 PATCH 无关任务卡。
- 修复：保留 debounce 语义以合并同一任务的密集状态变化；失败 session 不阻塞其他目标 session；完成/失败任务仍走 `_finalize_task_session()` 终态化，后续更新被 `_finalized_task_ids` 吞掉。
- 验证：
  - 新增红灯先确认 t1 状态变化时 t2 也收到 `TASK_LIST_UPDATED`。
  - `uv run python -m pytest tests/test_card_orchestrator.py::TestBroadcastStatusChange tests/test_card_orchestrator.py::TestBroadcastIsolation tests/test_card_orchestrator.py::TestHandlePlanUpdate::test_broadcasts_status_after_plan -q` → 4 passed
  - `uv run python -m pytest tests/test_card_orchestrator.py tests/test_deep_renderer_orchestrator.py tests/test_programming_card_session.py tests/test_acp_renderer.py -q` → 195 passed

## Deep task 工具分分合合卡片路由修复

- 问题：Deep/Coco 并发任务已经能进入多任务流程，但截图中的 `task` 工具调用没有被识别为独立子任务，只会留在最初的消息卡片里；且无 `PLAN_UPDATE` 时 `route_or_fallback()` 会直接走父卡 fallback，导致“task 后同时发送三张子任务卡片”的体验没有成立。
- 问题：上一轮优化收敛了子任务不刷父卡，但主编排卡归档前只追加文字总结，容易丢掉用户认可的“任务列表”结构化块；多个后续 `task` 子任务陆续出现时，早创建的子任务卡也看不到完整任务列表。
- 二次修正：用户指出真实需求不是“只有子任务才一任务一卡”，而是计划任务本身就必须是一等 Feishu 消息卡。`TaskOrchestrator.on_plan_received()` 现在不再 lazy 等 `in_progress`，而是在多任务计划出现时立即为每个可见计划项创建独立卡；overflow 仍按 `max_task_cards` 折叠到最后一张。
- 二次修正：`task` 工具调用如果和已有计划任务标题匹配，会绑定并回填到对应计划任务卡，不再额外创建一个 child/subagent 卡；同一个 tool_call 后续 update/done 通过绑定表继续进入该计划任务卡，done 时终态化该卡。
- 三次修正：飞书蓝色 active tool 条之前固定把 `tool_name` 加粗放在前面，`tool_name=task` 时只显示“task”而没有任务内容；现在 `generate_tool_summary()` 对 compact task/todo 优先取 `description/query/content/name` 或第一行，`render_active_tool_line()` 和完整 tool panel 对 `task` 用任务描述作为主标题。
- 修复：ACP 工具解析把 `title=task` 纳入子任务型工具，优先从 raw input 的 `description/prompt` 提取任务标题/描述；`TaskOrchestrator` 和普通编程 `ProgrammingCardSession` 都把 `task` 与 `agent/subagent` 同等识别为独立子任务卡。
- 修复：`TaskOrchestrator.route_or_fallback()` 在无 plan 阶段也优先路由 `task/agent/subagent` 工具调用，按每个 `tool_call.id` 新建独立 Feishu 卡片；新子任务注册后只刷新子任务卡集合的任务列表，不 PATCH 父任务卡。
- 修复：主编排/thinking 卡在归档前补发 `TASK_LIST_UPDATED`，保留原来的任务列表块；每张子任务卡创建时和后续同批子任务出现时也会看到任务列表块，并高亮自己的当前任务。
- 验证：
  - 二次修正定向 `uv run python -m pytest tests/test_card_orchestrator.py::TestOnPlanReceived tests/test_card_orchestrator.py::TestAgentTaskRouting -q` → 14 passed
  - 二次修正相邻 `uv run python -m pytest tests/test_card_orchestrator.py tests/test_deep_renderer_orchestrator.py tests/test_spec_renderer_orchestrator.py -q` → 124 passed
  - 二次修正相邻 `uv run python -m pytest tests/test_programming_card_session.py tests/test_acp_renderer.py tests/test_card_session.py tests/test_deep_renderer.py tests/test_deep_renderer_split.py tests/test_render_task_list.py -q` → 213 passed
  - 三次修正定向 `uv run python -m pytest tests/test_activity_digest.py::TestRenderActiveToolLine tests/test_card_render_panels.py::TestToolSummary -q` → 10 passed
  - 三次修正相邻 `uv run python -m pytest tests/test_activity_digest.py tests/test_card_render_panels.py tests/test_card_render_atoms.py tests/test_card_renderer.py tests/test_tool_collapse_v2.py tests/test_tool_panel_empty.py -q` → 130 passed
  - 三次修正链路相邻 `uv run python -m pytest tests/test_card_orchestrator.py::TestAgentTaskRouting tests/test_card_session.py tests/test_deep_renderer_orchestrator.py -q` → 131 passed
  - `uv run python -m pytest tests/test_card_orchestrator.py::TestAgentTaskRouting tests/test_programming_card_session.py::TestProgrammingCardSession::test_task_tool_opens_independent_card tests/test_acp_renderer.py::TestParseToolCallTodoWrite::test_task_tool_extracts_description_for_child_card_label -q` → 9 passed
  - `uv run python -m pytest tests/test_card_orchestrator.py tests/test_deep_renderer_orchestrator.py tests/test_programming_card_session.py tests/test_acp_renderer.py -q` → 193 passed
  - `uv run python -m pytest tests/test_spec_renderer_orchestrator.py tests/test_deep_renderer.py tests/test_deep_renderer_split.py tests/test_render_task_list.py tests/test_card_session.py -q` → 142 passed
  - 二次修正全量回归 `uv run python -m pytest tests/ -q` → 6522 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过

## 全量回归失败收口

- 问题：Deep 子任务卡片优化后的首次全量回归 `uv run python -m pytest tests/ -q` 暴露 5 个失败：3 个 Spec handler 旧测试在 `CardBuilder` 被整体 mock 时，新增 review-agent 选择卡路径因 `build_spec_review_agent_select_card()` 返回不可解包 mock 而阻断实际 engine 启动；2 个切模型测试在构建项目回包卡时，非字符串 thread context/mock 对象可能流入按钮 payload，触发 `MagicMock` JSON 序列化失败。
- 修复：`SpecHandler._start_spec_review_selection()` 对选择卡构建/发送异常做降级，记录 warning 后按无 review agents 直接启动 Spec，避免辅助选择卡故障阻断主任务；`CoreBuilder._build_footer_buttons()` 只把字符串类型 thread id 传入按钮构建，非字符串上下文按无 thread 处理。
- 验证：
  - 失败用例定向回归 `uv run python -m pytest tests/test_handlers.py::TestSpecHandlerLockIntegration::test_scheduled_run_calls_with_repo_lock tests/test_handlers.py::TestSpecHandlerLockIntegration::test_scheduled_run_catches_lock_conflict tests/test_log_noise.py::TestLogNoise::test_spec_handler_timeout_warning tests/test_switch_model.py::TestSwitchModelACPPath::test_acp_switch_success_sends_reply tests/test_switch_model.py::TestSwitchModelFallbackPath::test_fallback_restarts_session_and_replies -q` → 5 passed
  - 全量二次回归 `uv run python -m pytest tests/ -q` → 6514 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过

## Deep 子任务卡片冻结与最终汇总卡优化

- 问题：Deep 进入多任务/子任务卡片后，agent/subagent 子任务的运行中摘要和 `TOOL_CALL_START` 进度仍可能 PATCH 到父任务或最初任务卡；普通计划任务完成后也会继续接收后续任务列表广播，导致已完成卡片还在刷新。
- 修复：`TaskOrchestrator` 新增终态任务冻结集合；普通 plan task 完成/失败时先给对应任务卡发送 `completed/failed`，随后从后续 `TASK_LIST_UPDATED` 广播中排除；已终态的子任务后续 ACP update 会被消费但不再落到旧卡。
- 修复：agent/subagent 子任务注册和完成不再广播到父任务卡，也不再通过 `TOOL_MODEL_CHANGED(subagents=...)` 实时 PATCH 父卡摘要；子任务内容只更新自己的子任务卡。`DeepRenderer` 先路由 agent/subagent `TOOL_CALL_START` 以创建子卡，再把进度事件发给该子卡，避免父卡 footer 被子任务进度刷新。
- 修复：Deep 多卡任务完成后不再只关闭现有任务卡；`TaskOrchestrator.finish_with_summary()` 会先停止/终态化各任务卡，再创建 `任务总结` 新卡承载最终完成/失败摘要。
- 验证：
  - 红灯验证：新增回归先失败，覆盖子任务不 PATCH 父卡、完成任务卡冻结、Deep 完成后新建最终汇总卡。
  - `uv run python -m pytest tests/test_card_orchestrator.py tests/test_deep_renderer_orchestrator.py -q` → 112 passed
  - `uv run python -m pytest tests/test_deep_renderer.py tests/test_deep_renderer_split.py tests/test_deep_renderer_refactor.py tests/test_render_task_list.py tests/test_card_pipeline_advanced.py -q` → 48 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过

## Deep 子任务独立卡片路由修复

- 问题：Deep 多任务已经接入 `TaskOrchestrator`，但并行子任务如果以 ACP `agent/subagent` 工具调用形式出现，后续 `TOOL_CALL_START/UPDATE/DONE` 仍按最近活跃主任务解析，容易表现为子任务卡片只有任务列表，详情和最终结果集中在最初或某一张主任务卡片。
- 修复：`TaskOrchestrator.route_acp_event()` 在普通 plan task resolver 前先识别 agent/subagent 工具调用（`title=agent/subagent` 或内容包含 `子代理：`），按 `tool_call.id` 注册独立子任务 CardSession，并把 start/update/done 事件路由到该子任务卡。
- 修复：子任务 done 时向独立卡片同步 `completed/failed` 终态和最终内容；后续优化已取消父任务卡的实时子任务摘要 PATCH，避免子任务进展继续刷新父卡。
- 边界：未改 `DeepEngine`、`render`、`delivery`；仍通过 `CardEvent` 和 `CardSession.dispatch()` 走现有 `handler -> session -> render/delivery` 管线。
- 验证：
  - `uv run python -m pytest tests/test_card_orchestrator.py -q` → 102 passed
  - `uv run python -m pytest tests/test_deep_renderer_orchestrator.py tests/test_programming_card_session.py tests/test_card_orchestrator.py -q` → 151 passed
  - `uv run python -m pytest tests/test_deep_renderer.py tests/test_card_pipeline_integration.py -q` → 32 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过
  - 扩大尝试 `uv run python -m pytest tests/test_deep_renderer.py tests/test_deep_engine.py tests/test_card_pipeline_integration.py -q` 中 2 个 `tests/test_deep_engine.py` TTADK 启动用例失败，错误为本机缺 `ttadk` 可执行文件；其余 63 passed，判断为环境依赖缺失而非本次卡片路由回归。

## Deep 卡片副标题与折叠阈值优化

- 问题：Deep 多任务卡片的任务名会同时出现在 header title 与 subtitle，移动端看起来像“小字重复标题”；任务列表在 `total > 12` 时提前折叠已完成任务，工具调用摘要也只展示 6 条详情后折叠。
- 修复：Deep v2 header 的 subtitle 改为展示整体运行总耗时，格式为 `H时MM分SS秒`，耗时来源继续使用 CardSession 注入 `_now` 后推导的 `RuntimeStats.elapsed_seconds`，不新增定频刷新。
- 修复：任务列表折叠阈值与工具调用摘要阈值分离；任务三桶各自尽量展示到 50 条后才折叠，工具调用摘要详情提升到 12 条后折叠。
- 验证：
  - `uv run python -m pytest tests/test_header_v2.py tests/test_task_list_v2.py tests/test_render_task_list.py tests/test_activity_digest.py -q` → 61 passed
  - `uv run python -m pytest tests/test_card_renderer.py tests/test_card_budget_regression.py tests/test_deep_renderer_orchestrator.py -q` → 51 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过

## Spec 卡片 JSON 可读性与阶段状态流转修复

- 问题：Spec 模式卡片在 `SPEC/PLAN/TASK` 等结构化阶段会把模型输出的完整 JSON artifact 作为普通文本实时流出；阶段完成时还会把 raw `output` 写入 `PhaseBlock.content`，最终在 Feishu phase panel 中显示大段 JSON。
- 问题：同一 cycle 每个 phase 都追加独立 `phase_{cycle}_{phase}` 状态块，旧的“✅规格定义 → ⬜方案规划 → ...”快照会留在卡片顶部，看起来状态条没有推进。
- 修复：`SpecStreamProcessor` 对 `SPEC/PLAN/TASK` 的 `TEXT_CHUNK` 不再转发到卡片正文，raw 输出仍留在 engine tracker/artifact/persistence 路径；`PHASE_DONE` 只向 phase reducer 传 `SpecReporter.format_phase_done_content()` 生成的可读摘要。
- 修复：review 完成时 phase panel 展示“多视角审查完成”状态摘要，详细审查结果改走普通文本块；phase 事件新增 `content/subtitle` payload 以同步顶部状态和 header subtitle。
- 修复：phase reducer 在同一 cycle 内只保留一个当前 phase panel，新 phase start 会替换上一阶段状态块，避免旧状态快照固定在卡片顶部；同时补齐 Spec phase 的 footer 文案映射。
- 验证：
  - 红灯验证：新增回归先失败，覆盖结构化阶段不应流出 JSON、phase done 不应携带 raw JSON、同一 cycle 只保留当前 phase 状态块。
  - `uv run python -m pytest tests/test_spec_stream_processor.py tests/test_card_reducer_main.py tests/test_reducers.py tests/test_spec_renderer_callbacks.py tests/test_card_pipeline_integration.py -q` → 171 passed
  - `uv run python -m pytest tests/test_spec_engine.py tests/test_spec_renderer.py tests/test_spec_stream_processor.py tests/test_spec_renderer_callbacks.py tests/test_card_renderer.py tests/test_card_pipeline_integration.py tests/test_card_render_panels.py -q` → 340 passed
  - `uv run python -m src.main --validate` → 配置校验通过
  - `git diff --check` → 通过
