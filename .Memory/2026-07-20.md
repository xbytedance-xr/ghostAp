# 2026-07-20

## 测试套件分层与低信号用例治理

### 审查结论

- 治理前基线为 688 个测试文件、12,474 条 pytest 用例、11,201 个 AST 测试函数和 256,391 行测试源码。首次全量采样运行 269.46 秒仅完成 510 条后主动中断；该次 `KeyboardInterrupt` 只作为耗时证据，未被记作通过。
- Grill 审查采用“维护成本优先，其次反馈时延，最后才是数量”的决策：安全、权限、并发、锁、Journal/effect、持久化恢复和外部协议契约默认保留；只删除同路径重复、语言机制回显、复制生产映射和已被更强契约覆盖的测试。
- 不设置删减配额。不同 engine/backend 即使函数体相同，只要能捕获独立实现故障仍保留；最终 AST 完全同体候选从 10 组降到 6 组，剩余 5 组是 Deep/Spec 独立引擎契约，1 组是 Coco/TTADK 独立 handler 契约。

### 实现

- 将 Slock classifier 的 82 条参数样例收敛为 2 条等价类契约，并保留空白、中英文 greeting/ack/reaction、大小写、标点/emoji、短文本、开发词和长任务代表值；断言仍同时覆盖 `is_chitchat` 与 `is_task`。
- 删除或合并飞书路由、卡片模板、时间格式、redaction、Workflow cancel/terminal-state/subagent prompt、DTO/dataclass/Enum、retry status、TTADK option、Slock 状态序列化和大 payload 等重复或自证式用例。高风险序列化值、默认值兼容、mutable default 隔离、取消和终态契约均保留在更强的 canonical 测试中。
- 删除完整重复的 `tests/test_feishu_ws_client_routing.py`，新增只读 AST 清单工具 `scripts/test_inventory.py` 及自身回归；工具不会导入或执行测试，报告文件数、函数数、源码行、最大文件和完全同体候选。
- 建立 100 条 `slow` 契约层，覆盖真实等待、子进程、Channel SDK、故障注入、超时和取消边界；日常快速门禁为 `-m "not slow"`，发布前慢测/全量仍是必跑契约。
- 新增 `docs/testing.md`，固化保留/删除决策表、等价类写法、快慢门禁和审计阈值；`AGENTS.md` 命令入口同步增加快速套件与清单命令。
- 修复两处由完整顺序运行暴露的测试隔离缺口：WebSocket 通知 fixture 补齐可信 recipient scope；ACP 失败分支改为类级 registry mock，避免在 singleton 上遗留遮蔽方法。删除名为 auto-update、实际只重复 provider 委派且受模块状态影响的陈旧伪契约。
- 删除已全部完成且内容已进入 `.Memory/2026-07-19.md` 的 `docs/superpowers/plans/2026-07-19-employee-traex-home.md`；仓库文档守卫明确禁止归档计划噪声，该文件仍可从 Git 历史恢复。

### 结果与验证

- 最终清单为 688 个文件、12,319 条 pytest 用例、11,101 个 AST 测试函数、255,480 行测试源码：净减 155 条收集用例、100 个测试函数和 911 行测试源码；重点候选集从 362 条降到 207 条。
- 最终快速门禁：`12,210 passed, 9 skipped, 100 deselected, 45 subtests passed`，耗时 469.54 秒；相对第一轮快速门禁 836.42 秒缩短约 44%。唯一 warning 是 pinned Lark SDK protobuf 的既有 `utcfromtimestamp` 弃用提示。
- 当前 100 条慢测由分段门禁完整覆盖：原 50 条 `50 passed`（248.59 秒），新增一批 39 条 `39 passed`（317.50 秒），最后重新分层的 11 条已在上一轮快速全套中通过；marker 只改变调度层，不改变测试逻辑。
- 删除/合并候选的定向回归曾完成 `403 passed, 6 skipped`；Slock enum 完整性修正后相关 `24 passed`；Autonomous 集成前序 + ACP 解析类最终 `534 passed, 19 deselected`。
- `uv run python -m src.main --validate`、`git diff --check` 和所有本次变更 Python 文件的 Ruff 检查通过。仓库级 Ruff 另发现 96 条既有 Autonomous 测试告警，均不在本次改动文件，已录入 Backlog 而未扩大本次机械改写范围。

### 异常与处置

- 首次全量基线因预计需数小时而主动中断；中断产生的资源告警未作为测试结果，后续均使用完整快速门禁和分段慢测取得终态摘要。
- 第一次扩大回归发现合并后的 Slock 状态映射漏列三个现有状态；修正为完整稳定值契约后通过，证明合并没有弱化枚举完整性。
- 文档守卫发现通用计划目录与本仓库“禁止 `docs/superpowers` 归档噪声”规则冲突；临时计划及已完成旧计划均移除，执行证据统一进入 `.Memory/`。
- 快速全套先后发现 WebSocket fixture 缺少 recipient scope、ACP singleton mock 顺序污染；均缩小到稳定前序组合并修复/删除错误测试契约，最终完整快速门禁无失败。
- 直接运行仓库级 Ruff 返回 96 条既有未使用导入/排序告警；随后严格筛选本次变更 `.py` 文件重跑并全部通过，失败原因和剩余范围未被隐藏。

### 后续风险

- 快速层仍有少量 2–4 秒的 retry/集成用例；应在后续维护窗口优先注入 fake clock/Event，确属真实时间或进程契约的再迁入 `slow`，见 Backlog。
- AST 工具只报告完全同体候选，无法替代调用边界和故障检测能力审查；未来删测仍须逐条确认 canonical owner。
- 本次只治理测试与测试基础设施，没有修改生产行为。
