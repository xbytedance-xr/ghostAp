# 2026-01-22~23 项目记录

## 卡片回调修复 + 流式卡片实现
### 任务描述
修复飞书卡片按钮点击报错（200340/200671），并实现流式卡片输出。

### 执行内容
1. **200340 修复** — 飞书开放平台订阅 `card.action.trigger` 回调
2. **200671 修复** — SDK bug: `MessageType.CARD` 未被处理，添加 monkey patch
3. **200671 复发修复** — 空响应对象导致无效 data，改为返回 None
4. **移除卡片回调 Patch** — 不再 monkey patch SDK，增强回调日志
5. **流式卡片实现** — StreamingCardManager 重写，打字机效果配置优化
6. **Card JSON 2.0 兼容** — `body.elements` 不支持 `action` 标签，按钮移到顶层 `actions`
7. **流式卡片引用回复** — reply_to_message_id 支持
8. **统一卡片按钮格式** — value 改为 behaviors 对象格式
9. **移动端按钮布局** — column_set 两列等宽布局
10. **流式输出开关配置** — STREAMING_ENABLED 配置项

### 技术要点
- SDK bug 位置：`lark_oapi/ws/client.py` 第 264-265 行对 `MessageType.CARD` 直接 return
- Card JSON 2.0 的 `body.elements` 不支持 `action` 标签
- `flex_mode: stretch` 确保按钮填满宽度
- lark-oapi 升级至 >=1.5.2

---

## 代码清理
### 任务描述
移除未使用的模块和依赖。

### 执行内容
- 删除模块：src/notification/、src/tools/、src/agent/shell_agent.py
- 删除文档：docs/TOOL_CHAIN_REPORT.md
- 删除测试：test_marshal.py、test_tools.py、test_notification.py
- 清理依赖：移除 fastapi、uvicorn、httpx、pycryptodome
- 清理配置：移除 verification_token、encrypt_key、reload_settings()
- 清理代码：移除 message_formatter.py / mode/manager.py / streaming.py / executor.py 中未使用的方法
- 测试总数 107
