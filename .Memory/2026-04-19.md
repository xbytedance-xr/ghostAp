# 2026-04-19 项目记录

## review_diagnostics 源头消灭 (empty message) 标记 + 低风险路径增量加固

### 任务描述
落实改进建议：消除 `审查执行异常: TimeoutError (empty message)` 问题。将 review_diagnostics 层的 `(empty message)` 标记从"仅 timeout 替换"升级为"所有异常类型源头消灭"，同时补强 3 处低风险路径的超时上下文丢失问题。

### 执行内容
1. **源头加固** `src/utils/review_diagnostics.py`: `build_review_exception_diagnostics` 中 `(empty message)` 标记不再产生，空消息异常直接分流：timeout→"审查超时，将在下一轮重试"，非timeout→"审查执行异常，请检查服务状态"
2. **上下文保留** `src/worktree_engine/dispatcher.py:86`: `str(exc).strip() or "执行异常"` → `get_error_detail(exc)`
3. **上下文保留** `src/worktree_engine/manager.py:169`: `str(exc).strip() or "执行异常"` → `get_error_detail(exc)`
4. **上下文保留** `src/feishu/handlers/base.py:86`: `str(exc) or "操作失败"` → `get_error_detail(exc)`
5. **日志补强** `src/deep_engine/engine.py:336`: `logger.warning` bare `%s` → `str(e) or repr(e)`
6. **新增 14 个测试**: `TestDiagnoseReviewFailureNoEmptyMessageMarker`(9) + `TestWorktreeDispatcherTimeoutContext`(2) + `TestWorktreeManagerTimeoutContext`(1) + `TestBaseHandlerFallbackTimeoutContext`(2)

### 技术要点
- **源头消灭 vs 下游过滤**: 之前 `(empty message)` 标记在 diagnostics 层产生、由 spec/loop 消费端过滤；现在直接在 diagnostics 层消灭，下游过滤变为冗余安全网
- **err_repr 不受影响**: 日志中 `err_repr` 字段仍保留 `repr(exc)` 完整信息，调试能力不减
- 修改 5 个 src 文件、1 个 test 文件，新增 14 个测试

### 全量测试
- 2057 tests passed, 0 failed, 0 regressions

### 提交记录
- `a962ee7` fix(guard): eliminate (empty message) marker at source + harden 3 low-risk paths

---

## 完成零盲区 str(exc) 空值加固提交落地

### 任务描述
将 12 轮增量修复的全部未暂存改动（20 个文件，+707/-35 行）提交到版本控制，确保 8 层纵深防御改进完整落地。

### 执行内容
1. **确认全量测试通过**: 2043 tests passed, 0 failed
2. **提交代码变更**: 17 个 src/ 文件 + 2 个测试文件，commit `d2b28da`
   - 用户可见路径: 裸 `f"{e}"` → `get_error_detail(e)`（agent_session, programming, diagnostics）
   - Logger 路径: 裸 `f"{e}"` → `str(e) or repr(e)`（intent_recognizer, engine_base, ws_client 等）
   - 内部诊断: `str(e) or repr(e)`（acp/client, coco_model, sandbox, ttadk/*）
   - 核心工具: fmt_exception 非超时兜底 `str(exc) or repr(exc)`
   - 测试: +245 行 guard 测试 + 33 个端到端超时测试

### 8 层纵深防御体系总结
1. **核心层**: `fmt_error`/`get_error_detail` 中心化兜底（src/utils/errors.py）
2. **用户可见层**: 所有 `reply_error`/`send_error_card` 使用 `get_error_detail()`
3. **Logger 层**: 所有 `logger.warning/error` 使用 `str(e) or repr(e)`
4. **引擎层**: Deep/Loop/Spec 三引擎独立 `except TimeoutError` 分支
5. **Review 层**: 断路器 + 结构化诊断（Spec + Loop）
6. **收敛保护层**: review_failed 轮次不参与收敛检测
7. **回归 lint 层**: 2 个自动扫描测试防止裸 `f"{e}"` 退化
8. **测试层**: 130+ 个 timeout 相关测试覆盖全链路

### 提交记录
- `d2b28da` fix(guard): complete zero-blind-spot str(exc) empty-value hardening + e2e timeout tests

---

## 内部诊断路径 logger 裸 f"{e}" 全量加固 + 回归 lint 扩展

### 任务描述
在 8 层纵深防御体系完成后，扫描并修补所有剩余的 logger.warning/error 中裸 `f"{e}"` 引用（含 4 处计划内 + 9 处 lint 新发现），扩展回归 lint 覆盖 logger 路径，形成完整闭环。

### 执行内容
1. **计划内 4 处加固**：
   - `src/agent/intent_recognizer.py:1007` — `f"异常回退: {e}"` → `f"异常回退: {str(e) or repr(e)}"`（reasoning + logger）
   - `src/feishu/handlers/engine_base.py:190,192` — Engine 执行超时/异常 logger → `{str(e) or repr(e)}`
   - `src/project/manager.py:88` — 目录创建异常消息 → `{str(e) or repr(e)}`
   - `src/spec_engine/artifacts.py:58,89` — JSON 解析错误消息 → `{str(e) or repr(e)}`

2. **lint 扫描新发现 9 处额外修复**：
   - `src/feishu/ws_client.py:1429` — Backpressure logger → `{str(e) or repr(e)}`
   - `src/feishu/action_dispatcher.py:48,58` — Action/Prefix handler signature mismatch logger → `{str(e) or repr(e)}`
   - `src/utils/errors.py:148` — GhostAPError 降级日志 → `{str(exc) or repr(exc)}`
   - `src/ttadk/strategies.py:309,470,518` — ProbeStrategy/InteractiveStrategy/cleanup logger → `{str(e) or repr(e)}`
   - `src/feishu/handlers/engine_base.py:250,252` — `_safe_lifecycle_action` logger → `{str(e) or repr(e)}`

3. **测试新增**：
   - `TestIntentRecognizerEmptyGuard`（3 tests）：裸 Exception/TimeoutError reasoning 非空 + 有消息保留
   - `TestEngineBaseLoggerEmptyGuard`（3 tests）：logger 超时/异常格式串非空 + 有消息保留
   - `TestProjectManagerEmptyGuard`（3 tests）：目录创建异常非空 + OSError + PermissionError
   - `TestArtifactsParseEmptyGuard`（3 tests）：规格/规划 JSON 解析异常非空 + JSONDecodeError 保留
   - `TestNoBareFStringInUserVisibleErrors.test_no_bare_fstring_in_logger_errors`（1 test）：扫描 src/ 所有 `logger.warning/error(f"...{e}")` 裸引用

### 技术要点
- 回归 lint 新增 `_BARE_LOGGER_RE` 正则：`r'logger\.(?:warning|error)\s*\(\s*f["\'].*\{(?:e|exc|err)\}'`
- `_SKIP_GUARDS` 统一化：`("get_error_detail", "repr(", " or ", "str(")` 四种已有守卫模式
- 所有修改仅在 `str(e)` 为空时触发 `repr(e)` 兜底，有消息时行为完全不变

### 全量测试
- 2043 tests passed, 0 failed, 0 warning regressions（较前次 2030 新增 13 个测试）

---

## system.py handle_refresh_ttadk_models 最后一处裸 f"{e}" 修复 + lint 回归检查

### 任务描述
扫描发现 `src/feishu/handlers/system.py:1476` 的 `handle_refresh_ttadk_models` 方法中 `reply_error(message_id, f"刷新 TTADK 模型列表失败: {e}")` 是最后一处用户可见路径使用裸 `f"{e}"` 的代码，在 `TimeoutError()` 无参数时会产生空尾消息。

### 执行内容
1. **`src/feishu/handlers/system.py:1476`** — `f"刷新 TTADK 模型列表失败: {e}"` → `f"刷新 TTADK 模型列表失败: {get_error_detail(e)}"`
2. **`tests/test_empty_error_guard.py`** — 新增 `TestSystemHandlerRefreshModelsIntegration`（3 tests）：
   - `test_bare_timeout_reply_error_nonempty` — mock refresh_models 抛 TimeoutError() → reply_error 消息非空且含"超时"
   - `test_bare_exception_reply_error_nonempty` — mock refresh_models 抛 Exception() → reply_error 消息非空
   - `test_named_timeout_preserves_message` — mock refresh_models 抛 TimeoutError("模型服务不可用") → 消息含原始信息
3. **`tests/test_empty_error_guard.py`** — 新增 `TestNoBareFStringInUserVisibleErrors`（1 test）：
   - 基于 regex 扫描 `src/` 目录，检测 `reply_error`/`send_error_card` 调用中是否存在裸 `f"{e}"` 模式
   - 自动跳过已使用 `get_error_detail`/`repr`/`or` 守卫的行
   - 作为持续回归检查，防止未来引入新的裸 `f"{e}"` 模式

### 技术要点
- `get_error_detail` 已在文件顶部 import（line 24），无需额外 import
- lint 检查使用 regex `(?:reply_error|send_error_card)\s*\([^)]*f["'].*\{(?:e|exc|err)\}[^)]*\)` 匹配裸引用模式
- lint 检查排除已有守卫（`get_error_detail`/`repr`/`or`）的行，避免误报

### 全量测试
- 2030 tests passed, 0 failed（较前次 2026 新增 3 个集成测试 + 1 个 lint 回归测试）

---

## 用户可见 f"{e}" 裸引用最终收尾（6 处修复 + 6 测试）

### 任务描述
在 8 层 TimeoutError 防御体系和内部诊断路径全部加固完毕后，扫描并修补最后 6 处用户可见的 `f"...{e}"` 裸引用，防止 `TimeoutError()`/`Exception()` 空消息泄漏到用户侧。

### 执行内容
1. **`src/feishu/handlers/programming.py:708`** — `f"❌ 执行异常: {e}"` → `f"❌ 执行异常: {get_error_detail(e)}"`
2. **`src/feishu/handlers/programming.py:438`** — `f"切换 ... 模型失败: {e}"` → `f"切换 ... 模型失败: {get_error_detail(e)}"`
3. **`src/feishu/ws_client.py:2287`** — `f"❌ 操作失败 (...): {e}"` → `f"❌ 操作失败 (...): {str(e) or repr(e)}"`
4. **`src/agent_session.py:245`** — `f"❌ Claude 执行异常: {e}"` → `f"❌ Claude 执行异常: {get_error_detail(e)}"`
5. **`src/agent_session.py:520`** — `f"❌ TTADK 执行异常: {e}"` → `f"❌ TTADK 执行异常: {get_error_detail(e)}"`
6. **`src/feishu/handlers/diagnostics.py:652`** — `f"Diff 报告生成异常: {e}"` → `f"Diff 报告生成异常: {get_error_detail(e)}"`

### 新增测试（tests/test_timeout_e2e.py::TestUserFacingEmptyGuardFinal — 6 tests）
- `test_claude_session_bare_timeout_nonempty` — mock subprocess.Popen 抛 TimeoutError() → PromptResult.text 非空
- `test_ttadk_session_bare_timeout_nonempty` — 同上 TTADK 路径
- `test_programming_acp_execute_bare_exception_nonempty` — get_error_detail 对 3 种裸异常类型保证非空
- `test_programming_model_switch_bare_exception_nonempty` — 模型切换路径同上
- `test_ws_client_card_action_bare_exception_nonempty` — str(e) or repr(e) 保证非空
- `test_diagnostics_diff_report_bare_exception_nonempty` — get_error_detail 对 3 种裸异常保证非空

### 技术要点
- 用户可见路径统一使用 `get_error_detail(e)`（保证非空、含中文友好文案）
- ws_client.py 因 import 层级较深使用轻量 `str(e) or repr(e)` 模式
- 日志级路径（log-only）保持原样不做改动，属于可接受范围

### 全量测试
- 2026 tests passed, 0 failed（较前次 2020 新增 6 个测试）

---

## 内部诊断路径 str(e) 零盲区加固 + 端到端 TimeoutError 集成测试

### 任务描述
在五层 TimeoutError 防御体系已完整落地的基础上，将最后 8 处 internal-only `str(e)` 无显式守卫路径统一加固，并新增端到端集成测试验证全链路 TimeoutError→用户可见消息非空。

### 执行内容
1. **`src/acp/client.py`** — 2处修改（line 418-419, 432-433）：`str(e)` → `str(e) or repr(e)`，read_text_file/write_text_file 的 field_meta["error"] 和 _record 诊断
2. **`src/coco_model/manager.py`** — 1处修改（line 78）：`ModelListResult.error = str(e)` → `str(e) or repr(e)`
3. **`src/sandbox/executor.py`** — 1处修改（line 154）：`f"执行异常: {str(e)}"` → `f"执行异常: {str(e) or repr(e)}"`
4. **`src/ttadk/manager.py`** — 2处修改：
   - line 178: `str(err) or "(empty)"` → `(str(err) or repr(err))[:200]`（日志输出）
   - line 188: `str(err) or ""` → `(str(err) or repr(err))[:200]`（_degraded_reason）
   - line 1043: `str(e)` → `str(e) or repr(e)`（ToolListResult.error）
5. **`src/ttadk/cache.py`** — 2处修改（line 814, 819）：
   - error_snippet: `str(e) or ""` → `(str(e) or repr(e))[:200]`
   - error: `str(e)` → `str(e) or repr(e)`

### 新增测试（tests/test_timeout_e2e.py — 21 tests）
- `TestFormatterLayerE2E`（9 tests）：get_error_detail/fmt_error/fmt_exception 对 5 种裸异常类型永不返空
- `TestCardBuilderE2E`（4 tests）：build_error_card 对 TimeoutError/asyncio.TimeoutError/Exception 输出非空且含中文友好文案
- `TestDeepEngineE2E`（1 test）：裸 TimeoutError() → engine on_error 回调消息非空且含"超时"
- `TestLoopEngineE2E`（1 test）：裸 TimeoutError() → engine on_error 回调消息非空且含"超时"
- `TestSpecEngineE2E`（1 test）：裸 TimeoutError() → engine on_error 回调消息非空且含"超时"
- `TestSandboxExecutorE2E`（2 tests）：裸 Exception/TimeoutError → error_message 非空且含"执行异常"
- `TestInternalDiagnosticsGuard`（3 tests）：acp client/coco model/ttadk manager 内部诊断字段非空

### 技术要点
- `str(e) or repr(e)` 模式：仅在 `str(e)` 为空时触发 fallback，非空时保持原值不变，零侵入
- `[:200]` 截断：ttadk 日志和 _degraded_reason 路径加截断防止冗长字符串，与项目既有模式（sync_adapter `[:200]`）一致
- 端到端测试通过 `_TimeoutSession` fake 模拟 ACP 会话超时，仅 mock session 层，engine/handler/card 走真实代码路径

### 全量测试
- 2020 tests passed, 0 failed, 0 warning regressions（较前次 1999 新增 21 个测试）

---

## 彻底消除剩余 str(e) 空值缺口（6 文件 10 处）

### 任务描述
在已完成的五层 TimeoutError 防御体系基础上，扫描并修补全项目剩余的约 10 处裸 `str(e)` 调用，确保任何异常路径均不产生空消息展示。

### 执行内容
1. **`src/feishu/handlers/deep.py:127`** — `str(e)` → lazy import `get_error_detail(e)`，项目创建失败时显示有意义的错误详情
2. **`src/feishu/handlers/loop.py:105`** — 同上，Loop Engine 项目创建路径
3. **`src/feishu/handlers/spec.py`** — 4 处修改：
   - 行118: `fmt_error("创建项目", str(e))` → `fmt_error("创建项目", e)`（直接传异常对象，走 isinstance 分支处理 TimeoutError）
   - 行395: `f"❌ 导出失败: {str(e)}"` → `f"❌ 导出失败: {get_error_detail(e)}"`
   - 行651: `fmt_error("恢复项目上下文", str(e))` → `fmt_error("恢复项目上下文", e)`
   - 行699: `fmt_error("恢复任务上下文", str(e))` → `fmt_error("恢复任务上下文", e)`
   - 顶部 import 补充 `get_error_detail`
4. **`src/spec_engine/engine.py:577`** — `_run_phase` except 块中 `str(e)` + `e.args` + `repr(e)` 三级兜底简化为 `get_error_detail(e)` 一行
5. **`src/spec_engine/engine.py:1157`** — `rewrite_requirement` 失败返回 `str(e)` → `get_error_detail(e)`
6. **`src/worktree_engine/manager.py:127`** — `state.last_error = str(exc)` → `get_error_detail(exc)`
7. **`src/worktree_engine/manager.py:200`** — merge 失败 `"detail": str(exc)` → `get_error_detail(exc)`
8. **`src/main.py:72`** — 顶层异常处理 `fmt.format_error(str(e))` → `fmt.format_error(get_error_detail(e))`

### 技术要点
- `fmt_error(action, e)` 直接传异常对象比 `fmt_error(action, str(e))` 更优：前者走 `isinstance(detail, Exception)` 分支，对 TimeoutError 生成 "操作超时" 文案；后者预转换为字符串绕过了类型检查
- `get_error_detail()` 内部通过 `fmt_error("", exc)` + `unwrap_fmt_error()` 保证对任何异常类型返回非空字符串（最终兜底 "未知错误"）
- spec_engine `_run_phase` 原有的三级兜底（`str(e)` → `e.args[0]` → `repr(e)`）等价于 `get_error_detail(e)` 的逻辑，简化为单行调用减少维护成本
- 所有修改使用 lazy import（`from ...utils.errors import get_error_detail`），与项目既有模式一致

### 新增测试（tests/test_empty_error_guard.py）
- `TestDeepHandlerProjectCreateEmptyGuard`（2 tests）：bare Exception + bare TimeoutError → 非空错误消息
- `TestLoopHandlerProjectCreateEmptyGuard`（2 tests）：bare Exception + bare TimeoutError → 非空错误消息
- `TestSpecHandlerEmptyGuard`（5 tests）：项目创建/导出/恢复路径 bare Exception/TimeoutError → 非空且含关键词
- `TestSpecEngineInternalEmptyGuard`（2 tests）：last_error 和 rewrite_requirement 返回值非空
- `TestWorktreeManagerEmptyGuard`（2 tests）：init/merge bare Exception → 非空且 summary 无空尾
- `TestMainAppEmptyGuard`（1 test）：多种裸异常 → get_error_detail 非空

### 全量测试
- 1999 tests passed, 0 failed, 0 warning regressions（较前次 1995 新增 4 个测试，较前次 1981 共新增 18 个测试）

### 补充闭环（同 commit）
- **`src/feishu/handlers/spec.py:412`** — `fmt_error("保存 Spec 状态", str(e))` → `fmt_error("保存 Spec 状态", e)`（直传异常对象，走 isinstance 分支处理 TimeoutError 空消息）
- **`src/feishu/handlers/system.py:371`** — TTADK model refresh `str(e)` → `get_error_detail(e)` + 新增 import
- **tests/test_empty_error_guard.py** — 新增 `TestSpecHandlerSaveStateEmptyGuard`（2 tests）和 `TestSystemHandlerTTADKRefreshEmptyGuard`（2 tests），总计 33 tests

### 提交记录
- `be61258` fix(guard): close all remaining str(exc) empty-value gaps + complete guard test coverage

---

### 任务描述
在已完成的四层 TimeoutError 防御体系基础上，修补 5 处 `str(exc)` 无空值守卫的残余缺口，确保 `TimeoutError(empty message)` 在任何路径均不暴露给终端用户。

### 执行内容
1. **`src/card/builders/system.py:31`** — `build_error_card` 中 `message = str(exc)` 改为 `message = get_error_detail(exc) if isinstance(exc, Exception) else (str(exc) or "未知错误")`，使用集中化的 `get_error_detail()` 处理所有异常类型
2. **`src/feishu/handlers/base.py:86-89`** — `send_error_card` 的 fallback 文本路径，新增 `fallback_detail = str(exc) or "操作失败"` 空值守卫，避免 `"❌ title: "` 空尾
3. **`src/tasking/scheduler.py:657,770`** — 两处 `state.error = str(e)` 改为 `str(e) or repr(e)`，`_emit` 使用 `state.error` 而非重复计算
4. **`src/utils/errors.py:121`** — `fmt_exception` 非超时路径 `str(exc)` 改为 `str(exc) or repr(exc)`，确保 `ValueError()` 等裸异常不产生空串
5. **`src/worktree_engine/dispatcher.py:120`** — `str(te).strip() or f"执行超时 ({timeout}s)"` 统一改为 `get_error_detail(te)`，与其他引擎保持一致架构

### 技术要点
- 所有修改统一收口到 `src/utils/errors.py` 的 `get_error_detail()` 或 `fmt_error()`，保持单一收口
- `repr(exc)` 作为最终兜底（如 `"TimeoutError()"`），永远不会返回空字符串
- `build_error_card` 同时处理 `Exception` 实例和 `str` 参数（签名 `exc: Exception | str`），需用 `isinstance` 分发
- scheduler 的 `_emit` 改为使用已计算的 `state.error` 而非重复 `str(e)`，避免不一致

### 新增文件
- `tests/test_empty_error_guard.py` — 15 个测试用例，覆盖所有 5 处修改：
  - `TestBuildErrorCardEmptyGuard`（4 tests）：裸 TimeoutError、无空 body、字符串参数、带消息 TimeoutError
  - `TestBaseHandlerFallbackEmptyGuard`（1 test）：fallback 路径裸 TimeoutError 无空尾
  - `TestSchedulerEmptyErrorGuard`（2 tests）：裸 TimeoutError/Exception state.error 非空
  - `TestFmtExceptionEmptyGuard`（4 tests）：裸 Exception repr 兜底、ValueError repr、正常消息保留、timeout 固定文案
  - `TestWorktreeDispatcherGetErrorDetail`（1 test）：裸 TimeoutError → get_error_detail 输出含 "操作超时"
  - `TestGetErrorDetailNeverEmpty`（3 tests）：裸 TimeoutError/Exception/带消息 ValueError

### 全量测试
- 1981 tests passed, 0 failed, 0 warning regressions

### 提交记录
- `359fb82` fix(guard): close 5 remaining str(exc) empty-value gaps + 15 guard tests

---

## Loop Engine review 熔断器 + 收敛检测加固

### 任务描述
将 Spec Engine 已验证的 TimeoutError 三层防御体系推广到 Loop Engine，解决 Loop Engine review 连续超时时每轮浪费 120s 等待和误判收敛的问题。

### 执行内容
1. **config.py**: 新增 3 个配置项 `loop_review_failure_circuit_enabled` (True)、`loop_review_failure_max_consecutive` (3)、`loop_review_failure_cooldown_iterations` (3)
2. **models.py**: `IterationRecord` 新增 `review_decision: Optional[str]` 字段，含序列化/反序列化
3. **engine.py**: 新增 `LoopReviewCircuitState` dataclass（独立于 Spec 的 ReviewCircuitState，避免耦合）
4. **engine.py**: 改造 `_conduct_review` 返回 `tuple[ReviewResult, Optional[str]]`，加入熔断入口检查、成功重置、异常计数逻辑
5. **engine.py**: `execute` 和 `resume` 两处调用点解包 tuple，写入 `record.review_decision`
6. **engine.py**: `_detect_convergence` 增加 `review_failed` 开头的 review_decision 跳过逻辑
7. **test_loop_engine.py**: 修复 3 个既有 `TestConductReview` 测试（补充 mock 配置项、解包 tuple）

### 技术要点
- `LoopReviewCircuitState` 独立定义在 loop_engine/engine.py 而非复用 spec_engine/review.py 的 `ReviewCircuitState`，避免跨模块耦合和语义混淆（cycle vs iteration）
- review_decision 值域：`None`（成功）、`"review_failed_continue"`、`"review_failed_open_circuit"`、`"review_circuit_open_skip"`
- 收敛检测只跳过 `review_failed` 开头的轮次（不跳过 `review_circuit_open_skip`），这是设计决策：熔断跳过的轮次不算异常
- `_conduct_review` 的 f-string 日志改为 `%s` 占位符（消除 ruff W1203）

### 新增文件
- `tests/test_loop_review_circuit.py` — 8 个测试用例（初始状态、连续失败触发、冷却期跳过、冷却后恢复、成功重置、开关关闭、回调调用、混合场景）
- `tests/test_loop_convergence_skip.py` — 7 个测试用例（review_failed 阻止收敛、open_circuit 阻止、circuit_open_skip 不阻止、正常收敛、混合、不足窗口、标准停滞）

### 测试结果
- 新增 15 个测试全部通过
- 全量 1953 tests passed, 0 failed

### 提交记录
- `7176d7a` feat(loop): add review circuit breaker and convergence skip for TimeoutError hardening

---

## 修复 Spec Engine 收敛检测误判：review 异常轮次不应参与收敛比较

### 任务描述
Spec Engine 在循环 2 轮就触发收敛退出，但实际两轮都有 5 条审查建议应该继续循环。

### 根因分析
- `spec_convergence_window = 2`，只需连续 2 轮 suggestion 集合相同即判定收敛
- 循环 1 和循环 2 的 review 阶段都因 `TimeoutError` 失败
- 失败时 `conduct_review` 生成固定模板 fallback suggestions：每个视角一条 `"审查执行异常: TimeoutError (empty message)"`
- 两轮 fallback 文本完全相同 → `detect_convergence()` 的 `len(set(suggestion_sets)) == 1` 为 True → 误判为收敛
- 实际上这两轮根本没有执行真正的审查，不应参与收敛判定

### 执行内容
1. **修复 `src/spec_engine/convergence.py:detect_convergence()`**
   - 在遍历 recent cycles 构建 suggestion_sets 时，检查 `c.review_decision` 是否以 `"review_failed"` 开头
   - 如果是（timeout、熔断等异常情况），直接返回 `False`（不收敛）
   - 确保正常 review 的收敛检测逻辑不受影响

2. **新增测试 `tests/test_convergence.py::TestDetectConvergenceReviewFailed`**
   - `test_consecutive_review_failures_not_converged`：连续 2 轮 timeout 不判定为收敛
   - `test_one_failed_one_normal_not_converged`：1 轮异常 + 1 轮正常也不判定为收敛
   - `test_circuit_breaker_decision_not_converged`：熔断决策同样不参与收敛
   - `test_normal_reviews_still_converge`：正常 review 相同建议仍正确判定为收敛

### 技术要点
- review 异常时的 `review_decision` 字段固定以 `"review_failed"` 开头（`review_failed_continue`、`review_failed_open_circuit` 等）
- 收敛检测的本质是"LLM 反复给出相同建议却无法解决"，但异常产生的 fallback 文本不代表 LLM 的真实建议
- 修复方案最小化：仅在收敛检测中跳过异常轮次，不改变 review 异常处理本身的逻辑

### 提交记录
- `e1b99c4` fix(spec): skip review-failed cycles in convergence detection to prevent false exit

---

## 改进 Spec Engine 审查超时体验：解决 TimeoutError (empty message) 不友好文案

### 任务描述
Spec Engine review 阶段 ACP 调用超时后，用户看到 `"审查执行异常: TimeoutError (empty message)"` 这样的不友好建议文案，且连续超时会浪费大量时间（每轮 120s×3=360s）。

### 根因分析
- `SyncACPAdapter.future.result(timeout=120)` 超时抛出的是 Python 原生 `TimeoutError()`，不带任何消息
- `review.py` 的 `_extract_error_text()` 拿到空字符串后生成 `"TimeoutError (empty message)"` 兜底文案
- review timeout 120s 硬编码，无法配置
- 熔断器默认关闭，连续 TimeoutError 时每轮都浪费 360s

### 执行内容
1. **`src/acp/sync_adapter.py`** — `except TimeoutError` 分支改为 `raise TimeoutError(f"ACP prompt 执行超时 ({timeout}s)") from e`
2. **`src/config.py`** — 新增 `spec_review_timeout: int = 120` 配置项；`spec_review_failure_circuit_enabled` 默认改 `True`
3. **`src/spec_engine/review.py`** — 三处改动：
   - `conduct_review` 中 `timeout=120` 硬编码改为 `timeout=settings.spec_review_timeout`
   - `build_review_exception_diagnostics` 中 `fail_reason=="timeout"` 时 error_text 改为 `"审查超时，将在下一轮重试"`
   - fallback ReviewResult 中 `fail_reason=="timeout"` 时 suggestions 改为 `"审查超时，跳过本轮审查继续执行"`
4. **`tests/test_spec_engine.py`** — 新增 7 个测试：
   - `test_timeout_error_with_message_diagnostics_has_friendly_text` — TimeoutError 带消息时 diagnostics 正确
   - `test_timeout_error_empty_message_diagnostics_uses_friendly_text` — TimeoutError 空消息时使用中文友好文案
   - `test_non_timeout_error_empty_message_still_uses_empty_message_fallback` — 非 timeout 异常不受影响
   - `test_spec_review_timeout_config_exists_and_defaults` — 配置项存在且默认 120
   - `test_spec_review_timeout_passed_to_send_prompt` — 配置项正确传递到 send_prompt
   - `test_timeout_fallback_review_result_has_friendly_suggestion` — timeout fallback 使用友好文案
   - `test_non_timeout_fallback_review_result_keeps_generic_format` — 非 timeout fallback 保持原格式

### 技术要点
- `_infer_fail_reason` 使用 `isinstance(err, TimeoutError)` 判断，不依赖消息内容，sync_adapter 修改不影响
- 收敛检测已通过 `review_decision.startswith("review_failed")` 跳过异常轮次（commit e1b99c4），文案变更不影响
- 熔断器默认开启（`max_consecutive=3, cooldown=3`），连续超时时自动跳过审查节省时间

### 提交记录
- `416c13a` fix(spec): improve review timeout UX — friendly messages, configurable timeout, circuit breaker on by default

---

## 审查验证：TimeoutError (empty message) 改进建议落实确认

### 任务描述
对 commit 416c13a 和 e1b99c4 中的 TimeoutError 改进进行全面审查验证，确认修复完整性、补齐测试覆盖。

### 审查结论

#### T1: sync_adapter.py TimeoutError re-raise 逻辑 ✅
- `send_prompt` (line 1474) 超时时在 line 1477-1480 正确 re-raise 为 `TimeoutError(f"ACP prompt 执行超时 ({timeout}s)")`
- `_run_async` (line 1572) 无包装但仅用于生命周期管理（start/load），不走 prompt 路径，无需包装

#### T2: review.py _extract_error_text / _infer_fail_reason ✅
- `_infer_fail_reason` 通过 `isinstance(err, TimeoutError)` 精确检测（asyncio.TimeoutError 是子类）
- `_extract_error_text` 空消息时生成 `"TimeoutError (empty message)"`，但 line 185-187 立即替换为 `"审查超时，将在下一轮重试"`

#### T3: conduct_review 异常处理分支 ✅
- timeout → `"审查超时，跳过本轮审查继续执行"`
- 非 timeout → `"审查执行异常: {error_text}"`
- 区分正确且清晰

#### T4: config.py 配置项 ✅
- `spec_review_timeout: int = 120` 被 review.py:480 正确读取
- `spec_review_failure_circuit_enabled: bool = True` 默认开启
- `spec_review_failure_max_consecutive: int = 3` 和 `cooldown_cycles: int = 3` 正确生效

#### T5: convergence.py review_failed 跳过 ✅
- line 109: `review_decision.startswith("review_failed")` → return False
- 覆盖 `review_failed_continue`、`review_failed_open_circuit` 等所有异常 decision

#### T6: 其他引擎排查 ✅ 无遗漏
- `utils/errors.py:68-73`: `fmt_error` 已处理空消息 TimeoutError
- `loop_engine/engine.py:782-784`: 通过 isinstance + 字符串检测
- `deep_engine/reporter.py:45` 和 `loop_engine/reporter.py:208`: `"timeout" in err.lower()` 兜底
- 所有 feishu handlers 使用 `isinstance(e, (TimeoutError, asyncio.TimeoutError))`

### 新增测试
- `tests/test_review_timeout.py` — 14 个新测试，覆盖：
  - T7: sync_adapter 超时消息非空（2 tests）
  - T8: review 诊断友好文案（4 tests）
  - T9: conduct_review timeout 返回友好 ReviewResult（2 tests）
  - T10: 熔断器连续失败触发与恢复（3 tests）
  - T11: 收敛检测跳过 review_failed（3 tests）

### 全量测试
- 1934 tests passed, 0 failed, 0 warning regressions

---

## Worktree Engine TimeoutError 加固：推广 spec_engine 三层防御到 worktree_engine

### 任务描述
将 spec_engine 中已修复的 TimeoutError(empty message) 防御模式推广到 worktree_engine，避免相同漏洞在并行执行引擎中复现。

### 根因分析
- `worktree_engine/dispatcher.py` 的 `_run_single_unit` 中没有 `except TimeoutError` 分支
- 空消息 `TimeoutError("")` 经过 `str(exc)` 后变为空串，导致 `unit.error` 为空
- `execute_units` 外层和 `manager.py` 的 `state.last_error` 也存在同样的空串问题
- `reporter.py` 虽有 `unit.error or "执行失败"` 兜底，但错误信息丢失影响排查

### 执行内容
1. **`src/worktree_engine/dispatcher.py`** — 两处修改：
   - `_run_single_unit`：在 `except Exception` 前新增 `except TimeoutError as te` 分支，`unit.error = str(te).strip() or f"执行超时 ({timeout}s)"`
   - `execute_units`：外层 `except Exception` 中 `unit.error = str(exc).strip() or "执行异常"`

2. **`src/worktree_engine/manager.py`** — `execute_goal` 的 `except Exception` 中 `state.last_error = str(exc).strip() or "执行异常"`

3. **`tests/test_worktree_dispatcher.py`** — 新增 3 个测试：
   - `test_timeout_error_empty_message_unit_has_friendly_error`：`TimeoutError('')` → unit.error 非空且含"超时"
   - `test_timeout_error_with_message_preserves_original`：`TimeoutError('ACP prompt 执行超时 (120s)')` → 保留原始消息
   - `test_generic_exception_empty_message_has_fallback`：`Exception('')` → unit.error 非空（兜底文案）

4. **`tests/test_worktree_reporter.py`** — 新增 1 个测试：
   - `test_reporter_timeout_unit_shows_timeout_summary`：超时失败 unit 的 summary 展示包含超时信息

### 技术要点
- `TimeoutError` 是 `Exception` 子类，`except TimeoutError` 必须在 `except Exception` 之前
- `str(TimeoutError(""))` 在 Python 3.12 中返回空串，`.strip() or fallback` 模式确保非空
- worktree 并行执行中单 unit 超时不影响其他 unit，错误隔离完好
- 与 spec_engine 的区别：worktree 无收敛检测和熔断器需求（单轮并行执行，非迭代循环）

### 全量测试
- 1938 tests passed, 0 failed, 0 warning regressions

### 提交记录
- `5a60ef9` feat(worktree): add execution handler, TimeoutError hardening and comprehensive tests

---

## Loop Engine 结构化审查诊断：与 Spec Engine 对齐

### 任务描述
Loop Engine 的 `_conduct_review` 异常处理缺少结构化诊断（fail_reason/error_text/traceback_snippet 等），仅使用 `get_error_detail()` 字符串，不如 Spec Engine 的 `build_review_exception_diagnostics()` 可回溯。将两者对齐以提升 Loop Engine 审查异常的可诊断性和日志质量。

### 执行内容
1. **`src/utils/review_diagnostics.py`（新建）** — 从 `spec_engine/review.py` 提取 `build_review_exception_diagnostics`、`normalize_review_diagnostics`、`format_review_exception_log_line` 及内部辅助函数为独立可复用模块；`format_review_exception_log_line` 新增 `prefix` 参数（默认 `"[Spec]"`），Loop Engine 使用 `"[Loop]"`
2. **`src/spec_engine/review.py`（修改）** — 删除本地诊断函数实现，改为从 `utils.review_diagnostics` 导入并 re-export，保持所有外部引用不变
3. **`src/loop_engine/engine.py`（修改）** — 三处改动：
   - `LoopReviewCircuitState` 新增 `last_review_failure_diag: Optional[dict] = None` 字段
   - `_conduct_review` except 分支调用 `build_review_exception_diagnostics` 生成结构化 diag dict，存储到 `circuit.last_review_failure_diag`
   - 日志改用 `format_review_exception_log_line(diag, diag_json=..., prefix="[Loop]")` 结构化格式
   - 熔断器打开时补充 `decision/review_circuit_open/open_until_iter/consecutive_failures` 到 diag
   - try 前 `circuit.last_review_failure_diag = None` 清除旧值（与 Spec 一致）
4. **`tests/test_loop_review_circuit.py`（修改）** — 新增 6 个测试：
   - `test_timeout_error_produces_diag_with_timeout_reason` — TimeoutError → fail_reason='timeout'
   - `test_timeout_error_empty_message_friendly_text` — 裸 TimeoutError() → 无 "(empty message)"
   - `test_non_timeout_error_produces_diag_with_exception_reason` — RuntimeError → fail_reason='exception'
   - `test_diag_stored_on_circuit_state` — diag dict 包含所有 stable keys
   - `test_diag_cleared_on_success` — 成功 review 后 diag 置 None
   - `test_circuit_open_diag_has_extra_fields` — 熔断打开时 diag 含 circuit open 相关字段

### 技术要点
- `review_diagnostics.py` 放在 `src/utils/` 下，不依赖任何 engine 模块，避免循环导入
- `safe_str` 内联为 `_safe_str`（不从 `spec_engine.artifacts` 导入），保持 utils 独立性
- `format_review_exception_log_line` 通过 `prefix` 参数区分 `[Spec]` 和 `[Loop]` 日志前缀
- `LoopReviewCircuitState.last_review_failure_diag` 使用 `Optional[dict] = None` 默认值，向后兼容
- Spec Engine 的所有测试（188 个）无任何修改即通过，证明 re-export 策略零风险

### 全量测试
- 1959 tests passed, 0 failed, 0 warning regressions

### 提交记录
- `41b5970` refactor(review): extract shared review diagnostics module and integrate structured diagnostics in LoopEngine

## 三引擎 execute/resume 顶层 TimeoutError 分支加固

### 任务描述
将已有的 review 层 TimeoutError 三层防御推广到三引擎（Deep/Loop/Spec）的 execute/resume 顶层 except 块，将超时日志从 ERROR 降为 WARNING 并使用区分性文案（"执行超时" vs "执行异常"），形成完整的四层 TimeoutError 处理体系。

### 执行内容
1. **`src/deep_engine/engine.py`** — 3 处修改：
   - `plan_and_execute` except Exception 前新增 `except TimeoutError` 分支，`logger.warning` + `"执行超时"` 文案
   - `_drain_pending_context` except Exception 前新增 `except TimeoutError` 分支，`logger.warning` + `"超时"` 文案
   - `resume` except Exception 前新增 `except TimeoutError` 分支，`logger.warning` + `"恢复执行超时"` 文案
2. **`src/loop_engine/engine.py`** — 2 处修改：
   - `execute` except Exception 前新增 `except TimeoutError` 分支，`logger.warning` + `"Loop执行超时"` 文案，设置 `ABORTED` 状态
   - `resume` except Exception 前新增 `except TimeoutError` 分支，`logger.warning` + `"Loop恢复超时"` 文案
3. **`src/spec_engine/engine.py`** — 2 处修改：
   - `execute` except Exception 前新增 `except TimeoutError` 分支，`logger.warning` + `"Spec执行超时"` 文案，保留 `_save_failed_task` 逻辑
   - `resume` except Exception 前新增 `except TimeoutError` 分支，`logger.warning` + `"Spec恢复超时"` 文案，保留 `_save_failed_task` 逻辑

### 技术要点
- Python 3.11+ 中 `asyncio.TimeoutError` 是 `TimeoutError` 的子类，单一 `except TimeoutError` 即可同时捕获两者
- 所有 TimeoutError 分支的业务逻辑（project 状态设置、callbacks 调用、_save_failed_task）与原 Exception 块完全一致，仅日志级别（WARNING vs ERROR）和文案（"超时" vs "异常"）不同
- Spec Engine 的 TimeoutError 分支同样调用 `_save_failed_task` 保存异常任务，确保超时中断的任务可恢复

### 新增文件
- `tests/test_engine_timeout.py` — 7 个测试用例：
  - `test_plan_and_execute_timeout` — Deep execute TimeoutError → FAILED + WARNING
  - `test_drain_pending_context_timeout` — Deep drain TimeoutError → WARNING + 优雅返回
  - `test_resume_timeout` — Deep resume TimeoutError → FAILED + WARNING
  - `test_execute_timeout` (Loop) — Loop execute TimeoutError → ABORTED + WARNING
  - `test_resume_timeout` (Loop) — Loop resume TimeoutError → ABORTED + WARNING
  - `test_execute_timeout` (Spec) — Spec execute TimeoutError → ABORTED + WARNING
  - `test_resume_timeout` (Spec) — Spec resume TimeoutError → ABORTED + WARNING

### 全量测试
- 1966 tests passed, 0 failed, 0 warning regressions

### 提交记录
- `3b237e5` fix(timeout): harden TimeoutError handling across all engines and eliminate empty message display
  - 合并了「三引擎 execute/resume 顶层 TimeoutError 分支加固」和「闭合残余缺口」两项改动
  - 包含 7 个新测试 (test_engine_timeout.py) + 2 个测试断言更新 (test_convergence.py, test_log_noise.py)


## 闭合「审查执行异常: TimeoutError (empty message)」残余缺口

### 任务描述
虽然 review_diagnostics.py 已对 timeout+empty 场景做了友好化，但仍有 3 处残余缺口将 `(empty message)` 英文技术细节暴露给用户。本次增量修补闭合所有缺口。

### 执行内容
1. **src/feishu/handlers/engine_base.py**: `_safe_lifecycle_action` 中 TimeoutError 的用户消息从裸 `str(e)` 改为 `get_error_detail(e)`，消除空尾消息（如 `"❌ pause失败: "` → `"❌ pause失败: 操作超时，请稍后重试"`）
2. **src/loop_engine/engine.py**: `_conduct_review` 非 timeout 异常分支，当 `detail` 包含 `(empty message)` 或为空时，`err_msg` 改为中文友好文案 `"审查执行异常，将在下一轮重试"`
3. **src/spec_engine/review.py**: `conduct_review` 非 timeout 异常分支，当 `error_text` 包含 `(empty message)` 或为空时，`_suggestion_text` 改为 `"审查执行异常，将在下一轮重试"`
4. **tests/test_convergence.py**: `_make_failed_review` fixture 从旧的 `"审查执行异常: TimeoutError (empty message)"` 更新为 `"审查执行异常，将在下一轮重试"`
5. **tests/test_log_noise.py**: `test_lifecycle_action_timeout_warning` 断言更新为 `get_error_detail` 格式化后的消息格式

### 技术要点
- 仅修改用户可见的 `err_msg` / `_suggestion_text` / reply_message 文案，不修改 diagnostics dict 中的 `error_text`（日志层保持原始技术信息用于排查）
- `get_error_detail` 内部调用 `fmt_error`，对 TimeoutError 空消息输出 `"操作超时，请稍后重试"`，有消息时输出 `"操作超时 (消息内容)"`
- 三处 `(empty message)` 检测使用 `in` 而非 `==`，覆盖 `"SomeError (empty message)"` 等复合格式

### 全量测试
- 1966 tests passed, 0 failed, 0 warning regressions

### 提交记录
- 与「三引擎 execute/resume 顶层 TimeoutError 分支加固」合并提交于 `3b237e5`

## review_diagnostics 源头消灭 (empty message) 标记 + 低风险路径增量加固

### 任务描述
落实改进建议：消除 `审查执行异常: TimeoutError (empty message)` 问题。将 review_diagnostics 层的 `(empty message)` 标记从"仅 timeout 替换"升级为"所有异常类型源头消灭"，同时补强 3 处低风险路径的超时上下文丢失问题。

### 执行内容
1. **源头加固** `src/utils/review_diagnostics.py`: `build_review_exception_diagnostics` 中 `(empty message)` 标记不再产生，空消息异常直接分流：timeout→"审查超时，将在下一轮重试"，非timeout→"审查执行异常，请检查服务状态"
2. **上下文保留** `src/worktree_engine/dispatcher.py:86`: `str(exc).strip() or "执行异常"` → `get_error_detail(exc)`
3. **上下文保留** `src/worktree_engine/manager.py:169`: `str(exc).strip() or "执行异常"` → `get_error_detail(exc)`
4. **上下文保留** `src/feishu/handlers/base.py:86`: `str(exc) or "操作失败"` → `get_error_detail(exc)`
5. **日志补强** `src/deep_engine/engine.py:336`: `logger.warning` bare `%s` → `str(e) or repr(e)`
6. **新增 14 个测试**: `TestDiagnoseReviewFailureNoEmptyMessageMarker`(9) + `TestWorktreeDispatcherTimeoutContext`(2) + `TestWorktreeManagerTimeoutContext`(1) + `TestBaseHandlerFallbackTimeoutContext`(2)

### 技术要点
- **源头消灭 vs 下游过滤**: 之前 `(empty message)` 标记在 diagnostics 层产生、由 spec/loop 消费端过滤；现在直接在 diagnostics 层消灭，下游过滤变为冗余安全网
- **err_repr 不受影响**: 日志中 `err_repr` 字段仍保留 `repr(exc)` 完整信息，调试能力不减
- 修改 5 个 src 文件、1 个 test 文件，新增 14 个测试

### 全量测试
- 2057 tests passed, 0 failed, 0 regressions
