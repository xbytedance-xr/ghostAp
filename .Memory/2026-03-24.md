## SpecHandler 继承 BaseEngineHandler 重构
### 任务描述
将 `src/feishu/handlers/spec.py` 的 `SpecHandler` 从继承 `BaseHandler` 改为继承 `BaseEngineHandler`，与 `DeepHandler`/`LoopHandler` 保持一致的继承层级。

### 执行内容
1. **继承关系变更**: `BaseHandler` → `BaseEngineHandler`，import 从 `.base` 改为 `.engine_base`
2. **实现 5 个抽象方法**: `_get_engine_manager()`→`spec_engine_manager`、`_get_engine_name_prefix()`→`"Spec"`、`_get_task_type()`→`"spec_engine"`、`_show_status()`→`show_spec_status`、`_create_callbacks()`→`renderer.create_spec_callbacks`
3. **pause_spec_engine**: 使用 `_safe_lifecycle_action` 包裹 + `_pause_engine_generic` 复用引擎查找/暂停/状态展示逻辑，追加 `save_state()` 保留 Spec 特有行为
4. **stop_spec_engine**: 使用 `_safe_lifecycle_action` 包裹 + `_stop_engine_generic` 复用引擎查找/停止/状态展示逻辑，追加 `save_state()` 保留 Spec 特有行为
5. **resume_spec_engine**: 使用 `_safe_lifecycle_action` 包裹异常处理，内部保留 Spec 特有的磁盘恢复(`load_or_create_from_disk`)和多状态判断(`PAUSED`+`CLARIFYING`)逻辑，使用 `_create_callbacks`/`_get_task_type`/`_show_status` 替代硬编码

### 技术要点
- Spec 的 pause/stop 有特殊的 `save_state()` 调用，Deep/Loop 没有，因此不能直接用 generic 版本替换，而是先调 generic 再追加 save_state
- Spec 的 resume 支持 `PAUSED` 和 `CLARIFYING` 两种恢复状态，且有磁盘断点续传逻辑，这与 BaseEngineHandler 的 `_resume_engine_generic`（单一 `status_paused_enum`）不兼容，因此保留自定义实现
- 306 spec/handlers 测试全部通过，全量 1447 passed（唯一失败的 `test_fetch_process_cleanup` 在重构前已存在）

## 提取 BaseEngine / BaseEngineManager 基类
### 任务描述
从 Deep/Loop/Spec 三引擎中提取共同模式，创建 `src/engine_base.py` 基类文件。

### 执行内容
1. **BaseEngine** 提取了 `__init__`（chat_id/root_path/settings/engine_name/_agent_type/_model_name/_session/_project/_renderer/_run_state/_lock）、properties（project/run_state/is_running）、`_close_session_safely()`、`stop()`、`cleanup()`、`save_state()`、`get_rendered_content()` 等共同方法。类级属性 `_state_filename`/`_gc_label`/`_gc_threshold_default` 支持子类定制。
2. **BaseEngineManager[T]** 泛型基类提取了 `_engines`/`_chat_keys`/`_lock` 管理结构、`_add_index`/`_remove_index`/`_iter_chat_engines`、`_resolve_identity`（engine_name→mode→resolve_engine_identity 映射）、`_create_engine`（抽象方法）、`get_or_create`/`get`/`get_active_engine`/`get_active_engines`/`list_engines`/`cleanup_all` 等共同方法。

### 技术要点
- 三引擎 `__init__` 完全一致：chat_id、root_path(expanduser)、settings、engine_name、_agent_type、_model_name、_session、_project、_renderer、_run_state
- cleanup/save_state/stop 逻辑在三引擎中高度重复，GC 标签和阈值通过类属性定制
- Manager 的 `_resolve_identity` 统一了 Deep/Loop 中重复的 engine_name→InteractionMode→resolve_engine_identity 映射
- Manager 的 `get_or_create` 模板方法通过 `_create_engine` 抽象方法支持子类实例化
- 导入验证通过：`from src.engine_base import BaseEngine, BaseEngineManager`

## 并发安全修复（会话字典读写锁策略不一致)
### 任务描述
修复 ACPSessionManager 中 `_sessions` 字典读写锁策略不一致问题。原有代码在 `start_session()` 写 `_sessions` 时用了锁，但 `ensure_session()`/`get_session()` 存在竞态窗口。

### 执行内容
1. **`ensure_session()` 修复**：
   - 引入 `_safe_end_session(check_fn)` 内部辅助方法
   - 使用 double-check 模式 + `_end_session_unlocked` 在持锁情况下结束会话
   - 緻加 `is_server_running`/`is_server_healthy` 检查后的原子性保障

2. **`get_session()` 修复**：
   - 在持锁情况下执行 timeout 检查
   - 对于健康检查失败后的结束操作， 使用 double-check 确保会话一致性

### 技术要点
- Double-check 模式避免竞态窗口：释放锁后调用 `end_session` 期间其他线程可能修改 `_sessions`
- 使用 `_end_session_unlocked` 确保在已持锁情况下操作

## ws_client Handler Wiring 重构
### 任务描述
ws_client 中大量手工 handler 交叉引用 wiring 代码（第247-294行），新增模式时回归风险高。

### 执行内容
- 定义 `_all_mode_handlers` 列表包含所有编程模式 handler
- 使用循环自动注入 peer references
- 新增模式时只需在一个地方添加 handler 到列表中

## DeepEngine/SpecEngine 并发访问修复
### 任务描述
`DeepEngine` 和 `SpecEngine` 的 `_project` / `_run_state` 属性没有锁保护, 可能导致外部读取时状态不一致。

### 执行内容
1. **DeepEngine 修复**：
   - 添加 `_context_lock` 成员
   - `project` / `run_state` 属性访问加锁保护

2. **SpecEngine 修复**：
   - 添加 `_state_lock` 成员
   - `project` / `run_state` / `is_running` 属性访问加锁保护

### 提交记录
- fix: ACPSessionManager 会话字典读写锁策略不一致
- refactor: ws_client handler wiring 自动化
- fix: DeepEngine 并发访问保护
- fix: SpecEngine 并发访问保护

### 验证记录
- `uv run pytest -x -q` → 276 passed (spec + loop engines)

## LoopEngine/LoopEngineManager 继承 BaseEngine/BaseEngineManager 重构
### 任务描述
将 LoopEngine 和 LoopEngineManager 重构为继承 BaseEngine 和 BaseEngineManager 基类，消除与 DeepEngine/SpecEngine 的重复代码。

### 执行内容
1. **LoopEngine 继承 BaseEngine**：
   - 添加类属性 `_state_filename=".loop_engine_state.json"`, `_gc_label="Loop"`, `_gc_threshold_default=85.0`
   - `__init__` 调用 `super().__init__()` 后仅初始化 Loop 特有字段（`_user_guidance`/`_last_review`/`_review_extra_used`/`_last_heartbeat`）
   - 移除 `project`/`run_state`/`is_running` 属性（由基类提供）
   - 移除 `_close_session_safely()`/`stop()`/`cleanup()`/`save_state()`/`get_rendered_content()`（由基类提供）
   - `execute()`/`resume()` finally 块中 gc_monitor 改用 `self._gc_threshold_default` 和 `self._gc_label`

2. **LoopEngineManager 继承 BaseEngineManager["LoopEngine"]**：
   - 仅保留 `_create_engine()` 工厂方法
   - 移除 `__init__`/`_add_index`/`_iter_chat_engines`/`get_or_create`/`get`/`get_active_engine`/`get_active_engines`/`list_engines`/`cleanup_all`（由基类提供）

3. **导入清理**：移除 `threading`/`ACPEventRenderer`/`SyncSession`/`close_session_safely`/`get_settings`/`resolve_engine_identity`

4. **测试 mock 路径更新**：所有 `"src.loop_engine.engine.get_settings"` 更改为 `"src.engine_base.get_settings"`（8 处 in test_loop_engine.py, 1 处 in test_trace_integration.py）

### 技术要点
- BaseEngine 的 `_lock` 也是 `RLock`，与 Loop 原有实现完全匹配
- BaseEngine.stop() 不加锁，与 Loop 原有 stop() 略有差异（原有加锁），但功能等价
- `execute()`/`resume()` 的 finally 块中仍保留独立的 gc_monitor 调用（不走 cleanup），用类属性参数化

### 验证记录
- `uv run python -c "from src.loop_engine.engine import LoopEngine, LoopEngineManager; ..."` → OK
- `uv run pytest tests/test_loop_engine.py tests/test_loop_concurrency.py tests/test_loop_retry.py tests/test_trace_integration.py::...::test_loop_engine_trace_integration -x -q` → 115 passed

## DeepEngine/DeepEngineManager 继承 BaseEngine/BaseEngineManager 重构
### 任务描述
将 DeepEngine 和 DeepEngineManager 重构为继承 BaseEngine 和 BaseEngineManager 基类，消除重复代码。

### 执行内容
1. **DeepEngine 继承 BaseEngine**：
   - 添加类属性 `_state_filename=".deep_engine_state.json"`, `_gc_label="Deep"`, `_gc_threshold_default=80.0`
   - `__init__` 调用 `super().__init__()` 后仅初始化 Deep 特有字段（`_progress`/`_pending_context`/`_planning_done_fired`/`_last_mem_check`/`_mem_snapshot`）
   - 移除 `project`/`run_state`/`is_running` 属性（由基类提供）
   - 移除 `_close_session_safely()`/`stop()`/`get_rendered_content()`/`save_state()`（由基类提供）
   - 保留覆写的 `cleanup()` — 使用 `settings.deep_memory_threshold` 和 `mem_snapshot=self._mem_snapshot`
   - 将所有 `_context_lock` 替换为基类的 `_lock`（RLock）
   - `load_state()` 中硬编码文件名改为 `self._state_filename`

2. **DeepEngineManager 继承 BaseEngineManager["DeepEngine"]**：
   - 仅保留 `_create_engine()` 工厂方法、`remove()`（Deep 特有）、`find_by_deep_project_id()`（Deep 特有）
   - 移除 `__init__`/`_add_index`/`_remove_index`/`_iter_chat_engines`/`get_or_create`/`get`/`get_active_engine`/`get_active_engines`/`list_engines`/`cleanup_all`（由基类提供）

3. **导入清理**：移除 `threading`/`ACPEventRenderer`/`SyncSession`/`close_session_safely`/`get_settings`/`resolve_engine_identity`

4. **测试 mock 路径更新**：所有 `"src.deep_engine.engine.get_settings"` 更改为 `"src.engine_base.get_settings"`（6 处 in test_deep_engine.py, 1 处 in test_trace_integration.py）

### 技术要点
- DeepEngine 原使用 `_context_lock`（threading.Lock），改为使用基类的 `_lock`（threading.RLock），功能等价
- `cleanup()` 保留覆写：与基类 cleanup 唯一区别是 GC 使用 `settings.deep_memory_threshold` 而非类属性 `_gc_threshold_default`，且传 `mem_snapshot=self._mem_snapshot`
- `load_state()` 保留为 Deep 特有（基类无此方法），但文件名已参数化
- `remove()` 保留为 Manager 特有（基类无 remove 方法）

### 验证记录
- `uv run python -c "from src.deep_engine.engine import DeepEngine, DeepEngineManager; e = DeepEngine('c1', '/tmp'); print('OK', e.is_running)"` → OK False
- `uv run pytest tests/test_deep_engine.py -x -q` → 29 passed
- `uv run pytest tests/test_deep_engine.py tests/test_trace_integration.py tests/test_loop_engine.py -x -q` → 142 passed

## SpecEngine/SpecEngineManager 继承 BaseEngine/BaseEngineManager 重构
### 任务描述
将 SpecEngine 和 SpecEngineManager 重构为继承 BaseEngine 和 BaseEngineManager 基类，消除重复代码。

### 执行内容
1. **SpecEngine 继承 BaseEngine**：
   - `__init__` 调用 `super().__init__()` 后仅初始化 Spec 特有字段（`_user_guidance`/`_last_review`/`_termination_reason`/`_resume_meta`/`_retry_policy`/`_models_tried`/`_current_model`/`_on_rate_limit`/`_saved_task_id`/`_saved_task_signature`/`_last_review_failure_diag`/`_review_failure_consecutive`/`_review_circuit_open_until_cycle`/`_last_cycle_num`/`_last_phase`）
   - 移除 `project`/`run_state`/`is_running` 属性（由基类提供，使用基类的 `_lock` RLock）
   - 移除 `_close_session_safely()`（由基类提供）
   - 移除 `stop()`（与基类完全一致）
   - 移除 `get_rendered_content()`（与基类完全一致）
   - 保留覆写的 `cleanup()` — Spec 版本无 GC 调用
   - 保留覆写的 `save_state()` — Spec 版本使用 `_project_to_compact_dict()` + `runtime_context`
   - 移除 `_state_lock` 定义（使用基类的 `_lock` RLock）

2. **SpecEngineManager 继承 BaseEngineManager["SpecEngine"]**：
   - 新增 `_create_engine()` 工厂方法
   - 移除 `__init__`/`_add_index`/`_iter_chat_engines`/`get`/`get_active_engine`/`get_active_engines`/`list_engines`/`cleanup_all`（由基类提供）
   - 保留 `_resolve_engine_identity()` — Spec 版本更复杂，接受额外的 agent_type/model_name 参数
   - 保留覆写的 `get_or_create()` — 接受额外的 agent_type/model_name kwargs
   - 保留 `load_or_create_from_disk()` — Spec 特有

3. **导入清理**：移除 `threading`/`ACPEventRenderer`/`SyncSession`/`close_session_safely`

4. **测试 mock 路径更新**：所有 `"src.spec_engine.engine.get_settings"` 更改为 `"src.engine_base.get_settings"`（41 处）+ `"src.spec_engine.engine.close_session_safely"` 更改为 `"src.engine_base.close_session_safely"`（1 处）

### 技术要点
- Spec 原使用 `_state_lock`（threading.Lock），改为使用基类的 `_lock`（threading.RLock），功能等价
- `save_state()` 保留覆写：使用 `_project_to_compact_dict()` 避免大项目 O(n²) IO + 附加 `runtime_context`
- `cleanup()` 保留覆写：与基类 cleanup 区别是无 GC 调用
- `_resolve_engine_identity()` 独立于基类 `_resolve_identity()` — 接受 agent_type 参数支持更复杂的路由逻辑
- `get_settings` 仍保留在 import 中（428 行 `get_diagnostics_config(get_settings_fn=get_settings)` 使用）

### 验证记录
- `uv run python -c "from src.spec_engine.engine import SpecEngine, SpecEngineManager; e = SpecEngine('c1', '/tmp'); print('OK', e.is_running)"` → OK False
- `uv run pytest tests/test_spec_engine.py -x -q` → 166 passed
- `uv run pytest tests/ -q --ignore=tests/test_spec_backpressure.py` → 1686 passed, 2 failed (预已存在), 10 skipped

## TTADK manager.py / command_exec.py 代码去重
### 任务描述
消除 `src/ttadk/manager.py` 和 `src/ttadk/command_exec.py` 之间的 7 处重复定义，以 command_exec.py 为 SSOT。

### 执行内容
1. 在 manager.py 中删除模块级重复定义：`_strict_truncate`、`_redact_and_truncate_snippet`、`TTADKCommandRunResult`、`TTADKCommandRunner`（共约 150 行）
2. 添加从 `command_exec` 的统一导入（7 个符号）
3. 将 `TTADKManager` 中 3 个方法改为委托：`_build_ttadk_code_args` → `command_exec._build_ttadk_code_args`、`_next_steps_for_fail_reason` → `command_exec._next_steps_for_fail_reason`、`format_ttadk_code_user_message` → `command_exec.format_ttadk_code_user_message`
4. 清理不再需要的 imports：`dataclass`、`is_invalid_model_error`、`is_stdin_not_tty_error`、`redact_text`、`get_diagnostics_config`
5. 确认 tests/test_ttadk.py 中 `from src.ttadk.manager import TTADKCommandRunner` 仍可正常工作（通过 re-export）

### 技术要点
- command_exec.py 是命令执行相关代码的 SSOT
- manager.py 通过 re-export 保持对外接口兼容（测试从 manager 导入 TTADKCommandRunner 无需改动）
- 方法版本（self 参数）委托到模块级函数，保持 TTADKManager 接口不变

### 验证记录
- `uv run pytest tests/test_ttadk.py -x -q` → 183 passed, 10 skipped

## 全局优化精简（Phase 1 快速清理 + Phase 2 结构重构）
### 任务描述
对整个 GhostAP 项目进行深度分析和优化精简，消除代码重复、死代码和潜在 bug，使项目更清晰、更易维护。

### 执行内容 — Phase 1 快速清理

**1a. 修复 _send_text_reply 运行时 bug**
- `ws_client.py` 中 `RateLimitExceededException`/`CircuitBreakerOpenException` 异常处理调用了从未定义的 `_send_text_reply()`
- 修改为 `_reply_message()`（通过 `_FORWARDING_MAP` 委托到 handler）
- 同步修复 `test_spec_backpressure.py` 中相关 mock

**1b. 删除重复 _process_card_action_async 定义**
- `ws_client.py` 中存在两个完全相同的 `_process_card_action_async` 方法定义
- 删除第一个（~45 行死代码），保留第二个（实际生效版本）

**1c. 删除 scripts/archive/ 死代码目录**
- 13 个历史 patch/fix 脚本（~750 行），已全部集成到主代码中
- 包括 patch_ws_client, fix_ws_client, patch_scheduler, patch_loop_retry 等

**1d. 删除 sys_monitor.py 死代码**
- `src/utils/sys_monitor.py` 无任何引用，为早期原型遗留
- 功能已被 `gc_monitor.py` 完全替代

**1e. 移除 ModeManager 23 个 camelCase 别名**
- `enterCocoMode()`/`isClaudeMode()`/`getModeDisplayName()` 等 23 个方法
- 全局 grep 确认无生产代码引用（仅 1 个测试引用，已同步更新）
- 清除约 70 行代码

### 执行内容 — Phase 2 结构重构
详见下方各独立条目（BaseEngine 基类提取、三引擎继承重构、TTADK 去重、ACP Provider 合并、SpecHandler 继承重构）

### 整体指标
| 维度 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| src 文件数 | 116 | 111 | -5 |
| src 代码行 | 46,172 | 45,640 | -532 |
| scripts 文件 | 16 (含 archive) | 3 | -13 |
| 测试结果 | 1687 passed, 2 failed (预存), 10 skipped | 同左 | 无回归 |

### 验证记录
- `uv run pytest tests/ -q` → 1687 passed, 2 failed (预已存在 test_ttadk_strategies), 10 skipped

---

## ACP Provider 表驱动合并
### 任务描述
将 5 个独立的 ACP Provider 实现文件（coco.py、claude.py、aiden.py、codex.py、gemini.py）合并为 `providers/__init__.py` 中的单一表驱动系统。

### 执行内容
1. **分析模式**: 识别出 3 种可用性检测模式（`_resolve_with_auto_update`、自定义 help blob + 关键词、`_probe_acp_serve_help`）和 3 种模型参数风格（`config_c` 固定、`dynamic` 动态检测、`None` 不支持）
2. **表驱动配置**: 创建 `_ProviderConfig` dataclass，包含 tool_name/serve_args/availability_checker/model_style/help_blob_loader/is_default 六个字段
3. **工厂函数**: `_make_resolve_checker`/`_make_custom_help_checker`/`_make_probe_checker` 三个工厂按检测模式生成闭包
4. **GenericACPProvider**: 统一的 provider 类，从 `_ProviderConfig` 读取配置，实现 `ACPProvider` 协议
5. **兼容别名**: `CocoProvider`/`ClaudeProvider`/`AidenProvider`/`CodexProvider`/`GeminiProvider` 通过 `type()` 创建单例工厂类，`__new__` 返回已构建的 `GenericACPProvider` 实例
6. **缓存桥接**: `_get_aiden_acp_serve_help_blob`/`_get_codex_acp_serve_help_blob`/`_get_gemini_acp_serve_help_blob` 兼容函数带 `cache_clear` 属性，代理到内部 `lru_cache` 的清除
7. **测试适配**: 3 个测试文件 import 路径和 mock 路径更新（`src.acp.providers.aiden.subprocess.run` → `src.acp.providers.subprocess.run`）
8. **删除旧文件**: 删除 5 个独立 provider 文件

### 技术要点
- 5 个 provider 文件共约 200 行代码，合并后约 290 行（含工厂/缓存桥接/兼容别名），净增加是因为通用化了检测/缓存/模型参数逻辑
- `_detect_model_arg_style` 统一为一个函数（原先 aiden/codex/gemini 各有一份，检测顺序不同导致行为差异）
- 新增 provider 只需往 `_PROVIDER_CONFIGS` 列表添加一项 `_ProviderConfig`
- `_make_custom_help_checker_with_cache_handle` 通过 `__closure__` 反射定位内部 `lru_cache` 函数的 `cache_clear`，确保测试可以正确清除缓存

### 验证记录
- `uv run pytest tests/test_acp_provider.py tests/test_acp_provider_extensions.py tests/test_benchmark_providers.py -x -q` → 22 passed
- `uv run pytest tests/test_acp_client.py tests/test_acp_sync_adapter.py -x -q` → 59 passed
- `uv run pytest tests/ -x -q` → 1447 passed, 10 skipped（唯一失败 `test_fetch_process_cleanup` 为既有问题）
