# 2026-03-25 项目记录

## TTADK CLI 模式 prompt 传递修复

### 任务描述
TTADK 模式完全不能用——`SyncTTADKCLISession.send_prompt()` 将用户 prompt 作为 `ttadk code` 的位置参数，但 `ttadk code` 不接受位置参数（报错 "too many arguments for 'code'"）。

### 执行内容

**根因分析**
- `cmd.append(text)` 把 prompt 追加到 `["ttadk", "code", "-t", tool]` 末尾
- `ttadk code` 的 CLI 接口只接受选项（`-t`, `-m`, `-a`），不接受位置参数
- 正确方式是通过 `-a` (args passthrough) 将参数传递给底层工具

**工具调研**
- coco/claude: `-p` (print mode) 进入非交互模式，prompt 作为位置参数
- gemini: `-p/--prompt "text"` 进入 headless 模式
- codex: prompt 直接作为位置参数，无 `-p` print mode
- ttadk 的 `-a` 参数做 shell-like 解析（尊重引号），因此 `shlex.join()` 可正确编码

**修改文件**
1. `src/agent_session.py`:
   - 添加 `shlex` import
   - 新增 `_TTADK_PRINT_MODE_TOOLS = frozenset({"coco", "claude", "gemini"})`
   - 新增 `_build_ttadk_passthrough_prompt()` 函数：print-mode 工具用 `-p prompt`，其他用纯位置参数
   - `send_prompt()` 中 `cmd.append(text)` → `cmd.extend(["-a", _build_ttadk_passthrough_prompt(...)])`
   - 扩展 `_TTADK_PREAMBLE_PATTERNS` 新增 3 个模式（⚠/❯ emoji、Continuing/Login/Launching 行）
   - 添加 `Popen` 前 debug 日志 `logger.debug("[TTADK:CLI] cmd=%s cwd=%s", ...)`
2. `tests/test_ttadk.py`:
   - `TestBuildTTADKPassthroughPrompt` (8 tests): 验证各工具的参数格式化
   - `TestSyncTTADKCLISessionCmdArgs` (3 tests): 验证 send_prompt 命令构建正确性
   - `TestTTADKPreambleNewPatterns` (4 tests): 验证新增 preamble 模式

### 技术要点
- `ttadk code -a "..."` 做 shell-like 解析，`shlex.join()` 生成正确的引号格式
- coco/claude/gemini 的 `-p` flag 让工具在无 TTY 环境下也能执行（输出结果后退出）
- codex 没有 `-p` print mode，但 subprocess 环境下模型选择需要 `-m` 参数避免交互卡住
- 多个 `-a` 标志不被 ttadk 支持（只有最后一个生效），必须用单个 `-a` + 完整参数串

### rloop 审查记录
- Round 1: 研发工程师提出添加 debug 日志（🟡）→ 已落实
- Round 2: 所有角色通过，仅剩 🟢 建议（`Login successful` 可锚定行首 — 低风险可接受）
- 总轮次: 2 轮

### 验证记录
- `uv run pytest tests/ -q` → 1697 passed, 2 failed (预已存在), 10 skipped
- 端到端真实执行验证: `ttadk code -t coco -a "-p 'what is 3+3?'"` → 正确返回 "6"

---

## TTADK 卡片 banner 过滤 + 标题增强

### 任务描述
两个问题：(1) TTADK ASCII art banner 第 3 行含单引号 `'`，未被 preamble 正则过滤，残留在飞书卡片中；(2) 卡片标题只显示 `🎮 ghostAp · TTADK`，不显示当前代理的工具名（如 claude/gemini）和模型名。

### 执行内容

**1. Preamble 过滤修复**
- 根因：正则 `^[\s_/\\|.-]{6,}$` 不包含单引号 `'`，banner 第 3 行 `| |   | | / _ \ | | | | ' /` 未被匹配
- 修复：字符类扩展为 `[\s_/\\|.'"-]`，同时加入双引号 `"` 防御其他 FIGlet 变体

**2. 卡片标题增强**
- `src/card/shared.py`: `resolve_title_and_template()` 新增 `ttadk_tool_name/ttadk_model_name` 参数 + `_build_ttadk_title_suffix()` 辅助函数
- `src/card/streaming.py`: `create_streaming_card()` 和 `_resolve_title_and_template()` 透传新参数
- `src/feishu/handlers/programming.py`: 基类新增 `_get_ttadk_tool_display()` / `_get_ttadk_model_display()`；TTADKModeHandler 覆写前者复用 `_get_agent_type_override` 去前缀
- `src/card/builders/core.py`: `_build_header_title()` 从 `project.ttadk_tool_name/model_name` 构建非流式卡片标题
- 标题格式示例：`🎮 ghostAp · TTADK · claude(glm-5)`

**3. 新增测试**
- `TestTTADKTitleSuffix` (3 tests): suffix 辅助函数
- `TestResolveTitleTTADKWithToolModel` (5 tests): 各种 tool/model 组合
- `TestPreambleAsciiArtSingleQuote` (2 tests): 单引号过滤 + 全 5 行 banner 验证

### rloop 审查记录
- Round 1: 架构师提出简化 `_get_ttadk_tool_display` 复用 `_get_agent_type_override`（🟡）→ 已落实
- Round 2: 所有角色通过
- 总轮次: 2 轮

### 验证记录
- `uv run pytest tests/ -q` → 1679 passed, 10 skipped, 0 regression

---

## TTADK 手机端最小交互 + YOLO 模式优化

### 任务描述
优化手机端 TTADK 模式进入与核心编程流程，减少多次确认/选择交互；新增 YOLO 模式开关以跳过非必要步骤，并加入耗时统计以对比流程优化效果。

### 执行内容
**流程与配置**
- `src/config.py`: 新增 `ttadk_yolo_default_enabled` 默认关闭
- `src/project/context.py`: ProjectContext 增加 `ttadk_yolo_enabled` 字段并持久化（加载缺省回退设置）
- `src/project/manager.py`: 创建项目时按设置初始化 yolo 状态

**卡片与交互**
- `src/card/builders/core.py`: TTADK 状态条增加 `YOLO: 开启/关闭`
- `src/card/builders/system.py` + `src/card/builder.py`: TTADK 工具/模型选择卡新增 YOLO 切换按钮（`toggle_ttadk_yolo`）
- `src/feishu/ws_client.py`: 动作路由新增 `toggle_ttadk_yolo`、`show_ttadk_menu` 强制进入选择菜单

**最小交互与 YOLO 行为**
- `src/feishu/handlers/system.py`: 
  - `/ttadk` 在已配置工具/模型时自动进入（show_ttadk_menu 仍强制出菜单）
  - 选择工具后自动挑选默认/已配置模型，减少二次确认
  - YOLO 模式下自动选工具/模型（必要时 fallback 为首个模型）
  - 自动路径静默切换模型，减少冗余提示

**耗时基线**
- `src/feishu/handlers/system.py`: 增加 `ttadk_flow_duration_ms` 计时记录（开始/进入模式）以便对比优化前后耗时

**测试补齐**
- `tests/test_handlers.py`: 自动进入、自动模型选择、YOLO fallback、耗时记录
- `tests/test_ws_client_patch.py`: TTADK 菜单/YOLO 动作路由
- `tests/test_card.py`: YOLO 按钮与状态条展示
- `tests/test_project.py`: yolo 持久化字段

### 验证记录
- `uv run pytest -x -q tests/test_handlers.py tests/test_ws_client_patch.py tests/test_card.py tests/test_project.py` → 314 passed, 1 warning

---

## TTADK 进入失败兜底与错误提示优化

### 任务描述
优化手机端进入 TTADK 模式的错误提示与失败兜底，避免加载过程中出现“错误卡片”或阻断提示，提供可恢复指引。

### 执行内容
**错误提示与兜底**
- `src/feishu/handlers/system.py`: 新增 `_reply_ttadk_load_hint`，TTADK 工具/模型加载失败与切换失败改为温和提示与重试引导
- `src/feishu/handlers/programming.py`: TTADK 启动超时/异常改为警告提示，不发送错误卡片
- `src/feishu/ws_client.py`: TTADK 卡片动作异常时改为柔性提示，避免错误弹窗

**测试补齐**
- `tests/test_handlers.py`: 增加 TTADK 工具/模型失败提示回归测试与 TTADK 启动超时提示测试

### 验证记录
- `uv run pytest -x -q tests/test_handlers.py tests/test_ws_client_patch.py` → 170 passed, 1 warning

---

## TTADK 软失败卡片与恢复入口

### 任务描述
为 TTADK 进入失败场景提供统一的软失败卡片与恢复入口，避免手机端出现硬错误提示。

### 执行内容
**卡片与入口统一**
- `src/card/builders/system.py`: 新增 `build_ttadk_soft_failure_card`，包含“重新进入TTADK”按钮
- `src/card/builder.py`: 增加对应门面方法

**入口失败提示统一**
- `src/feishu/handlers/system.py`: 入口/工具/模型失败改为软失败卡片
- `src/feishu/handlers/programming.py`: TTADK 启动超时/异常改为软失败卡片
- `src/feishu/ws_client.py`: TTADK 卡片动作异常改为软失败卡片

**测试补齐**
- `tests/test_card.py`: 新增软失败卡片构建测试
- `tests/test_ws_client_patch.py`: 新增卡片异常软失败测试
- `tests/test_handlers.py`: 更新 TTADK 失败提示断言

### 验证记录
- `uv run pytest -x -q tests/test_handlers.py tests/test_card.py tests/test_ws_client_patch.py` → 279 passed, 1 warning

---

## TTADK 软失败提示统一服务

### 任务描述
抽取统一的 TTADK 软失败提示模板与调用入口，保证 System/Programming/WS 的提示一致，并补齐可达性回归。

### 执行内容
**统一模板与入口**
- `src/card/builders/system.py`: 增加 `_format_ttadk_soft_failure_message` + `build_ttadk_soft_failure_card_for`
- `src/card/builder.py`: 增加统一入口 `build_ttadk_soft_failure_card_for`
- `src/feishu/handlers/system.py`: 入口/工具/模型失败改为统一 helper（并修复 model 失败分支 project_id 为空的问题）
- `src/feishu/handlers/programming.py`: TTADK 启动超时/异常改为统一 helper
- `src/feishu/ws_client.py`: TTADK 卡片动作异常改为统一 helper

**测试补齐**
- `tests/test_handlers.py`: 更新 TTADK 软失败文案与按钮断言
- `tests/test_card.py`: 软失败卡片构建测试改用 helper
- `tests/test_ws_client_patch.py`: 卡片异常软失败文案一致性验证

### 验证记录
- `uv run pytest -x -q tests/test_handlers.py tests/test_card.py tests/test_ws_client_patch.py` → 279 passed, 1 warning

---

## TTADK 入口成功判定测试

### 任务描述
明确手机端 TTADK 进入成功的最小 UI 要素，并以单测形式验证卡片包含状态条与入口按钮。

### 执行内容
- `tests/test_card.py`: 新增 TTADK 进入成功 UI 断言（含“TTADK 状态”与 `show_ttadk_menu` 入口）

### 验证记录
- `uv run pytest -x -q tests/test_card.py` → 109 passed, 1 warning
