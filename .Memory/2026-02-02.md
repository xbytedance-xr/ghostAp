# 2026-02-02 项目记录

## 表情类型无效 & Deep 卡片内容重复修复
### 任务描述
修复飞书 API 不支持的表情类型和 Deep 模式卡片内容重复。

### 执行内容
**表情类型**：更新 `src/feishu/emoji.py`，替换不支持的类型：
- SMART→FINGERHEART, CheckMark→DONE, CrossMark/ERROR→Skull, WRONGED→SOB
- SALUTE→THANKS, HIGHFIVE→FISTBUMP, CLAP→APPLAUSE
- StatusFlashOfInspiration→JIAYI, StatusReading→THINKING, BusyStatus→OneSec

**Deep 卡片重复**：
1. `build_deep_card` 中不传 title 给 `_build_content_element`（reporter 已包含标题）
2. 移除 `_with_ref` 函数，让 `send_message` 统一处理 ref_note

---

## 项目结构精简与可维护性提升
### 任务描述
合并小文件、统一会话模块目录结构。

### 执行内容
**Phase 1**: 合并 themes.py 到 shared.py — ProjectTheme/THEMES/get_theme() 移入 shared.py
**Phase 2**: 统一会话目录 — src/coco/session.py → src/session/coco.py, src/claude/session.py → src/session/claude.py
- 删除 5 个文件，净减少 3 个
- 附带修复 test_unified_context.py 时间精度问题（sleep 0.01→0.05s）
- 测试总数 744

---

## 项目级任务隔离与系统命令快速通道
### 任务描述
解决编码模式下系统命令被阻塞、不同项目任务无法并发的问题。

### 执行内容
**TaskSpec 增强**：
- 新增 `is_system_command` + `get_effective_queue_key()`
- 路由：系统命令→`{chat_id}:SYSTEM`，项目任务→`{chat_id}:{project_id}`

**TaskScheduler 改进**：
- `per_chat_concurrency` → `per_key_concurrency`
- 新增 `system_concurrency`（默认 10），系统命令独立快速通道

**ModeManager 项目级模式**：
- 支持 project_id 参数，每个项目独立编码模式
- 解析顺序：项目模式 > chat 模式 > SMART

**ws_client.py**：
- `_is_system_command_message()` + `_is_system_card_action()` 识别系统命令
- 系统命令用 TaskPriority.HIGH + is_system_command=True

### 技术要点
- 系统命令：/help, /帮助, /projects, /status, /coco_info, /claude_info, /deep_status, /deep_board
- 新增 13 个测试，总测试数 744

---

## 项目持久化原子写与损坏恢复
### 执行内容
- 跨进程文件锁 + 原子写入降低 projects.json 损坏风险
- 损坏备份：读取失败时备份为 `projects.json.corrupt.<ts>` 并继续启动
- 新增损坏恢复测试

---

## ws_client.py God Class 拆分重构
### 任务描述
将 ws_client.py（3444 行/104 方法）拆分为 Dispatcher + Handlers 架构。

### 执行内容
**新增 6 个 Handler**：
- HandlerContext（handler_context.py）— 依赖注入容器，16+ 共享依赖
- BaseHandler（handlers/base.py）— 公共基类
- ProgrammingModeHandler（handlers/programming.py）— Coco/Claude 共享模板方法
- DeepHandler（handlers/deep.py）— Deep Engine 全生命周期
- ProjectHandler（handlers/project.py）— 项目 CRUD
- SystemHandler（handlers/system.py）— 帮助/退出/Shell/目录
- DiagnosticsHandler（handlers/diagnostics.py）— 任务看板/Diff/追踪

**ws_client.py 改造**：
- 3444→~1170 行
- 保留 ~60 个向后兼容转发 stub
- 核心路由方法保留

### 技术要点
- 模板方法模式：ProgrammingModeHandler 提供共享基类，CocoModeHandler/ClaudeModeHandler 为薄配置子类
- 新增 85+16 个测试，总测试数 730

---

## Deep Engine 实时上下文调整优化
### 任务描述
解决任务 prompt 静态固化、失败重试盲目、无用户介入通道三个缺陷。

### 执行内容
**新增 ContextEntry + ExecutionContext**（models.py）：
- 支持 task_result/user_injection/deviation/adaptation 四种类型
- 线程安全（threading.Lock）、flag 机制避免重复触发

**新增 adapt_task_prompt()**（planner.py）：
- LLM 返回 JSON: {should_adapt, reason, adapted_prompt}
- 异常 fallback 到原始 prompt

**engine.py 循环改造**：
- 上下文感知 prompt 适配 → 执行 → 记录结果 → 智能失败 replan

**新增 /deep_update 命令**：
- IntentType.DEEP_UPDATE + 路由 + inject_context()

- 新增 35 个测试，总测试数 617

---

## 流式卡片更新修复 + Claude 会话闲置优化
### 任务描述
修复流式卡片更新失败（code=99992402）和 Claude 会话闲置恢复延迟。

### 执行内容
**卡片 API 修复**：
- `UpdateMessageRequest` → `PatchMessageRequest`（PUT→PATCH）
- 飞书卡片更新应使用 PATCH 端点

**Claude 会话闲置优化**：
- 新增 `_CLI_SESSION_MAX_IDLE = 1800`（30分钟）
- `_reset_stale_session()` 在 send_prompt 前检查闲置时间，超过阈值主动创建新会话

### 技术要点
- `PUT /messages/:id` 用于文本/富文本，`PATCH /messages/:id` 用于卡片
- 测试总数 582

---

## 编程模式命令拦截 + 即时反馈 + 卡片渲染优化
### 任务描述
修复编程模式中系统命令被错误发送给 AI、无即时反馈、卡片样式简陋。

### 执行内容
- `_is_interceptable_command()` — 判断系统命令（/help、/projects 等 9 种）
- `_handle_intercepted_command()` — 统一路由拦截命令
- 重构 `_handle_claude_response()` / `_handle_coco_response()` — 统一先创建流式卡片
- `text_size` 属性优化卡片渲染

- 新增 9 个测试，总测试数 580
