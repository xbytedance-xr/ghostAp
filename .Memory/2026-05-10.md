# 2026-05-10 项目记录

## 飞书编程卡片 v2 深层接线与 Backlog 清理
### 任务描述
继续完成 `docs/superpowers/plans/2026-05-10-card-redesign-v2.md` 中剩余核心能力，重点是切卡冻结态、累计时间、flow bridge、subagent 独立 streaming，并修复 card v2 slice review 产出的 B006-B013 缺口。

### 执行内容
- 将 v2 新增 metadata/API 接入真实 reducer、renderer、orchestrator、engine renderer 与 programming handler 路径：切卡时封存旧卡 final state，新卡继承续接信息与累计 elapsed，Deep/Loop/Spec split 统一传递 `bridge_phrase="续接："`。
- `continuation_seq` 作为主卡序号 SSOT，`card_sequence` 只作为展示值；主卡由 continuation 自动推导 `#N`，subagent 允许使用 dotted 序号（如 `1.a`）并保留 `parent_card_seq`。
- 引擎卡 header 增加强 v2 signal 判断，避免仅因 `tool_name` 存在就覆盖 Deep/Loop/Spec 原有 title/subtitle；subagent/续卡/冻结/working_dir/card_sequence 才进入 v2 header。
- `CardSession` 用注入 clock 初始化 `session_started_at`，并在 terminal/archived reducer 中通过 `_now` 计算冻结 elapsed，避免 wall-clock 与 monotonic 混用。
- 统一 task/tool 状态集合：task list 对 failed/cancelled 使用“已结束”语义并显示完成/失败计数；footer 当前工具只认 `active`；`render_task_list_panel(compact=...)` 明确保留 compact 兼容语义；LiveTicker 默认帧改为 🟢↔⚪。
- `ProgrammingCardSession` 与 orchestrator 接入 subagent metadata，agent/subagent 工具调用生成独立 card sequence 与 bridge phrase，支持并行子任务持续更新。
- 针对复审指出的 B006 残留死代码补齐真实生产 caller：ProgrammingCardSession 启停 `LiveTicker`；ACP 事件流写入 `ACPEventRenderer.snapshot_turns()` 并按 turn 边界切正文块；主卡通过 `build_subagent_dispatch_atom()` 渲染并行子任务汇总面板；Programming handler 通过 `CardSessionFactory.create_subagent()` 创建子代理卡。
- 引擎卡 header 首卡也以 `session_started_at` 作为 v2 signal，避免同一 Deep/Loop/Spec 运行中 #1 legacy、#2+ v2 的样式割裂；subagent footer 的 tool 标签改从 `子代理：X` 提取，不再使用任务描述；orchestrator 统计 subagent 数量时加锁读 `_sessions`。
- 根据 Backlog 清理规则，已从 `.Memory/Backlog.md` 删除 B006-B013，仅保留未解决 B001-B005。

### 技术要点
- 为保持历史测试和潜在内部调用兼容，`CardSession.on_card_split_completed` 在没有 bridge phrase 时仍以 `(reason, hint)` 调用旧二参 callback；有 bridge phrase 时才调用三参 callback。
- SpecStreamProcessor 的单元测试使用 `MagicMock` renderer，`_rotate_session()` 在生产 `CardMetadata` 实例上才应用 dataclass `replace()`，避免 mock metadata 触发 dataclass 类型错误，同时不影响真实续卡 bridge。
- 任务列表折叠尾部文案按桶语义选择：纯 completed 仍显示“已完成”，混入 failed/cancelled 时显示“已结束”，避免 B010 回归。
- LiveTicker frame 与 subagent summary 都通过 metadata diff 触发结构更新；ticker callback 只 dispatch 轻量事件，不在 timer 线程做 Feishu I/O。

### 提交记录
- `57f7dc5` feat(card): wire v2 split continuity
- 待提交：复审补齐 B006 残留生产接线与 header/subagent 修复。
- 验证：`uv run python -m pytest tests/test_card_session.py::TestCardSessionDispatch::test_card_split_emits_completed_callback_after_terminal_delivery tests/test_spec_renderer_callbacks.py::TestSpecStreamProcessorUnifiedCycleCard::test_error_closes_active_stream_blocks_before_failed tests/test_spec_stream_processor.py::TestRotateSession::test_rotate_session_calls_rotator_rotate tests/test_spec_stream_processor.py::TestRotateSession::test_rotate_session_uses_delivered_message_id tests/test_spec_stream_processor.py::TestRotateSession::test_rotate_session_falls_back_to_original_message_id tests/test_spec_stream_processor.py::TestOnCycleStart::test_updates_max_cycles tests/test_spec_stream_processor.py::TestOnCycleStart::test_dispatches_cycle_started tests/test_task_list_v2.py::test_task_list_downgrades_completed_and_pending_when_large -q` → `8 passed, 1 warning`
- 验证：`uv run python -m pytest tests/test_programming_card_session.py::TestProgrammingCardSession::test_start_and_finish_drive_live_ticker tests/test_programming_card_session.py::TestProgrammingCardSession::test_acp_text_after_tool_uses_new_turn_block tests/test_programming_card_session.py::TestProgrammingCardSession::test_parallel_agent_tasks_update_main_summary_panel tests/test_programming_card_session.py::TestProgrammingCardSession::test_agent_task_uses_card_session_factory_create_subagent_when_available tests/test_header_v2.py::test_engine_header_uses_v2_when_session_started_for_first_card -q` → 先红灯 `4 failed, 1 passed`，实现后复跑 `5 passed`
- 验证：`uv run python -m pytest tests/test_programming_card_session.py tests/test_header_v2.py tests/test_card_v2_foundation.py tests/test_live_ticker.py tests/test_footer_v2.py tests/test_render_task_list.py -q` → `68 passed`
- 验证：`uv run python -m pytest tests/ -v` → `6368 passed, 1 skipped, 141 warnings`

## 飞书编程卡片 v2 重设计首批实现
### 任务描述
基于已确认的 brainstorm、双屏 HTML mockup、`docs/superpowers/specs/2026-05-10-card-redesign-design.md` 和 `docs/superpowers/plans/2026-05-10-card-redesign-v2.md`，开始实现飞书编程卡片 v2。首批范围聚焦低耦合基础能力：metadata foundation、header、task list、tool panel 展开规则、footer hint。

### 执行内容
- `CardMetadata` 增加 `card_sequence`、`session_started_at`、`working_dir`、`is_subagent`、`parent_card_seq`、`final_state_for_freeze`、`frozen`、`frozen_total_elapsed` 等 v2 字段；`CardSession` 暴露对应只读属性；`CardEvent.card_split()` 增加可选 `bridge_phrase`。
- `src/card/render/header.py` 支持 v2 两行编程 header：第一行展示项目、工具、卡片序号、模型；第二行展示目录/父卡来源与运行时长；冻结卡展示 `已封存`、灰色 template 和 final elapsed。
- `src/card/render/task_list.py` 将 task list 改成三段常开：进行中 / 已完成 / 未处理；compact sticky 路径不再只显示当前任务；大任务量下折叠已完成尾部、未处理只显示前 5 个。
- `src/card/render/tools.py` 在渲染层强制只有 `status=active` 且 `is_latest_active=True` 的工具展开，避免 stale latest flag 导致已完成/失败工具展开。
- `src/card/render/footer.py` 增加 `render_now_tool_hint()`，footer 展示当前运行工具一句话 hint；subagent metadata 打开时展示 model/tool/parent badge。

### 技术要点
- 计划文档里 `CardSession` 数据模型路径与当前实现不一致：真实渲染持久契约在 `CardMetadata`/`CardState`，所以 v2 字段放入 `CardMetadata`，并通过 `CardSession` 属性透出，避免改动 session 构造链路。
- v2 header 仅在 metadata 带编程上下文时启用，保留普通静态卡片的 legacy header 行为，降低测试卡和非编程卡误伤。
- task list v2 与旧 compact 行为冲突，相关旧测试已改为断言三段常开，这是本次设计确认后的语义变化。
- 完整回归中 `tests/test_validate_mode.py::TestValidateNoTombstoneTrigger::test_validate_does_not_trigger_tombstone` 曾出现一次 subprocess 30s 偶发超时，单独复跑通过；最终阶段全量通过。

### 提交记录
- `826330c` feat(card): add v2 session metadata foundations
- `4c48579` feat(card): render v2 programming headers
- `d7f9a8e` feat(card): render three-group sticky task lists
- `e6a8634` feat(card): tighten tool panels and footer hints
- 验证：`uv run python -m pytest -x -q` → `6342 passed, 1 skipped, 141 warnings`

## 飞书编程卡片 v2 后续实现
### 任务描述
继续执行 `docs/superpowers/plans/2026-05-10-card-redesign-v2.md`，补齐首批实现后剩余的 v2 卡片渲染合同：turn snapshot、split 续接、subagent 独立卡、LiveTicker 基础能力，以及去掉旧的 activity summary 自动注入。

### 执行内容
- `src/card/render/footer.py` 提取 `render_subagent_badge()` 与 `build_footer_atoms()`，让 footer 当前工具 hint / subagent badge 的展示合同可复用、可测试。
- `src/acp/renderer.py` 新增 `TurnSnapshot` 与 `snapshot_turns()`：连续文本归入 reasoning，随后工具归入同一 turn，工具后新文本开启下一 turn；保持旧 markdown 输出不变。
- `CardMetadata` 增加 `bridge_phrase`；renderer 在首个 text/reasoning atom 注入续接短句；v2 header 对续卡展示本卡 elapsed 与累计 elapsed。
- `CardSessionFactory.create_subagent()` 生成继承父卡项目/目录/session_started_at 的独立 subagent session；subagent header 使用 orange template；新增 `subagent_dispatch` atom 与并行子任务摘要 panel。
- 新增 `src/card/render/live_ticker.py`，提供可注入 `TimerScheduler` 的轻量 `LiveTicker`，按 interval 产出状态帧，避免在 timer 线程做 I/O。
- 从 `render_card()` 和 `sticky_head` 移除自动派生的 `activity_summary` atom；编程卡正文回到 reasoning/tool/text 流，旧 helper 暂留作兼容入口但不再自动注入。

### 技术要点
- 计划后半段提到的部分文件名与当前实现不一致，实际落点按现有 `CardMetadata`、`CardSessionFactory`、`src/card/render/*`、`ACPEventRenderer` 架构完成。
- `activity_summary` 的旧测试语义与 v2 turn-based 设计冲突，已改为断言不再自动展示“已运行/已探索/已编辑”聚合文案，同时保留正文与工具面板。
- 当前 Codex sandbox 的外部权限额度用尽后，`uv run pytest` 无法访问用户目录 uv cache；改用仓库 `.venv/bin/python -m pytest` 做行为回归。两个测试内部直接调用 `uv run`，在 sandbox 中分别因 uv cache 权限和 macOS `system-configuration` 初始化 panic 受限，需在正常终端或恢复外部权限后复跑。

### 提交记录
- `e0efbaf` feat(card): expose footer v2 atom helpers
- `0f6f816` feat(acp): expose renderer turn snapshots
- `069ac69` feat(card): preserve split continuity in v2 render
- `98c7aa0` feat(card): add subagent session render contract
- 待提交：LiveTicker 基础能力、移除 activity_summary 自动注入、Memory 更新（当前 sandbox git index 写入可能需要外部权限）
- 验证：`.venv/bin/python -m pytest tests/test_live_ticker.py tests/test_timer_scheduler.py -v` → `13 passed, 3 warnings`
- 验证：`.venv/bin/python -m pytest tests/test_sticky_head.py tests/test_card_session.py::TestAtomRendererRegistry tests/test_card_renderer.py::TestUnifiedCardSections::test_programming_card_does_not_inject_activity_summary_panel tests/test_subagent_dispatch_render.py -v` → `10 passed`
- 验证：`.venv/bin/python -m pytest -x -q --ignore=tests/test_lint_banned_imports.py -k 'not test_validate_does_not_trigger_tombstone'` → `6354 passed, 1 skipped, 1 deselected, 141 warnings`
- 受限验证：`tests/test_lint_banned_imports.py::TestTID251BannedImports::test_banned_import_detected` 与 `tests/test_validate_mode.py::TestValidateNoTombstoneTrigger::test_validate_does_not_trigger_tombstone` 均在当前 sandbox 内部 `uv run` 子进程处失败；此前外部权限可用时完整 `uv run python -m pytest -x -q` 已多次通过。
