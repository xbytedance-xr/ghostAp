# 2026-05-07 项目记录

## 统一 Spec/Loop/Deep/Worktree 卡片到直接编程模式结构
### 任务描述
- 将 `spec`、`loop`、`wt`、`deep` 四种编程模式的卡片组织方式对齐到 Coco 直接编程模式：单轮迭代/循环独立成卡，单卡内部展示与直接编程保持一致，并在可行时透出原生工具调用。

### 执行内容
- 在 `src/feishu/renderers/base.py` 抽取 `_ACPStreamBridge` 与 `_dispatch_text_block()`，统一把 ACP 流转换为 reasoning/text/tool 等共享 `CardEvent`，避免各模式分别维护一套流式卡片逻辑。
- 为 `CardMetadata` 补充 `unit_id/unit_kind/unit_label`，并在 `src/card/state/reducers/_shared.py` 复用 `build_header()` 自动拼接轮次/工作树标签；`src/feishu/renderers/loop_renderer.py`、`src/feishu/renderers/_spec_stream_processor.py`、`src/feishu/renderers/worktree_renderer.py` 在迭代/循环/项目维度写入 scoped metadata。
- `src/feishu/renderers/loop_renderer.py`、`src/feishu/renderers/deep_renderer.py`、`src/feishu/renderers/_spec_stream_processor.py` 全部改走共享 stream bridge：切轮/切 phase/异常时先关闭活跃 text/reasoning block，再补发 phase/cycle/project 总结，确保展示顺序与直接编程模式一致。
- `src/card/state/reducers/worktree.py` 改为复用共享 header 生成逻辑，worktree 卡片标题与普通编程模式保持统一，仅切换 step subtitle；并发多 worktree 继续以多 session 并行呈现。
- `src/card/render/renderer.py` 增加按 status/body/appendix 重排 atom 的逻辑，让 phase、正文、工具面板的布局顺序稳定；相关回归测试同步更新。
- 补充回归测试：`tests/test_loop_renderer.py`、`tests/test_spec_renderer_callbacks.py`、`tests/test_worktree_renderer.py` 新增轮次 scoped 卡片、原生 tool event 透传、error 前自动关闭活跃 block 等覆盖。

### 验证
- 定向回归：`uv run python -m pytest tests/test_card_renderer.py tests/test_loop_renderer.py tests/test_spec_renderer_callbacks.py tests/test_worktree_renderer.py -q` → `67 passed`

## 修复卡片 Patch failed：`div` 元素不支持 `padding/background_style`
### 任务描述
- 修复投递层更新卡片时出现的解析失败：`parse card json err`，提示 `unknown property: padding/background_style`（路径 `ROOT -> body -> elements -> [..](tag: div)`），导致 `page_mutator` PATCH 失败并反复告警。

### 执行内容
- 修复 `src/card/render/renderer.py` 的 warning banner / phase panel：不再对 `div` 设置 `padding/background_style`，统一改用 `column_set` 生成带背景的横幅块。
- 修复 `src/card/render/worktree.py` 的失败单元提示：移除 `div` 上的 `padding/background_style`，改为 `column_set` 承载红色背景强调，避免 Schema 2.0 解析失败。
- 补充回归测试 `tests/test_card_renderer.py`：递归遍历渲染产物，断言任何 `tag=div` 节点都不包含 `padding/background_style`，覆盖 warning banner / phase / worktree failed units 三条路径。

### 验证
- 定向回归：`uv run python -m pytest tests/test_card_renderer.py -q` → `28 passed`

## 修复 `/wt` 命令识别与路由闭环
### 任务描述
- 修复 `/wt` 在现有 slash 命令体系中的触发失效问题，确保 worktree 命令识别、系统拦截、handler 路由、失败反馈与聊天锁反馈形成闭环。

### 执行内容
- 新增统一 slash 命令解析器 `src/feishu/slash_command_parser.py`（`SlashCommandParser` + `CommandMatch`）：大小写不敏感、支持任意空白（Space/Tab）分隔、`/wt` 归一化为 canonical `/worktree`，并以 `args/has_args` 结构化承载参数。
- `SystemHandler`、`WorktreeHandler`、`FeishuWSClient` 与 `ChatLockGate` 全链路接入统一解析，避免各处 `startswith()+切片` 漂移：
  - 系统拦截命令识别与路由统一用 parser（worktree 路由使用 `goal=m.args`）。
  - worktree prefix 命令识别由 parser 驱动，确保 `/wt\t...` 等输入不会被忽略。
  - chat lock gate 接收 `command_match`，并将 `has_args` 透传给 `ChatLockManager.should_block()`，锁定状态下“裸 `/wt` 放行、带 goal 阻断”的判定与路由保持一致。
- 更新帮助文案 `src/card/ui_text.py`：补充 `/wt`、`/wt <目标>`、`/worktree <目标>` 与“空格/Tab 分隔符”说明。
- 补齐测试：
  - `tests/test_worktree_command_routing.py` 增加 parser 与 worktree 路由覆盖（含大写/Tab 分隔）。
  - `tests/test_chat_lock.py` 增加锁定场景下的 parsed `/WT` 行为覆盖。
  - `tests/test_card_builders.py` 增加 help card 对 worktree 文案关键片段断言。

### 技术要点
- Slash 命令解析实现 SSOT：命令匹配/归一化/参数提取只在 parser 内发生，上层只消费 `CommandMatch`，减少“同一命令不同模块不同解析规则”的漂移风险。
- 兼容性：仍保留 `/wt` 用户入口，但内部统一 canonical `/worktree`，同时保持“无参数即进入选择流程、有参数即启动并行执行”的历史行为。

### 提交记录
- 未提交；工作区已完成代码与测试变更，定向回归：
  - `uv run python -m pytest tests/test_card_builders.py::test_help_card_mentions_worktree_wt_alias_and_separators -q` → `1 passed`
  - `uv run python -m pytest tests/test_worktree_command_routing.py tests/test_chat_lock.py tests/test_chat_lock_gate.py -q` → `149 passed`


## 重构：统一消费 `CommandMatch`（Slash 命令 SSOT）
### 背景
- 项目已引入统一解析器（`SlashCommandParser`/`CommandMatch`），但消费链路仍存在多处“手工解析/切片/兜底字段”并行：
  - `ChatLockManager.should_block()` 仍接受 `raw_text/has_args` 并在无结构化输入时推断参数。
  - `SystemHandler` 对 `/switch`、`/new`、`/close` 仍存在 `startswith()+切片` 的参数提取逻辑。
- 这会导致 SSOT 失效：同一条指令在不同模块的“是否有参数/参数内容”判定可能漂移，进而影响锁逻辑与路由一致性。

### 执行内容
- 解析变为 request-scoped：在 `FeishuWSClient` 清洗文本后只解析一次，得到 `command_match` 并沿链路透传。
- `ChatLockGate.check()` 仅接收 `command_match`（或 `None`），不再传 `command/raw_text`；阻断与 fail-close 路径统一基于同一份结构化信息。
- `ChatLockManager.should_block()` 改为 `command_match` 入参（`Protocol` 解耦 core 与 feishu 层），删除 `raw_text/has_args` 兜底推断逻辑：
  - 锁定场景下 “裸 `/worktree`（无参数）放行 / 带 goal 阻断” 由 `CommandMatch.has_args` 驱动，避免 `strip()` 推断漂移。
- `SystemHandler` 的系统命令参数消费统一改用 `CommandMatch.args`：
  - `/switch`、`/new`、`/close` 走专用 `_handle_*_args()`，不再通过文本切片提取参数。
- `WorktreeHandler` 增加 `handle_worktree_command_match()`：直接消费 `CommandMatch.args` 作为 goal，旧入口保留为薄兼容层。
- 测试同步迁移：`chat_lock`/`chat_lock_gate`/`ws_client_routing`/`worktree_command_routing` 全部以 `SlashCommandParser.parse()` 产出的 `CommandMatch` 作为输入构造，避免再写重复解析规则。

### 回归风险清单（重点关注）
- `command_match=None` 的非 slash 消息：链路需保持“正常不拦截、不误判 has_args”。
- 文本清洗顺序：必须在清洗后解析，避免 `\t/\n` 等空白字符导致 args 提取不一致。
- 锁逻辑一致性：拦截卡提示与实际阻断判定需基于同一份 `CommandMatch`，避免“提示放行但实际阻断”的错配。
- handler 兼容：卡片 action 或旧调用点仍能走到兼容层（若存在），且不会再次触发二次解析。

### 验证
- 全量测试：`5645 passed, 1 skipped, 122 warnings`（约 195s）。
