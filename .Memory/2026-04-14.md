# 2026-04-14 项目记录

## /help 卡片扁平化重构（全展开 + 手机快捷入口）

### 任务描述
原 `/help` 卡片使用 4 个 tab（编程模式/Deep 任务/项目管理/更多…）切换显示，每次只能看到一个分类。用户要求：一次展开所有命令；如果能提供手机端方便的交互更好。

### 执行内容
重写 `src/card/builders/system.py::SystemBuilder._build_help_card_cached`：

1. **移除 tab 切换** — `category` 参数保留（向后兼容 `handle_help_category` 调用方），但不再驱动分段，每次渲染全部内容。
2. **新增快捷入口按钮行**（手机优先点按，不需输入斜杠命令）：`🧠 Deep 任务 / 🎮 TTADK / 🧩 ACP / 📊 状态 / 🔄 切换项目 / ➕ 新建项目`。所有 action 均复用已在 `ws_client.py` 注册的 callback（`enter_deep_prompt`、`show_ttadk_menu`、`show_acp_menu`、`show_status`、`switch_project`、`new_project_prompt`）。
3. **全部分类内联展开**，6 个 section 之间用 `hr` 分隔：编程模式切换 / Deep Engine / Loop Engine / Spec Engine / 项目管理 / TTADK 管理。底部保留使用提示，新增「手机端优先点按快捷入口」引导。
4. **保持 schema 2.0 + `lru_cache`**，无需改 handler/ws_client，category 参数忽略（通过 `del category` 声明）。

### 技术要点
- 手机端交互走 callback button + responsive layout（>2 按钮自动垂直栈，见 `src/card/shared.py::build_responsive_layout`），无需引入 `collapsible_panel`（兼容性风险）。
- 扁平化后 `_build_help_card_cached` 仍是幂等缓存函数（primitives 参数），`category` 任意值命中同一缓存。
- 测试：`test_help_card_structure`（断言 header/body/column_set/编程模式 markdown）+ `test_handle_help_category_*`（handler patch/reply 分支）全部通过，共 5 cases；附带跑 `test_handlers.py + test_renderer_throttling.py` 共 123 passed。

### 提交记录
（本次在 `ttadk` 分支，尚未单独提交，合并至后续 commit）

---

## Deep Agent 完成卡片无内容修复

### 任务描述
Deep Agent 执行完成后，卡片"执行输出"区域几乎为空，进度条显示 0%，只有 `🔧 grep ✅` 一条工具调用行，无实质信息。

### 根因分析
1. **0% 进度条**：无 `PLAN_UPDATE` 事件时 `total_steps=0`，`make_progress_bar(0,0)="[░░░░░░░░░░] 0%"`，完成态仍显示 0%，误导用户。
2. **执行输出近空**：`engine.get_rendered_content()` 依赖引擎 renderer 的 `_text_chunks`；工具调用无 title 时不向 `_text_chunks` 写行，纯工具调用执行（无文本输出）时 rendered_content 几乎为空。
3. **工具调用统计粗糙**：`format_summary()` 只输出 `"🔧 工具调用: 95 次"`，没有 kind 拆分，信息量低。

### 修复内容
- **`src/feishu/renderers/deep_renderer.py`** `on_project_done`：
  - 改用本地 closure `renderer.get_final_content()`（与流式更新一致），引擎 renderer 作兜底。
  - 当 `rendered_content` 为空时补充提示"（Agent 未产生文本输出，仅执行工具调用）"，避免区域空白。
  - 完成态且 `total_steps=0` 时不传 `progress_bar`，消除误导性的 0% 进度条。
- **`src/deep_engine/progress.py`** `format_summary()`：
  - 按 kind 统计工具调用次数，输出如 `🔧 工具调用: 95 次 (search: 90 · execute: 5)`。
- **`tests/test_deep_renderer.py`**：为 mock 补充 `engine.progress.total_steps = 0`，适配新的 `> 0` 判断。

### 技术要点
- 78 tests pass（`test_deep_renderer / test_deep_engine / test_deep_reporter`）。

## /exit 在编程模式中误报"不在模式中"修复

### 任务描述
用户确认已在编程模式中，但输入 `/exit` 仍收到 `⚠️ 当前不在 Claude 模式中` 警告。

### 根因分析
`exit_mode` 中的成功路径条件：
- `has_session`：ACP 会话存在并成功关闭
- `is_pending_slot`：仅在 `thread_programming_enabled=True` 时有效

当用户进入编程模式但**尚未发送任何消息**（lazy init，ACP 会话未创建），或会话已过期时：
- mode_manager 已标记为对应模式 ✓
- `end_session` 返回 None → `has_session=False`
- `thread_programming_enabled=False` → `is_pending_slot=False`
- 两者均 False → 误显示"不在模式中"警告

### 修复方案
在 `exit_to_smart` 重置模式之前捕获 `was_in_this_mode`，新增 `is_mode_only_exit` 分支（非 thread 模式、无 session、但确实在该模式中），三个条件任一满足均走正常退出路径，显示 `mode_exit_pending_msg`。

### 技术要点
- `_is_in_this_mode` 必须在 `exit_to_smart` 前调用，之后状态已被重置
- `is_mode_only_exit` 用 `mode_exit_pending_msg`（无会话可保存），避免显示"会话已保存"

### 提交记录
- `c806030` fix(exit): 修复在编程模式中 /exit 误报"不在模式中"的警告
