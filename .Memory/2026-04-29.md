# 2026-04-29 项目记录

## /wt TTADK 独立入口与模型来源解耦
### 任务描述
修复 `/wt` 工具选择中 TTADK 与原生 ACP 工具混排的问题，让 TTADK 作为单独入口出现，并将 TTADK 的工具/模型选择与原生 Coco/Aiden/Codex/Gemini 的模型来源彻底隔离。

### 执行内容
1. 调整 `src/worktree_engine/tool_discovery.py`
   - 主工具列表不再平铺 TTADK 内部工具
   - 新增 `get_ttadk_tools()`，主列表仅注入单个 `TTADK` 聚合入口
2. 调整 `src/feishu/handlers/worktree.py`
   - 新增 `_get_ttadk_worktree_tools()` 包装方法
   - `handle_worktree_select_tool()` 识别 `provider=ttadk && tool_name=ttadk` 时，跳转到 TTADK 子工具卡，而不是直接走模型选择/默认加入
3. 调整卡片层
   - `src/card/builders/worktree.py` 新增 `build_worktree_ttadk_tool_select_card()`
   - `src/card/builder.py` 暴露对应 facade 方法
   - `src/card/styles.py` 新增 TTADK 子工具卡标题与提示文案
4. 补充测试
   - `tests/test_worktree_tool_discovery.py`：覆盖“主列表只有 TTADK 聚合入口”和“TTADK 子工具列表仍可获取”
   - `tests/test_worktree_selection_flow.py`：覆盖“顶层仅展示 TTADK 入口”和“点击 TTADK 后进入 TTADK 子工具卡”
5. 落盘实现计划
   - 新增 `docs/superpowers/plans/2026-04-29-worktree-ttadk-entry.md`

### 技术要点
- `/wt` 现在形成两条明确路径：原生工具直接进入原生模型选择，TTADK 先选 TTADK 子工具，再选该子工具模型
- 执行层仍只接收具体的 `(provider, tool_name, model_name)`，因此无需修改 worktree 调度/执行逻辑
- 通过聚合入口避免 `coco` 同时以“原生 Coco”和“TTADK coco”在同一层混淆出现

### 测试结果
- `uv run python -m pytest tests/test_worktree_tool_discovery.py tests/test_worktree_selection_flow.py -q` → 17 passed
- `uv run python -m pytest tests/test_worktree_tool_discovery.py tests/test_worktree_selection_flow.py tests/test_worktree_card_flow.py tests/test_worktree_command_routing.py -q` → 46 passed

## Phase 3 完成：SystemHandler God Class Mixin 提取
### 任务描述
将 `src/feishu/handlers/system.py`（1681 行）的 God Class 通过 Mixin 模式拆分为 3 个文件。

### 执行内容
1. 创建 `src/feishu/handlers/lock_commands.py`（LockCommandsMixin，9 个方法，~280 行）
   - `resolve_lock_message`（静态方法）、`_handle_lock_command`、`_reply_lock_permission_error`、`_check_confirm_card_expiry`
   - `handle_confirm_lock`、`handle_cancel_lock`、`handle_force_release_repo_lock`、`handle_confirm_force_release`、`handle_cancel_force_release`
2. 创建 `src/feishu/handlers/ttadk_commands.py`（TTADKCommandsMixin，17 个方法，~400 行）
   - TTADK 流程方法（refresh/resolve/pick/mark/report）+ 卡片交互处理方法
3. 重写 `src/feishu/handlers/system.py`：1681 → 814 行（-51%）
   - 类声明改为 `class SystemHandler(LockCommandsMixin, TTADKCommandsMixin, BaseHandler)`
   - 清理 10 个无用导入
4. 更新测试 monkeypatch 路径：`tests/test_handlers.py`（11 处）、`tests/test_empty_error_guard.py`（3 处）

### 技术要点
- Python MRO 保证 Mixin → BaseHandler 的方法解析顺序
- TTADK 状态字段保留在 SystemHandler.__init__ 中，Mixin 通过 self 访问
- 大量方法删除操作使用 Task agent 一次性重写比逐段 Edit 更可靠

### 测试结果
- 3857 passed, 0 failed ✅

### Phase 3 God Class 拆分总结
| 文件 | 原行数 | 拆分后 | 缩减 |
|------|--------|--------|------|
| agent_session.py | 2258 | 7 个子模块 | 包拆分 |
| ttadk/manager.py | 1917 | 1507 | -21% |
| ws_client.py | 2241 | 2090 | -7% |
| system.py | 1681 | 814 | -51% |

## Phase 5+6 完成：代码质量优化 + P2 清理
### 任务描述
Phase 5 代码质量（魔法数字/长函数/UI_TEXT）+ Phase 6 P2 清理（测试 sleep 替换）。

### 执行内容
**Phase 5.1 — 魔法数字提取**
- `config.py` 新增 `message_cache_cleanup_interval`、`card_action_dedup_cleanup_interval` 2 个字段
- `registry_setup.py` 6 处硬编码值（MessageCache TTL/max_size/cleanup、system_concurrency、rate_limiter、circuit_breaker）全部替换为 Settings 字段读取

**Phase 5.2 — 长函数分解**
- `dispatcher.py::execute_single_task` 从 ~250 行 if-elif 链分解为：
  - ~65 行路由主方法
  - 8 个聚焦辅助方法（`_handle_no_task`/`_handle_enter_coco`/`_handle_mode_message`/`_handle_ttadk_message`/`_dispatch_project`/`_handle_deep_status_or_stop`/`_handle_engine_guide`/`_dispatch_shell`）
  - 6 个类级别字典分发表（`_MODE_ENTER_MAP`/`_MODE_EXIT_MAP`/`_MODE_MESSAGE_MAP`/`_ENGINE_ENTER_MAP`/`_SIMPLE_ENGINE_DISPATCH`/`_ENGINE_GUIDE_MAP`/`_PROJECT_INTENTS`）

**Phase 5.3 — UI_TEXT 统一**
- `styles.py` 新增 37 个 UI_TEXT key（覆盖 dispatcher/programming 核心用户消息）
- `dispatcher.py` 迁移 11 处硬编码中文（_ENGINE_GUIDE_MAP/步骤反馈/意图未知/项目关闭）
- `programming.py` 迁移 32 处硬编码中文（banner/卡片标题/模型切换/执行结果/会话恢复）

**Phase 6 — 测试 sleep 替换**
- `test_handlers.py` 心跳测试：0.8s→0.4s + 3 处 0.1s 固定 sleep→monotonic 轮询（2s deadline）

### 技术要点
- `registry_setup.py` 中 Settings 实例 `s` 提前获取（原在 `create_scheduler` 内部获取），避免重复调用
- 字典分发表使用 `getattr(self.client, f"_enter_{mode}_mode")` 动态路由，消除重复的 per-mode 分支
- `_ENGINE_GUIDE_MAP` 值为 `(method_name, hint_text)` 元组，用 UI_TEXT 引用替换硬编码
- 测试轮询使用 `time.monotonic()` + deadline 模式，避免 CI 负载导致的时序抖动

### 测试结果
- 3857 passed, 0 failed ✅

### 修改文件清单
| 文件 | 变更 |
|------|------|
| src/config.py | +2 字段 |
| src/feishu/registry_setup.py | 6 处硬编码→Settings |
| src/feishu/dispatcher.py | execute_single_task 分解 + 11 处 UI_TEXT |
| src/feishu/handlers/programming.py | 32 处 UI_TEXT |
| src/card/styles.py | +37 个 UI_TEXT key |
| tests/test_handlers.py | 4 处 sleep→polling |
