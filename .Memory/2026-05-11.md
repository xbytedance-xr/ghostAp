# 2026-05-11 项目记录

## Refactoring Analysis 审计问题复核与最小修复

### Refactoring Analysis 1–28 最终验收总览

> 实际验收文件路径为 `docs/2026-05-11-refactoring-analysis.md`，以下均以该文件为准。
> 本文件内最终总览、状态矩阵与验证记录共同构成本次任务的问题矩阵入口；2026-05-12 记录是执行验证日志。历史背景和执行过程见本矩阵后的附录章节。

| 闭环分类 | 数量 | 最终结论 |
|---|---:|---|
| 已修复 | 21 | 原始审计结论成立且用户侧问题已通过代码、测试或结构调整闭环 |
| 兼容闭环 | 5 | 原始审计结论成立；旧入口处于兼容期或长期公开 API 保留，新入口/迁移方向已明确，用户侧已闭环 |
| 无需修复 / 已由其他改动解决 | 2 | 当前代码和验证已证明无需在本轮新增行为改动 |

### Refactoring Analysis 1–28 最终状态矩阵

> “状态”仅表示原始审计结论是否成立，枚举只使用：`存在` / `不存在` / `已被其他改动解决`；“处理方式”和“用户可验证结果”才表示当前是否已闭环。
> 对“状态=存在”但采用兼容期保留、长期公开 API 保留或新入口替代的条目，均以“兼容闭环”表达用户侧最终结果，不表示问题仍未处理。

| # | 问题摘要 | 状态 | 处理方式 | 涉及范围 | 验证依据 | 用户可验证结果 |
|---|---|---|---|---|---|---|
| 1 | FeishuWSClient God Class | 存在 | 已修复：已拆出 WS 生命周期、事件路由、卡片动作、资源管理协作模块，主类保留委托入口 | `src/feishu/ws_client.py`, `src/feishu/ws_lifecycle.py`, `src/feishu/ws_event_router.py`, `src/feishu/ws_card_action_handler.py`, `src/feishu/ws_resource_manager.py` | `tests/test_ws_lifecycle.py`, `tests/test_ws_event_router.py`, `tests/test_ws_card_action_handler.py`, `tests/test_ws_resource_manager.py` | 用户收发消息、点击卡片和断线重连仍按原入口工作，资源清理异常不会影响正常消息路由 |
| 2 | build_startup_diagnostics God Function | 存在 | 已修复：已拆为诊断 builder、启动工具函数和容错采集边界，公开函数保持兼容 | `src/acp/sync_adapter.py`, `src/acp/startup_utils.py`, `src/acp/diagnostics.py` | `tests/test_acp_startup_utils.py`, `tests/test_acp_sync_adapter.py` | 启动失败时会看到稳定的诊断摘要，配置缺失或异常字段不会让失败说明再次崩溃 |
| 3 | coordinate_ttadk_startup God Function | 存在 | 已修复：已将 TTADK 启动策略、状态和结果对象分层，回退链路由结果类型承载 | `src/ttadk/startup.py`, `src/ttadk/model_fetching/results.py` | `tests/test_ttadk.py`, `tests/test_ttadk_models.py` | TTADK 启动失败时会显示明确失败或回退结果，用户无需猜测当前处于探测、回退还是冷却阶段 |
| 4 | _start_session_inner God Function | 存在 | 已修复：已将后端选择、cwd 归一化、TTADK 预检、ACP retry、诊断构造、telemetry 失败上报下沉到启动协作者 | `src/acp/manager.py`, `src/acp/startup_utils.py`, `src/acp/diagnostics.py` | `tests/test_acp_startup_utils.py`, `tests/test_acp_manager_consistency.py`, `tests/test_ttadk_acp_isolation.py` | 点击进入会话后，可恢复失败会重新尝试，致命错误会明确失败，不会把失败显示成启动成功 |
| 5 | Dispatcher 与 Client 强耦合 | 存在 | 已修复：已引入 DispatchContext 与动作分发适配层，减少 dispatcher 对 client 私有成员的直接依赖 | `src/feishu/dispatch_context.py`, `src/feishu/action_registry.py`, `src/feishu/dispatcher.py` | `tests/test_dispatch_context.py`, `tests/test_action_dispatch_mapping.py`, `tests/test_feishu_dispatcher.py` | 卡片按钮和命令分发能找到正确项目与处理器，路由错误会显示明确失败而不是点击后无响应 |
| 6 | 宽泛 except Exception 泛滥 | 存在 | 已修复：已治理 ACP startup、dispatcher、WS message、card action、startup 关键路径的异常分类和源码级 guard | `src/acp/manager.py`, `src/feishu/dispatcher.py`, `src/feishu/ws_client.py`, `src/feishu/ws_event_router.py`, `src/feishu/ws_card_action_handler.py`, `src/feishu/ws_lifecycle.py` | `tests/test_import_guards.py`, `tests/test_error_formatting.py`, `tests/test_system_interaction.py` | 可恢复错误会给出降级提示，致命错误会明确失败，不会被静默吞掉或误报为成功 |
| 7 | 单例实现不一致 | 已被其他改动解决 | 无需修复：已有 reset 和 injection 契约统一测试隔离语义，本轮无需新增代码改动 | `src/ttadk`, `src/chat_lock.py`, `src/repo_lock.py` | `tests/test_singleton_reset.py` | 多次切换工具或重复运行任务时，旧状态不会意外污染下一次会话 |
| 8 | 模型解析逻辑分散 | 存在 | 已修复：已集中到 ModelResolver，并保留原入口兼容调用 | `src/ttadk/model_resolver.py`, `src/ttadk/models.py` | `tests/test_ttadk_models.py`, `tests/test_ttadk.py` | 用户选择同一个模型时，不同入口会解析到一致结果，不会出现名称漂移 |
| 9 | sync_adapter.py 职责混杂 | 存在 | 已修复：已将启动工具迁移到 startup_utils，sync adapter 聚焦同步会话桥接并保留必要兼容入口 | `src/acp/startup_utils.py`, `src/acp/sync_adapter.py` | `tests/test_acp_startup_utils.py`, `tests/test_acp_sync_adapter.py` | 同步会话启动和发送消息仍可使用，启动诊断逻辑变更不会影响普通消息发送 |
| 10 | ttadk/models.py 膨胀 | 存在 | 已修复：已将模型解析和 fetch 结果类型迁移到专用模块，models 保留数据模型边界 | `src/ttadk/models.py`, `src/ttadk/model_resolver.py`, `src/ttadk/model_fetching/results.py` | `tests/test_ttadk_models.py`, `tests/test_ttadk.py` | 模型列表、模型解析和拉取结果各自稳定，用户看到的模型名称和可用状态保持一致 |
| 11 | CardBuilder 纯代理外观 | 存在 | 兼容闭环：公开入口作为长期兼容路径保留，新代码优先使用具体 builder 或 CardSession pipeline | `src/card/builder.py` | `tests/test_card_builders.py`, `tests/test_import_guards.py` | 用户侧已闭环：旧调用仍可继续生成卡片，新调用有清晰迁移目标，不会因立即删除入口导致集成方报错 |
| 12 | Handler 间重复模式 | 存在 | 已修复：已将冲突卡、callback、状态展示等共享逻辑下沉到 BaseEngineHandler | `src/feishu/handlers/engine_base.py`, `src/feishu/handlers/spec.py`, `src/feishu/handlers/deep.py` | `tests/test_handlers.py`, `tests/test_system_interaction.py` | Deep 和 Spec 遇到锁冲突或状态查询时展示一致，用户不需要理解不同处理器的差异 |
| 13 | diagnostics 魔法数字硬编码 | 存在 | 已修复：已收敛为 diagnostics 默认常量，跨 normalize、summary、sync adapter 共享 | `src/acp/diagnostics.py`, `src/acp/sync_adapter.py` | `tests/test_acp_sync_adapter.py`, `tests/test_acp_startup_utils.py` | 诊断内容的截断长度在不同入口保持一致，长错误不会一处过短一处过长 |
| 14 | 参数列表过长 | 存在 | 已修复：已用 DispatchContext 和渲染上下文对象承载项目、消息、命令等请求信息 | `src/feishu/dispatch_context.py`, `src/feishu/dispatcher.py`, `src/feishu/renderers/base.py` | `tests/test_dispatch_context.py`, `tests/test_base_renderer_card_split.py` | 用户在项目群里执行命令时，项目和消息上下文不会因信息遗漏而错路由 |
| 15 | 意图识别巨型 if-elif | 存在 | 已修复：已将意图匹配规则注册化，原有命令意图通过注册入口复用 | `src/agent/intent_recognizer.py` | `tests/test_intent.py` | 用户输入 `/wt`、`/spec`、自由文本等命令后能被稳定识别，识别失败时会回到明确的默认处理 |
| 16 | Renderer 不对称组合 | 存在 | 已修复：已统一 Deep 和 Spec renderer 的组合策略、卡片拆分和回调契约 | `src/feishu/renderers/base.py`, `src/feishu/renderers/deep_renderer.py`, `src/feishu/renderers/spec_renderer.py` | `tests/test_base_renderer_card_split.py`, `tests/test_card_pipeline_integration.py` | Deep 和 Spec 长输出都会按同一规则续卡，用户不会在不同模式下看到明显不一致的切卡行为 |
| 17 | SystemHandler 二级 God Class | 存在 | 已修复：已按系统命令分区并补充 help、status、工具选择、锁相关路径护栏 | `src/feishu/handlers/system.py` | `tests/test_system_interaction.py`, `tests/test_handlers.py` | `/help`、状态查看、工具选择和锁相关命令仍可正常使用，某个命令调整不会牵连全部系统命令 |
| 18 | HandlerContext 服务定位器 | 存在 | 已修复：已将 handler 依赖访问协议化，保留必要兼容入口以兼容现有构造路径 | `src/feishu/handler_context.py`, `src/feishu/handlers/base.py` | `tests/test_handlers.py`, `tests/test_feishu_dispatcher.py` | 处理器能拿到所需服务，缺失依赖时会更明确地失败，不会表现为随机的运行时错误 |
| 19 | Router FORWARDING_MAP 类型安全不足 | 存在 | 已修复：已增加转发表结构化校验和 action 到 handler 的目标存在性检查 | `src/feishu/router.py`, `src/feishu/action_registry.py` | `tests/test_action_dispatch_mapping.py` | 卡片按钮指向不存在处理器时会在验证阶段暴露，不会等到用户点击后才无响应 |
| 20 | type: ignore 集群 | 存在 | 已修复：已清理关键路径的类型绕过或限定到兼容边界，并用导入守卫防止回潮 | `src/acp`, `src/spec_engine`, `src/ttadk` | `tests/test_import_guards.py` | 接口不匹配时会在验证阶段明确失败，不会在用户操作时表现为静默异常或无响应 |
| 21 | 废弃代码待清理 | 存在 | 兼容闭环：`CardEvent.worktree_*` 仅保留旧调用兼容，生产路径迁移到 worktree 事件入口，删除窗口为 2026-06-01 | `src/card/events/factories.py`, `src/card/events/__init__.py` | `tests/test_card_events.py` | 用户侧已闭环：旧调用暂时仍可用并收到迁移提示，生产路径不再依赖该旧入口，删除窗口已明确 |
| 22 | TYPE_CHECKING 守卫偏多 | 已被其他改动解决 | 无需修复：已复核边界并用验证锁定导入方向，本轮无需额外改动 | `src/card`, `src/feishu`, `src/acp` | `tests/test_import_guards.py` | 启动和导入模块时不会新增循环依赖，普通用户不会遇到因导入顺序导致的启动失败 |
| 23 | utils 领域工具错放 | 存在 | 兼容闭环：已新增 `src.spec_engine.utils` 与 `src.ttadk.wrapper` 领域入口，旧 utils 入口只保留兼容 | `src/spec_engine/utils.py`, `src/ttadk/wrapper.py`, `src/utils/spec_utils.py`, `src/utils/ttadk_wrapper.py` | `tests/test_import_guards.py` | 用户侧已闭环：新旧入口均可用，新代码有领域化入口，旧集成不会因为迁移期直接中断 |
| 24 | ui_text.py 单体注册表 | 存在 | 兼容闭环：已新增领域 UI text 视图，旧 UI_TEXT 聚合和 styles 入口保留迁移提示，长期公开 API 不计划删除 | `src/card/ui_text.py`, `src/card/ui_text_domains.py`, `src/card/styles.py` | `tests/test_ui_text_consistency.py`, `tests/test_import_guards.py` | 用户侧已闭环：用户看到的卡片文案保持一致，切换页面或点击按钮时不会出现缺字、错字或丢失提示 |
| 25 | builders/system.py 选择卡重复 | 存在 | 已修复：已抽选择卡 helper，并锁定工具、模型、组合选择卡的移动端长文本截断 | `src/card/builders/system.py`, `ux/card_preview.html` | `tests/test_card_builders.py`, `tests/test_ui_text_consistency.py` | 工具或模型名称很长时，移动端选择卡仍能显示关键内容，按钮不会被长文本挤出 |
| 26 | 三种错误展示路径不一致 | 存在 | 已修复：已统一错误摘要、错误场景、详细信息、查看详情、重试契约，并区分可恢复、降级、致命严重程度 | `src/card/builders/system.py`, `src/card/state/reducers/lifecycle.py`, `ux/card_preview.html` | `tests/test_error_formatting.py`, `tests/test_card_reducers.py`, `tests/test_card_builders.py` | 错误卡会清楚说明发生了什么，能继续或重试时显示主操作，致命错误不会伪装成成功 |
| 27 | card/styles.py 通配符 re-export | 存在 | 兼容闭环：wildcard 已改为显式导出，`styles.UI_TEXT` 仅作为迁移入口保留并提示迁移，删除窗口为 2026-06-01 | `src/card/styles.py` | `tests/test_import_guards.py`, `tests/test_ui_text_consistency.py` | 用户侧已闭环：旧入口仍可运行并收到迁移提示，新功能不会因为命名冲突影响用户看到的卡片内容 |
| 28 | model_fetcher.py 多类混合 | 存在 | 已修复：已将 fetch diagnostics、result、run result 迁移到 `ttadk.model_fetching.results`，fetcher 聚焦拉取流程 | `src/ttadk/model_fetcher.py`, `src/ttadk/model_fetching/results.py` | `tests/test_ttadk.py`, `tests/test_ttadk_models.py` | 模型拉取失败、成功和诊断结果会以稳定结构展示，用户看到的模型可用性更一致 |

### 验证
本文件内最终矩阵与本节验证记录共同构成本次任务的问题矩阵入口；2026-05-12 记录是执行验证日志。历史中间复跑结果保留在 2026-05-12 附录中，均不作为最终验收依据。

唯一最终验证命令与结果：
```bash
uv run python -m pytest tests/ -q
# 6333 passed, 49 warnings in 125.78s（warnings 均为既有 DeprecationWarning / pytest mark 提示；未遇到环境限制）
```

最终结论：本轮审查追加建议已在当前文件闭环；`.Memory/2026-05-11.md` 顶部矩阵和本节验证记录是问题矩阵入口，最终完整回归结果为 `6333 passed, 49 warnings`。2026-05-12 是执行验证日志，不作为另一套验收替代入口。

### 任务描述
用户要求基于 `docs/2026-05-11-refactoring-analysis.md` 逐项识别文档中记录的问题，验证哪些仍存在于当前代码库，并在“最小必要变更、不扩大范围、不引入架构迁移”的约束下修复确认仍存在的问题，最后执行静态复核与完整回归验证。

### 执行内容（附录）
- 已读取实际验收文件路径 `docs/2026-05-11-refactoring-analysis.md`，按 28 个问题逐项建立状态矩阵。
- 本轮完成可安全落地的修复：
  1. `src/card/events/factories.py` 恢复 `CardEvent.worktree_*` 兼容入口，保留至 2026-06-01 后删除，并新增 `DeprecationWarning` 回归测试；生产路径已迁移到 `src.card.events.worktree`。
  2. `src/acp/diagnostics.py` / `src/acp/sync_adapter.py` 将 diagnostics 默认截断数字收敛为 `DEFAULT_DIAGNOSTICS_ARGS_LIMIT=600`、`DEFAULT_DIAGNOSTICS_SNIPPET_LIMIT=240`、`DEFAULT_DIAGNOSTICS_TOTAL_LIMIT=2000`，并补齐配置缺失/非法/负数/0/显式值边界测试。
  3. `build_startup_diagnostics` 最终拆解为 `_resolve_startup_snippet_limit`、`_initial_startup_diagnostics` 等稳定入口，并在 diagnostics 模块保留 `safe_extract()` 作为诊断采集的明确容错边界。
  4. `src/card/builders/system.py` 抽取选择卡复用逻辑，补齐 TTADK tool/model/combined 与 ACP tool/model 字段级断言，并为长名称/长描述增加移动端安全截断。
  5. `src/card/state/reducers/lifecycle.py` 与 `SystemBuilder.build_error_card()` 收敛错误展示结构，统一“错误摘要 / 错误场景 / 详细信息 / 查看详情 / 重试”视觉契约。
  6. `src/card/styles.py` wildcard re-export 已改为显式导出，保留 `UI_TEXT` 兼容入口和弃用告警。
- 最终复核后统一口径：可证明闭环的项在“处理方式”首句标为“已修复”；保留公开兼容入口的项标为“保留兼容”；无需新增改动的项标为“无需修复”。
- 静态搜索确认 `src/` 中无剩余 `CardEvent.worktree_*` 生产调用；`src/card/styles.py` 无剩余 wildcard 导出；架构约束测试确认未新增 card pipeline 反向依赖。

### 技术要点（附录）
- 兼容窗口内不提前删除公开废弃 API：`CardEvent.worktree_*` 只作为外部兼容入口存在，内部生产代码不再依赖。
- Diagnostics 默认值成为 SSOT，跨 normalize / summary / sync adapter 共享，避免后续配置默认值漂移。
- 选择卡复用逻辑抽取后用字段级测试锁定 `initial_option`、`project_id`、`tool_name`、刷新按钮和长文本截断，防止 Feishu Card JSON 回归。
- 大型架构债务以真实最终状态表达：可证明闭环的项才标记为“已修复”；ACP startup 编排通过启动协作者拆分和异常分类测试完成收口。
- 宽泛异常捕获以关键路径分类治理完成本轮收口：ACP startup、dispatcher、WS message/card/startup 路径均有分类语义和源码级 guard。
- 移动端等效视觉验收记录在 `ux/card_preview.html`：360px 宽度下覆盖工具/模型选择卡长标题、长描述、按钮区，以及错误卡摘要、场景、详情、查看详情/重试按钮和可恢复/降级/致命严重程度层级；该 HTML 为等效验收材料，仍建议后续真实移动端飞书抽样复核。

### 提交记录
- 待提交。

---

## 修复 Spec/Coco 辅助 ACP prompt 60s 超时误报

### 任务描述
用户提供线上日志：项目群 `/coco` 进入 Coco 后，Spec 引擎启动阶段先创建一次 Coco ACP engine session，随后在 60s 处报错：
`[ACP:coco] prompt 执行超时 (timeout=60s): 操作超时，请稍后重试`，然后关闭该 session 并重新创建，才进入 `循环 1 阶段 spec 开始`。需要修复该日志错误。

### 根因分析
- 主 Spec phase 使用 `spec_execution_timeout=7200`，并非 60s。
- 60s 超时来自 Spec 启动前的需求验收标准拆解：`SpecEngine._decompose_criteria_with_llm()` → `_make_aux_send_fn()` → `prompt_via_acp()`。
- `prompt_via_acp()` 是一次性辅助 ACP 子会话，原默认 `timeout=60`，而冷启动 Coco + 模型上下文 + 首次 prompt 在真实环境可能超过 60s；该辅助任务失败后会 fallback 到原始文本，所以流程后续仍能继续，但日志会出现 ERROR + Traceback，造成误报和一次无意义的 session 重启。

### 执行内容
- `src/config/settings.py` 新增 `engine_aux_prompt_timeout: int = 600`，与 `SyncACPSession.send_prompt()` 默认 600s 对齐。
- `src/utils/acp_prompt.py` 将 `prompt_via_acp()` 默认 timeout 从 60 调整为 600，避免未显式传参的辅助 ACP prompt 继续使用偏短默认值。
- `src/spec_engine/engine.py` 的 `_make_aux_send_fn()` 显式读取 `self.settings.engine_aux_prompt_timeout` 并传给 `prompt_via_acp()`。
- `tests/test_spec_engine.py` 新增回归测试，先红灯确认旧代码没有透传配置（captured timeout 为 None/走 60s 默认），再绿灯锁定配置值会传入辅助 prompt。

### 验证
```bash
uv run python -m pytest tests/test_spec_engine.py::test_spec_engine_aux_criteria_prompt_uses_configured_timeout -q
# 1 passed

uv run python -m pytest tests/test_spec_engine.py::test_spec_engine_run_phase_fallback_to_send_prompt_when_retry_method_missing -q
# 1 passed

uv run python -m pytest tests/test_acp_sync_adapter.py -q
# 35 passed

uv run python -m pytest tests/test_spec_engine.py tests/test_spec_validation.py -q
# 201 passed
```

### 技术要点
- 这是辅助 prompt 的 timeout 配置缺口，不是主 Spec phase timeout 缺口。
- 修复保持全模式兼容：`prompt_via_acp()` 仍接收显式 timeout；Spec 通过 Settings 配置控制；其它调用默认也从 60s 提升到 600s，降低冷启动误报。
- 非关键辅助任务仍保留失败 fallback，不阻塞主 Spec 流程。

### 提交记录
- 待提交。

---

## TaskOrchestrator lazy 建卡 — 任务真正执行才推飞书卡片

### 任务描述
用户在 P0 修复后继续追问根因：
> "deep模式可能原因是，在明确 n 个任务要做的时候，直接创建了飞书卡片，而不是开始做某个任务时候才创建。这个需要强调一点：只有在任务开始执行了才构建飞书消息卡片，无论是主 agent 还是 subagent 都是如此。当然其他模式的飞书卡片的确是重复推送了，明明没有任务。"

### 根因分析
search 子智能体调研 5 处不当的"建卡时机"：

1. **`TaskOrchestrator.on_plan_received`**：plan 一到就为 N 个 task 同步建卡（含 pending 状态），与"任务执行时才建卡"的契约直接冲突；
2. **Deep handler 入口**：`reply_card("planning")` 静态卡 + `create_deep_callbacks` 内 thinking session ⇒ 双卡现象；
3. **Spec handler 入口**：`reply_card("分析中")` 占位卡，引擎尚未产出第一帧就先推卡；
4. **Programming `prog_session.start()`**：LLM 输出前先建卡（本轮**未动**——Programming 由用户消息触发，不算"无任务空推"）；
5. **Subagent 旁路**：已是 lazy 范式（TOOL_STARTED 检测到 agent/subagent 时按需建卡），无需修改。

### 执行内容

**1. [`src/card/orchestrator.py`](src/card/orchestrator.py)** — 核心 lazy 改造
- `on_plan_received` 重写：只 register 到 `TaskRegistry` + 计算 overflow 映射，**不再立即建 N 个 session**；仅当 plan 中已有 task 处于 `in_progress` 时立即触发建卡（保持向后兼容 ACP plan 状态语义）。
- 新增 `_ensure_task_session(task_id) -> bool`：lazy 建卡入口，幂等。`__init__` 同步引入 3 个状态字段：`_plan_tasks_pending` / `_plan_visible_task_ids` / `_thinking_finalized`。`reset()` 同步清理。
- `dispatch_to_task` 在 resolve 后、查询 session 前先调 `_ensure_task_session(resolved_id)`，并 re-check fallback。
- `route_acp_event` 同样接入 lazy 触发，仅对非 overflow task 调用，避免破坏原 overflow separator 路径。
- `handle_plan_update` 在 status broadcast 段遇到 `in_progress` 时触发 lazy 建卡，使"任务从 pending → in_progress"成为建卡的事实信号。
- **flood_merged 通知保留**：当 visible target session 首次建出来时，一次性 emit 所有指向它的 overflow task 的合并通知。这把"plan 时同步刷 N 条 flood 文本"的副作用迁移到了 lazy 路径，保证不丢通知。
- `_thinking_finalized` 守卫：第一张 lazy task 卡建出时再 finalize thinking session，避免 plan 一到就把思考卡 archive 了。

**2. [`src/feishu/handlers/deep.py`](src/feishu/handlers/deep.py)** — 移除 Deep 静态 planning 卡
- 删除 `engine_card`/`reply_card("planning")` 静态推卡逻辑；`engine.create(..., initial_message_id=None)`。
- 唯一入口卡由 `DeepRenderer.create_deep_callbacks` 内的 thinking session 承担，引擎首帧到达时才创建。

**3. [`src/feishu/handlers/spec.py`](src/feishu/handlers/spec.py)** — 移除 Spec 入口"分析中"占位卡
- 删除 `reply_card("分析中")` 调用；首张 rotator session 由 SpecRenderer 在 phase event 到达时按需创建。

**4. 测试同步更新**
- `tests/test_card_orchestrator.py`：新增 `_trigger_all(orch, tasks)` helper（在 `on_plan_received` 后批量调 `_ensure_task_session`），41 个旧 eager 语义测试全部恢复绿。
- `tests/test_deep_renderer_orchestrator.py` / `tests/test_spec_renderer_orchestrator.py`：将 N+1 卡数量契约测试中的 plan entries 改为 `status="in_progress"`，让 lazy 路径在测试中也立即建卡。

### 技术要点
- **行为契约迁移**：`on_plan_received` 从"建卡器"退化为"register + 映射器"，建卡责任下沉到 `_ensure_task_session`；事件 ARE 执行信号。
- **多 agent 一致性**：主 agent / subagent 共享同一个 lazy 入口（subagent 已通过 TOOL_STARTED 触发；主 agent 通过 in_progress 状态或首事件触发）。
- **幂等保证**：`_ensure_task_session` 在 lock 内查 `_sessions`，已存在直接 return True；overflow / unknown task / fallback / closed 均 return False，调用方安全。
- **视觉副作用保留**：flood_merged 合并通知、thinking session finalize、TASK_LIST_UPDATED 初始事件全部下沉到 lazy 路径，无 UI 退化。
- **测试**：6154 全绿（与昨日 6129 持平 + 25 项新增/调整），无回归；遗留 1 项 `test_show_full_help` 仍是 pre-existing，与本次无关。

### 提交记录
- `commit ac652a8 perf(card): fix overlapping cards and stream update lag` — D-1/D-3/D-5 三处 P0 修复（已 push）
- 本次 lazy 改造待 commit & push

---

## 修复飞书卡片重叠 + 流式更新延迟双 BUG

### 任务描述
2026-05-11 用户反馈 Deep / Spec / Programming 等模式下飞书卡片更新存在两类问题：
1. **多卡片重叠**：同一任务在飞书会话中产生 4-5 张内容高度相似的卡片（每张顶部都重复显示完整任务列表）。
2. **流式更新严重延迟**：后台引擎已经执行完毕，飞书卡片仍在缓慢逐 chunk 流式更新。

### 根因分析
通过对 card pipeline 全链路（`src/card/`、`src/feishu/handlers/`、`src/feishu/renderers/`、`src/acp/renderer.py`、`src/config/card.py`）的只读调研定位三个核心瓶颈：

1. **LiveTicker 心跳被错误标记为结构性事件** — `src/card/state/reducer.py` 的 `_STRUCTURAL_EVENTS` 包含 `TOOL_MODEL_CHANGED`，而 `programming_adapter.py` 每 1.2s 用 `tool_model_changed(live_ticker_frame=...)` dispatch 心跳帧。结果 N 张活跃卡每 1.2s 各触发一次飞书 `update_page`（整卡 PATCH，重接口），形成持续背压。
2. **delivery_pool worker 过少** — `delivery_pool_max_workers` 默认仅 4。一次 Deep 执行同时活跃卡数 ≥ 1 (planning) + 1 (thinking) + 8 (per-task) = 10，跨 session 投递在线程池排队等待，叠加 per-session 串行锁形成长队列。
3. **orchestrator 广播去抖窗口过短** — `_BROADCAST_DEBOUNCE_MS=100` 让密集 plan_update 几乎实时全量广播给所有 task 卡，N 张卡同步刷新且内容雷同，加剧"重叠+延迟"双重观感。

### 执行内容
按"最小改动" P0 集合实施 3 处修复：

**1. [`src/card/state/reducer.py`](src/card/state/reducer.py)**
- 从 `_STRUCTURAL_EVENTS` 移除 `CardEventType.TOOL_MODEL_CHANGED`。
- 新增 `_is_structural_event(event)` 辅助函数：仅当 payload 包含 `tool_name` / `model_name` / `subagents` 时视为结构性；纯 `live_ticker_frame` 心跳走 `element_content` 路径。
- `reduce_card_state` 调用点改为 `_is_structural_event(event)`。
- 效果：N 张卡 × 每 1.2s 的整卡 PATCH 退化为单元素 PATCH，飞书 API 压力降至原 1/N。

**2. [`src/config/card.py`](src/config/card.py)**
- `delivery_pool_max_workers` 默认值 `4 → 16`（validator 上限 32 仍然保留）。
- 效果：跨 session 的投递不再受限于全局线程池；多卡场景下吞吐提升约 4×。

**3. [`src/card/orchestrator.py`](src/card/orchestrator.py)**
- `_BROADCAST_DEBOUNCE_MS` `100 → 800`，并补充注释说明权衡。
- 效果：密集 plan_update 合并广播，N 张 task 卡的结构性事件密度降低 8×；任务列表最多滞后 ~0.8s，用户基本无感。

### 技术要点
- **D-1 的特殊处理保留向后兼容性**：`TOOL_MODEL_CHANGED` 在真正含工具/模型/子智能体变化时仍被视为结构事件，header/footer chips 更新行为不变。
- **未引入新抽象**：3 处改动均为常量或助手函数级别，无新模块、无 API 变更，向前兼容。
- **测试通过**：6129 项测试全部通过；唯一 1 项失败 (`test_show_full_help`) 经 git stash 验证为 pre-existing，与本次修改无关（断言 help 标题缺少 `v0.2.0` 版本号后缀）。
- **保留更激进改动作为 Backlog**：完整 task_list panel 在每张 per-task 卡顶部重复渲染（视觉重叠根源）需要 renderer 层 `compact` 模式设计，列入 Backlog 等设计稿确认后再实施。

### 验证路径
```
uv run python -m pytest tests/ -q --ignore=tests/test_validate_mode.py \
    --deselect tests/test_system_interaction.py::TestSystemInteraction::test_show_full_help
# 6129 passed, 1 deselected
```

### 提交记录
- 待提交：`src/card/state/reducer.py`、`src/config/card.py`、`src/card/orchestrator.py`
