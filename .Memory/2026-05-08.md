# 2026-05-08 项目记录

## 修复项目群已在编程模式时重复展示模型选择卡与冗余通知

### 任务描述
用户反馈：项目群中已经处于 Coco 编程模式时，系统不应再弹出模型选择卡片；选了模型后也不应重复发"已开启Coco编程模式"通知，因为卡片信息中已包含足够信息。

### 执行内容
1. **`src/feishu/dispatcher.py` — `_handle_enter_coco` 增加已在模式检查**
   - 调用 `is_coco_mode(chat_id, project_id=_pid)` 判断是否已在 Coco 模式
   - 若已在模式：有 pending_prompt 则直接转发到 handler，无则静默返回
   - 若未在模式：保持原有模型选择卡流程

2. **`src/feishu/handlers/system.py` — `_enter_mode_with_acp_model` 修复两处问题**
   - `mode_checker(chat_id)` → `mode_checker(chat_id, project_id=_project_id)`: 修复项目级模式检测缺失 project_id 导致误判
   - `handler.enter_mode(...)` → `handler.enter_mode(..., silent=True)`: 模型选择卡已提供足够视觉反馈，消除冗余"已开启"通知

3. **`tests/test_feishu_dispatcher.py` — 适配测试**
   - 显式设置 `_mode_manager.is_coco_mode.return_value = False` 确保测试覆盖"未在模式"路径

### 技术要点
- `is_coco_mode` / `is_programming_mode` 均需传递 `project_id` 才能正确判断项目级模式状态
- 模型选择后的 `enter_mode` 应使用 `silent=True`，因为 `build_switching_status_card` 已提供用户反馈
- `_handle_enter_coco` 中的已在模式守卫与 `process_with_intent` 的 `is_in_programming` 检查互补：后者处理正常消息转发，前者处理显式进入命令的边界情况

### 提交记录
- fix(dispatcher,system): 已在编程模式时跳过模型选择、选模型后静默进入
