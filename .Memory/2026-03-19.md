# 2026-03-19 项目记录

## 低风险死代码清理

### 任务描述
2026-03-19 02:14:16 UTC
在不影响现有功能和不干扰工作区其他未提交改动的前提下，清理已确认无引用的私有辅助代码与无效局部状态，降低维护噪音。

### 执行内容
1. 先检查 `.Memory/Abstract.md` 与当前工作区状态，确认仓库存在多处未提交改动，因此本次仅处理与现有改动解耦的低风险清理项。
2. 清理 `src/ttadk/runtime_repair.py` 中未被任何生产代码或测试引用的私有 helper `_redact_and_truncate()`，保留 `build_invalid_model_context()` 的兼容签名不变。
3. 清理 `src/ttadk/startup_probe.py` 中未参与任何判定结果的本地缓冲 `buf` 及其相关保留逻辑，保持 quickcheck 行为不变。
4. 清理 `src/acp/diagnostics.py` 中仓内零引用的私有 compat wrapper：`_get_diagnostics_redaction_config()` 与 `_redact_text()`。
5. 按“逐步验证”要求执行定向回归：
   - `uv run python -m pytest tests/test_ttadk.py -q -k "build_invalid_model_context or runtime_repair or startup_probe_quickcheck"` → `15 passed`
   - `uv run python -m pytest tests/test_acp_manager_ttadk_precheck.py -q` → `4 passed`
   - `uv run python -m pytest tests/test_acp_manager_ttadk_precheck.py tests/test_acp_client.py -q` → `27 passed`
   - `uv run python -m pytest tests/test_ttadk.py -q -k "compat or startup_probe_quickcheck or build_invalid_model_context"` → `15 passed`

### 技术要点
- 清理范围必须限定为“可证明无引用”的私有实现，避免误删处于迁移窗口的兼容层和业务入口。
- 在脏工作区中做清理时，应优先选择文件级解耦改动，并逐项回归验证，减少与用户未提交改动的冲突面。

### 提交记录
- 未提交

## 内存监控稳定性加固与临时文件清理

### 任务描述
2026-03-19 02:03:06
在不改变现有功能的前提下，提升工具本身的稳定性，并清理根目录临时脚本与日志文件，保持项目整洁可维护。

### 执行内容
1. 加固 `src/utils/gc_monitor.py`：将 `psutil` 改为可选依赖式导入，运行期若缺失则记录 debug 日志并跳过内存监控，不再因为导入失败影响 Deep/Loop 主流程。
2. 补充 `tests/test_gc_monitor.py`：新增 `psutil` 缺失时的优雅降级测试，覆盖稳定性回退路径。
3. 按仓库要求逐步验证：执行 `uv run python -m pytest tests/test_gc_monitor.py -q`，结果 `5 passed`。
4. 清理未被项目引用的根目录临时脚本与临时日志，包括 `fix_*.py`、`patch_*.py`、`test_loop_review_timeout.py`、`test_syntax.py`、`logs.log`、`stress_test.log` 以及 `__pycache__/`。

### 技术要点
- 监控类组件应采用 graceful degradation，避免观测能力异常反向拖垮业务主链路。
- 临时修补脚本不应长期滞留根目录；确认未引用后及时删除，减少维护噪音与误判成本。

### 提交记录
- 未提交
