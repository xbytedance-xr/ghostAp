# 2026-03-19 项目记录

## 工作区改动提交并推送

### 任务描述
2026-03-19 05:43:14 UTC
按用户要求将当前工作区所有已完成改动统一提交并推送到远端分支。

### 执行内容
1. 检查 `git status --short`，确认跟踪/未跟踪改动清单。
2. 核查 AGENTS 作用域，仅根目录规则生效。
3. 按规则检查 `.Memory/Abstract.md` 与当日日志，并补记本次任务。
4. 执行 `git add -A`、`git commit`、`git push`，并在推送后执行 `git status` 验证工作区清洁。

### 技术要点
- 提交信息遵循 `[Aiden]` 前缀与 `Co-Authored-By: Aiden` 规则。
- 推送后必须用 `git status` 二次确认是否还有未提交改动。

### 提交记录
- 本次任务提交并推送（hash 见本次 git 输出）

## Spec 触发顺序与模型初始化策略修复

### 任务描述
2026-03-19 04:35:03
修复 Spec 模式触发与模型初始化相关问题：确保 `/spec*` 与编程初始化命令（`/coco`、`/claude`、`/ttadk`）按消息顺序串行执行；模型策略上，ACP 模式按协议获取可用模型，CLI 模式（如 claude）不强行传模型，TTADK 保持特化逻辑。

### 执行内容
1. 日志核查：确认本次日志中 `/spec` 启动发生在 coco 会话初始化后，但代码层面存在 system queue 并发窗口，仍可能出现初始化与 spec 竞争。
2. `src/feishu/ws_client.py`：新增控制面串行队列构造逻辑 `_build_control_queue_key()`，将编程初始化命令与 `/spec*` 命令路由到同一 `chat_id:control:{project}` 队列；并将 TaskSpec `queue_key` 改为统一使用 `shell_queue_key or control_queue_key`。
3. `src/coco_model/manager.py`：调整 `set_model()` 校验策略为 ACP-first（优先基于 `get_models()` 的运行时模型列表校验），仅在获取为空时回退 `DEFAULT_MODELS`，避免动态模型可见但无法切换。
4. `src/spec_engine/engine.py`：
   - 新增 `_initialize_model_context()`：按 agent 区分初始化（coco 用 coco manager；claude 不初始化模型列表；ttadk 用 ttadk manager 当前模型）。
   - 新增 `_send_prompt_with_retry()` 兼容封装：session 无 `send_prompt_with_retry` 时回退 `send_prompt`。
   - 替换所有阶段/审查/验收评估中的直接 `self._session.send_prompt_with_retry(...)` 调用为兼容封装，修复日志中 `ModelFailureAwareSession has no attribute send_prompt_with_retry` 风险。
5. 测试修复与补充：
   - 新增 `tests/test_ws_client_patch.py` 回归测试：验证 `/coco` 与 `/spec*` 命中同一控制队列 key。
   - 新增 `tests/test_coco_model.py` 回归测试：验证 ACP 动态发现模型可被 `set_model()` 接受。
   - 新增 `tests/test_spec_engine.py` 回归测试：验证 session 缺少 `send_prompt_with_retry` 时 `_run_phase` 可回退到 `send_prompt`。
6. 处理阻塞验证的既有问题（非本次需求功能改动）：
   - 修复 `src/agent_session.py` 中既有语法错误（错误位置 `finally`）。
   - 修复 `tests/test_spec_engine.py` 既有缩进错误，恢复测试可收集。
7. 逐步验证执行：
   - `uv run python -m compileall src/feishu/ws_client.py src/coco_model/manager.py src/spec_engine/engine.py src/agent_session.py`
   - `uv run python -m pytest tests/test_coco_model.py -q` → `16 passed`
   - `uv run python -m pytest tests/test_ws_client_patch.py -q` → `9 passed`
   - `uv run python -m pytest tests/test_spec_engine.py -q -k "fallback_to_send_prompt_when_retry_method_missing or review_error_empty_message_uses_snippets or try_switch_model_claude_returns_false_without_switch"` → `3 passed`
   - `uv run python -m pytest tests/test_ws_client_patch.py tests/test_coco_model.py tests/test_spec_engine.py -q -k "control_queue_key or set_model_accepts_acp_discovered_model or fallback_to_send_prompt_when_retry_method_missing or try_switch_model_claude_returns_false_without_switch or review_error_empty_message_uses_snippets"` → `5 passed`

### 技术要点
- Spec 触发顺序问题的根因在调度层并发语义：slash 命令都被标记 system command 时，默认 system 并发会打乱初始化与控制命令顺序。
- 模型策略需按后端能力分层：ACP 后端可依协议获取可用模型；CLI 后端无列表接口时保持 `(auto)` 启动；TTADK 维持其工具特化预检与解析路径。
- 引擎层对 `send_prompt_with_retry` 不能假设强接口，需做兼容兜底，避免包装器/会话实现差异导致主流程硬失败。

### 提交记录
- 未提交

## 低风险死代码清理

### 任务描述
2026-03-19 02:14:16 UTC
在不影响现有功能和不干扰工作区其他未提交改动的前提下，清理已确认无引用的私有辅助代码与无效局部状态，降低维护噪音。

### 执行内容
1. 先检查 `.Memory/Abstract.md` 与当前工作区状态，确认仓库存在多处未提交改动，因此本次仅处理与现有改动解耦的低风险清理项。
2. 清理 `src/ttadk/runtime_repair.py` 中未被任何生产代码或测试引用的私有 helper `_redact_and_truncate()`，保留 `build_invalid_model_context()` 的兼容签名不变。
3. 清理 `src/ttadk/startup_probe.py` 中未参与任何判定结果的本地缓冲 `buf` 及其相关保留逻辑，保持 quickcheck 行为不变。
4. 清理 `src/acp/diagnostics.py` 中仓内零引用的私有 compat wrapper：`_get_diagnostics_redaction_config()` 与 `_redact_text()`。
5. 按“逐步验证”要求执行定向回归：
   - `uv run python -m pytest tests/test_ttadk.py -q -k "build_invalid_model_context or runtime_repair or startup_probe_quickcheck"` → `15 passed`
   - `uv run python -m pytest tests/test_acp_manager_ttadk_precheck.py -q` → `4 passed`
   - `uv run python -m pytest tests/test_acp_manager_ttadk_precheck.py tests/test_acp_client.py -q` → `27 passed`
   - `uv run python -m pytest tests/test_ttadk.py -q -k "compat or startup_probe_quickcheck or build_invalid_model_context"` → `15 passed`

### 技术要点
- 清理范围必须限定为“可证明无引用”的私有实现，避免误删处于迁移窗口的兼容层和业务入口。
- 在脏工作区中做清理时，应优先选择文件级解耦改动，并逐项回归验证，减少与用户未提交改动的冲突面。

### 提交记录
- 未提交

## 内存监控稳定性加固与临时文件清理

### 任务描述
2026-03-19 02:03:06
在不改变现有功能的前提下，提升工具本身的稳定性，并清理根目录临时脚本与日志文件，保持项目整洁可维护。

### 执行内容
1. 加固 `src/utils/gc_monitor.py`：将 `psutil` 改为可选依赖式导入，运行期若缺失则记录 debug 日志并跳过内存监控，不再因为导入失败影响 Deep/Loop 主流程。
2. 补充 `tests/test_gc_monitor.py`：新增 `psutil` 缺失时的优雅降级测试，覆盖稳定性回退路径。
3. 按仓库要求逐步验证：执行 `uv run python -m pytest tests/test_gc_monitor.py -q`，结果 `5 passed`。
4. 清理未被项目引用的根目录临时脚本与临时日志，包括 `fix_*.py`、`patch_*.py`、`test_loop_review_timeout.py`、`test_syntax.py`、`logs.log`、`stress_test.log` 以及 `__pycache__/`。

### 技术要点
- 监控类组件应采用 graceful degradation，避免观测能力异常反向拖垮业务主链路。
- 临时修补脚本不应长期滞留根目录；确认未引用后及时删除，减少维护噪音与误判成本。

### 提交记录
- 未提交
