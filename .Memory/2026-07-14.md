# 2026-07-14

## Durable Ingress Phase 3 Task 5：最终权限栅栏与终态附件处置

### 复审发现与修复

- 第二轮独立审查发现 authority provider 采样与最终 queue/rebalance/dequeue 提交之间仍可锚定
  workforce 撤权事件。最终 writer guard 现在用采样的 projection sequence/hash 对最新 Journal 做
  纯本地 fence；发现任何漏过的 workforce event 时 newcomer durable `authority_stale`，不得排队、
  驱逐合法 victim 或取得 dispatch grant。
- requester ACL 只接受精确布尔 `True`；整数、字符串、任意 truthy object 与异常统一 durable
  `requester_denied`。parent-trusted Channel status 要求完整 tenant/bot/app/agent/connection 字符串、
  enum-backed READY state，以及非 bool 的正整数 generation；`3.0`、`True`、raw string state 等畸形
  shape 默认拒绝。
- attachment staging 不再二次读取未经校验的 registry。Router 使用私有冻结
  `_AuthorityResolution` 临时携带已验证 credential ref；它不进入 repr、Journal、snapshot 或 dispatch
  grant。外部 staging 前重复完整 registry/binding/employee/Channel/membership/ACL 校验，staging 后再做
  第三次校验；credential 轮换或撤销时终止并清理，不进入队列。

### 终态附件清理所有权

- 自审证明 Task 4 completed staging 在 Router 因 `queue_full`、`queue_rebalanced`、dequeue 撤权或
  dispatch finish 进入 terminal 后没有其他清理 owner，用户附件字节会残留。Router 现在仅通过 Task 4
  的 `completed_for_acceptance`/`cleanup`/`recover` 公共端口清理 terminal records；不直接读取
  attachment service `.state`，不清理 queued、dispatching 或当前 grant。
- `route`、`dequeue`、`finish` 在 Router mutex 与 Journal transaction guard 全部释放后的 `finally`
  执行 terminal-only sweep。清理异常不覆盖已经提交的 Router 结果，只写入无密
  `RouterAttachmentCleanupReport`；显式 `recover_terminal_attachments()` 先收敛 Task 4 中断状态再重扫，
  重复调用幂等。
- Task 4 `completed_for_acceptance()` 对 cleanup-completed tombstone 返回 `None`，因为它已不再是可复用
  stage。真实 `AttachmentStagingService` 跨服务测试证明 stage → Router queue-full terminal → cleanup，
  随后两次 recover 均无 failed/cleaned 重复项，Journal 只有一次 cleanup-completed。

### 第三轮审查：READY enum 身份

- 独立审查确认旧四项与终态清理均闭环，但发现 Router 仍只比较 `status.state.value == "ready"`，
  `SimpleNamespace(value="ready")` 可冒充父进程 READY。新增 RED 精确复现后，将
  `ChannelProcessState` 提取到只依赖 stdlib Enum 的 `supervisor/channel_models.py`；
  `employee_channels` 继续 re-export 同一 enum，Router 直接依赖共享对象并要求
  `status.state is ChannelProcessState.READY`，不依赖类名或可伪造 value。
- 依赖方向保持无环：Router → channel_models，employee_channels → channel_models + ingress；fresh-process
  import 与旧/新 enum identity 均验证通过。最终 authority/queue/attachment 聚焦集合由 182 增至
  `184 passed`；包含 Channel process/bridge/contract 与 runtime composition 的受影响集合
  `277 passed, 1 warning in 124.69s`，Ruff 与 diff check 通过。
- 在共享 enum 修复提交的精确 HEAD 上再次执行 fresh 全量 Autonomous：
  `1590 passed, 2 skipped, 1 warning in 366.33s`；warning 仍仅为 pinned SDK protobuf deprecated
  datetime，没有新增回归。
- 第四轮独立审查对完整 `326626da..3a5d378d` 包分别批准 Spec compliance 与 Code quality，
  Critical/Important 均为 0。`docs/goals.md` 与 durable ingress 计划已更新为 Tasks 0-5 完成、
  Task 6 下一项；这不提升真实租户证据或 visible employee readiness。

### TDD 与验证

- 新增 RED/GREEN 覆盖：三处 workforce TOCTOU、truthy ACL、float/bool generation、raw Channel state、
  forged/rotated credential、queue-full/victim/dequeue terminal 清理、queued/dispatching 不清理、清理
  crash/recover、锁外端口、畸形 completion port，以及真实 Task 4 服务幂等。
- 最终 authority/queue/attachment 聚焦集合 `182 passed`；扩大
  security/integration/contract/chaos `879 passed, 1 skipped, 1 warning in 305.38s`；fresh 全量
  Autonomous `1588 passed, 2 skipped, 1 warning in 365.00s`。warning 仍仅来自 pinned
  `lark_channel` protobuf deprecated datetime。
- EI-QUEUE-01 精确 selector 与文档引用合计 `5 passed`；`ruff check src/autonomous/` 及三个改动测试
  文件、`git diff --check`、GhostAP v0.2.1 配置校验全部通过。
- Task 5 仍不创建 ACP attempt、不调用 ACP、不提升真实租户 readiness；visible employee limit 保持 0。
  下一步必须先取得独立 Spec/Code APPROVE，再更新阶段文档并进入 Task 6 attempt/effect/gateway。

## Durable Ingress Phase 3 Task 6：锚定员工执行与真实 Slock Gateway

### 执行、恢复与锁边界

- 新增 `EmployeeDispatchCoordinator` 与 frozen dispatch/permit/environment contracts。Router dispatch、
  attempt binding、dispatch committed 在同一 Journal frame 锚定；公开 dequeue 只读，真实 dispatch 只由
  coordinator 驱动。0 个或多个 Slock、projection/credential/generation/membership/ACL 漂移均 durable
  fail-close，完整 replay 在提交锁之外，提交区只做 captured head/CAS。
- accepted attempt 最多一次调用既有 `_run_acp_session`。completed/failed/canceled/timeout/action_required
  原子写入数据面；unknown dispatch restart 后转 ACTION_REQUIRED 而不重跑。terminal/finalize CAS 冲突有界
  重试并复用 staged Blob，started attempt 必有唯一 terminal 或 action-required disposition。
- timeout/cancel 通过低依赖 ContextVar 贯穿 Sync ACP/Slock，保持 `_run_acp_session` 内部语义不变；
  provider 异常对外只暴露稳定类型，异常 context、日志、Journal 与返回文本不泄露 provider secret。

### Context、环境与权限闭包

- permit 冻结 tenant/agent/generation/BotPrincipal credential ref、persona、tool/model/profile/effort、
  permissions/capabilities、Context snapshot/watermark 与 renderer contract。Context 使用 canonical JSON
  区分 trusted instruction 和 untrusted thread/group/L1/L2，envelope 纳入 token reserve，`tokens_per_char`
  同时进入 snapshot hash，防止换算配置漂移后复用旧快照。
- 真实链路覆盖 Slock → factory → SyncACPSession → ACPSession.start → spawn boundary，并启动真实子进程
  验证 Manager/Vault/peer credential 环境变量不继承。员工 Git 与 Shell 均视为 write/shell-equivalent，
  permissions/capabilities 必须同时具备 `shell + git + file_write`；CR/LF、wrapper、危险 Git 输出/
  external-process 参数、非 canonical shape 与 safety exception 全部 fail-close。
- ACP `request_permission` 会把 canonical Git execute 分类到 Git policy 并补齐受控 cwd；真实
  `request_permission → SlockEngine filter` 回归证明能力可达，newline 注入不能借 Git policy 绕过。

### `/hire` 模型生产契约

- visible `/hire` 卡片回调现在只在 global hire 路径拆分 base model/profile/effort 并写入 typed
  `EmployeeHireRequest`；普通 `/new-role` 继续保留 composite variant，避免 legacy 运行语义回归。
- HireService 在 Journal anchor 前严格拒绝 composite employee model、未知 effort、不支持 profile；
  Projected Registry 只在锚定三元组一致时组合 provider runtime value。真实测试贯穿 cascade callback →
  ProductionEmployeeHireService → Journal projection → ProjectedAgentRegistry，Traex `max/xhigh` 只组合一次。

### 审核与验证

- 多轮 grill 审核依次封闭真实 permission bridge 不可达、permission exception fail-open、Git newline/
  路径/外部进程逃逸、卡片模型重复组合、legacy `/new-role` 回归和锚定模型字段不一致。
- 最终受影响集合 `224 passed`；fresh 全量 Autonomous
  `1686 passed, 2 skipped, 1 warning in 399.52s`。唯一 warning 来自 pinned `lark_channel` protobuf
  deprecated datetime。changed-file Ruff 与 `git diff --check` 通过。
- 独立 Spec compliance 与 Code quality 均 APPROVE，Critical/Important 为 0。`docs/goals.md` 已更新为
  Phase 3 Task 0-6 完成、Task 7 生产证据聚合为下一项；真实租户与外部 release trust 尚未完成，
  `autonomous_visible_employee_limit` 继续保持 0。

## Durable Ingress Phase 3 Task 7：生产组合、恢复与证据交接

### 生产组合与派发所有权

- `EmployeeDepartmentRuntime` 现在统一拥有 canonical data、独立 AES-GCM Inbox、附件暂存、
  `DurableEmployeeIngressRouter` 与 `EmployeeDispatchCoordinator`。`FeishuWSClient` 将既有
  `SlockEngineManager` 注入组合根，员工进程默认只继承 PATH/LANG 等运行时白名单，不继承 Manager
  Bot 或共享 provider secret；缺 Slock/environment/Context/Ingress/Gateway 时 execution readiness
  返回稳定 blocker，不能因当前没有 ACTIVE 员工而乐观放行。
- 新增 Journal polling 派发 worker：Channel callback 只等待 Inbox anchor 后 ACK，后台再从 Journal
  路由并调用唯一 coordinator。idle 轮询和持续异常使用有界指数退避；Router terminal 会先写 Inbox
  disposition，再 tombstone/GC payload。
- 组合级真实测试从 ACTIVE hire、membership、Channel authority、加密 Inbox acceptance 一直贯穿到
  owned Router queue。该测试发现 Inbox frame 推进共享 Journal 后 Router 仍读取旧 workforce projection，
  会把合法请求误终止为 `authority_stale`；registry provider 现先 synchronize projection 再授权。

### 恢复、关停与失效路径

- 启动恢复顺序固定为 Inbox/Router replay → unknown committed dispatch 转 `action_required` → terminal
  attachment/disposition/GC 收敛 → Channel/hire 恢复 → 启动新派发，恢复异常稳定阻断
  `employee_recovery`，绝不重跑未知 ACP outcome。
- 关停先停止 ingress ACK admission，再停止 hire/context admission 与新派发；dispatch thread 超过 5 秒
  grace 时保留 Channel、附件、Inbox、data、Journal 和 Vault，避免仍运行的 ACP 使用已释放依赖。
  已接受 payload 在 admission 关闭后仍可被读取派发。已有 Journal 但 release evidence 被撤销时，默认
  ChannelSupervisor 不再收到半配置 ingress/resolver，而是以 `release_evidence` dormant 恢复。

### 九门禁与 FI-29

- Phase 3 implementation manifest 的九个 frozen exact selectors 均本地通过：两项 platform、
  `EI-IPC-01`、两项 bridge、queue、ACP-once、terminal 与 recovery。严格 evaluator 要求同一 commit、
  build artifact、SDK capability artifact、exact collected node ID 与 result summary，九项齐全才为 PASSED。
- 全局 FI-29 selector 已替换为真实 spawn child + Pipe + FileAnchor 的 `EI-IPC-01`。Acceptance evaluator
  对 FI-29 使用专用 bridge：嵌入结果必须重新通过 Phase 3 strict evaluator；缺 expected commit/digest、
  selector 不符或仅 `passed=true` 全部保持 PENDING。
- 本地 evidence 仍明确不可晋级；仓库没有伪造外部 final-build attestation。外部 build/workload
  provenance、QA trust root、单调 ledger 与真实租户验收仍属后续 Phase 8/9，visible limit 保持 0。

### 审核与验证

- 三轮 `grill-me` 分别按规格、安全和运维审查，并自动采纳作用域内建议；关闭了端口缺失乐观放行、
  revoked-release restart miscomposition、stale workforce projection、忙循环/日志风暴和未排空资源释放。
- 九个 exact selectors：`9 passed, 1 warning in 23.45s`；受影响聚焦集合最终 `198 passed`；
  fresh 全量 Autonomous：`1700 passed, 2 skipped, 1 warning in 397.24s`。两项 skip 仍为未授权真实租户
  与宿主 bwrap attestation，warning 仍仅为 pinned SDK protobuf deprecated datetime。
- `docs/goals.md` 与 Phase 3 计划已更新为 Tasks 0-7 完成；下一阶段是 Employee Response Channel，
  Phase 3 完成不代表真实员工可发布或 Agent Department 最终目标完成。

## Employee Response Channel Phase 4：加密 Outbox 与员工单卡闭环

### Durable Outbox 与投递协议

- 新增 `src/autonomous/outbox/`：snapshot、binding、delivery effect 均为 frozen exact-schema；
  `(tenant, agent, attempt)` 派生稳定 `outbox_id` 与 UUIDv5 create UUID。snapshot 版本严格 +1，
  progress 单调、created_at 固定、terminal version 永久围栏；card payload 递归拒绝 secret aliases。
- snapshot 先写员工 keyring 加密 Blob并回读认证，再以 safe metadata/BlobRef 写 Journal。
  replay 会验证所有 live Blob，缺失/损坏只关闭对应员工 Outbox；并发重复 append 幂等，冲突拒绝，
  orphan 隔离与 terminal-delivered 后 superseded snapshot tombstone/GC 均可恢复。
- create/patch 作为 PREPARED → EXECUTING → COMMITTED external Effect，前两态必须先锚定。
  同一 Outbox 至多一个 active effect；未知 create 永远先用同一 UUID/旧 snapshot 重试，不能被新版本
  超车。create 后 app/message 永久绑定；generation/connection 只在当前员工 authority 证明且成功 patch
  后更新。
- employee Channel IPC 新增 bounded `UPDATE_CARD` frame；child 仅调用 pinned SDK public
  `update_card(message_id, card)`。parent 对 app/generation/connection/message 四元组回执 fail-close；
  create 使用 public `send(..., opts.uuid)`，没有 `channel.stream()` 或主 Bot fallback。

### Gateway 与生产组合

- `EmployeeDispatchCoordinator` 新增显式 attempt lifecycle port：dispatch commit 后 append queued，ACP 前
  append running，history + attempt terminal + Router terminal 原子 frame 锚定后才 append terminal。
  completed/failed/canceled/timeout/action_required 映射到同一张员工状态卡；投递失败不修改执行终态。
- `EmployeeDepartmentRuntime` 拥有独立 Outbox BlobStore、lifecycle、delivery coordinator 与恢复 worker；
  readiness 要求 outbox 与 employee update capability。恢复先 replay Outbox/terminal attempts，再重试 pending
  delivery；shutdown 先关 Outbox admission/停 worker，再关 employee Channels、Outbox、Journal/Vault。
  旧 `provisioning/response.py` 内存 outbox 及其测试已删除，生产只有 Journal-backed 实现。
- terminal frame 锚定后、Outbox append 前崩溃的窗口由 `reconcile_terminal_snapshots()` 关闭：从加密
  history payload 和 attempt projection 重建 terminal snapshot，重复调用幂等且绝不重跑 ACP。
- UI 证据在 `ux/employee-response-card.html`，覆盖 queued/running/completed/failed/canceled/
  action-required 的桌面与窄屏预览。新增独立配置根
  `AUTONOMOUS_EMPLOYEE_OUTBOX_BLOB_DIR`，默认仍保持 visible employee limit 为 0。

### Grill 审核与验证

- 多轮 `grill-me` 自动采纳建议，依次关闭通用 token/client_secret 别名、created_at 漂移、非空投影 clone、
  wrong-message receipt、重连后安全 rebinding、未知 create 被新 snapshot 超车、未投递 snapshot 提前 GC、
  terminal callback 锁内/锚定前执行等缺口。
- Phase 4/受影响聚焦集合 `212 passed`，最终 data/recovery 集合 `133 passed`；fresh Autonomous
  `1723 passed, 2 skipped, 1 warning in 397.51s`；共享 Slock/WS/ContextVar/project/docs 回归
  `193 passed in 11.89s`。`ruff check src/autonomous/`、配置校验和 `git diff --check` 全部通过。
- 两项 skip 仍为未授权真实租户验收与宿主 bwrap attestation，warning 仍仅来自 pinned SDK protobuf
  deprecated datetime。Phase 4 完成不提升 release readiness；Phase 5 团队 membership、role 与 `/stop`
  仍是下一阶段。
