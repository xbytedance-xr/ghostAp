# 2026-07-14

## Durable Ingress Phase 3 Task 5：最终权限栅栏与终态附件处置

### 复审发现与修复

- 第二轮独立审查发现 authority provider 采样与最终 queue/rebalance/dequeue 提交之间仍可锚定
  workforce 撤权事件。最终 writer guard 现在用采样的 projection sequence/hash 对最新 Journal 做
  纯本地 fence；发现任何漏过的 workforce event 时 newcomer durable `authority_stale`，不得排队、
  驱逐合法 victim 或取得 dispatch grant。
- requester ACL 只接受精确布尔 `True`；整数、字符串、任意 truthy object 与异常统一 durable
  `requester_denied`。parent-trusted Channel status 要求完整 tenant/bot/app/agent/connection 字符串、
  enum-backed READY state，以及非 bool 的正整数 generation；`3.0`、`True`、raw string state 等畸形
  shape 默认拒绝。
- attachment staging 不再二次读取未经校验的 registry。Router 使用私有冻结
  `_AuthorityResolution` 临时携带已验证 credential ref；它不进入 repr、Journal、snapshot 或 dispatch
  grant。外部 staging 前重复完整 registry/binding/employee/Channel/membership/ACL 校验，staging 后再做
  第三次校验；credential 轮换或撤销时终止并清理，不进入队列。

### 终态附件清理所有权

- 自审证明 Task 4 completed staging 在 Router 因 `queue_full`、`queue_rebalanced`、dequeue 撤权或
  dispatch finish 进入 terminal 后没有其他清理 owner，用户附件字节会残留。Router 现在仅通过 Task 4
  的 `completed_for_acceptance`/`cleanup`/`recover` 公共端口清理 terminal records；不直接读取
  attachment service `.state`，不清理 queued、dispatching 或当前 grant。
- `route`、`dequeue`、`finish` 在 Router mutex 与 Journal transaction guard 全部释放后的 `finally`
  执行 terminal-only sweep。清理异常不覆盖已经提交的 Router 结果，只写入无密
  `RouterAttachmentCleanupReport`；显式 `recover_terminal_attachments()` 先收敛 Task 4 中断状态再重扫，
  重复调用幂等。
- Task 4 `completed_for_acceptance()` 对 cleanup-completed tombstone 返回 `None`，因为它已不再是可复用
  stage。真实 `AttachmentStagingService` 跨服务测试证明 stage → Router queue-full terminal → cleanup，
  随后两次 recover 均无 failed/cleaned 重复项，Journal 只有一次 cleanup-completed。

### 第三轮审查：READY enum 身份

- 独立审查确认旧四项与终态清理均闭环，但发现 Router 仍只比较 `status.state.value == "ready"`，
  `SimpleNamespace(value="ready")` 可冒充父进程 READY。新增 RED 精确复现后，将
  `ChannelProcessState` 提取到只依赖 stdlib Enum 的 `supervisor/channel_models.py`；
  `employee_channels` 继续 re-export 同一 enum，Router 直接依赖共享对象并要求
  `status.state is ChannelProcessState.READY`，不依赖类名或可伪造 value。
- 依赖方向保持无环：Router → channel_models，employee_channels → channel_models + ingress；fresh-process
  import 与旧/新 enum identity 均验证通过。最终 authority/queue/attachment 聚焦集合由 182 增至
  `184 passed`；包含 Channel process/bridge/contract 与 runtime composition 的受影响集合
  `277 passed, 1 warning in 124.69s`，Ruff 与 diff check 通过。
- 在共享 enum 修复提交的精确 HEAD 上再次执行 fresh 全量 Autonomous：
  `1590 passed, 2 skipped, 1 warning in 366.33s`；warning 仍仅为 pinned SDK protobuf deprecated
  datetime，没有新增回归。
- 第四轮独立审查对完整 `326626da..3a5d378d` 包分别批准 Spec compliance 与 Code quality，
  Critical/Important 均为 0。`docs/goals.md` 与 durable ingress 计划已更新为 Tasks 0-5 完成、
  Task 6 下一项；这不提升真实租户证据或 visible employee readiness。

### TDD 与验证

- 新增 RED/GREEN 覆盖：三处 workforce TOCTOU、truthy ACL、float/bool generation、raw Channel state、
  forged/rotated credential、queue-full/victim/dequeue terminal 清理、queued/dispatching 不清理、清理
  crash/recover、锁外端口、畸形 completion port，以及真实 Task 4 服务幂等。
- 最终 authority/queue/attachment 聚焦集合 `182 passed`；扩大
  security/integration/contract/chaos `879 passed, 1 skipped, 1 warning in 305.38s`；fresh 全量
  Autonomous `1588 passed, 2 skipped, 1 warning in 365.00s`。warning 仍仅来自 pinned
  `lark_channel` protobuf deprecated datetime。
- EI-QUEUE-01 精确 selector 与文档引用合计 `5 passed`；`ruff check src/autonomous/` 及三个改动测试
  文件、`git diff --check`、GhostAP v0.2.1 配置校验全部通过。
- Task 5 仍不创建 ACP attempt、不调用 ACP、不提升真实租户 readiness；visible employee limit 保持 0。
  下一步必须先取得独立 Spec/Code APPROVE，再更新阶段文档并进入 Task 6 attempt/effect/gateway。

## Durable Ingress Phase 3 Task 6：锚定员工执行与真实 Slock Gateway

### 执行、恢复与锁边界

- 新增 `EmployeeDispatchCoordinator` 与 frozen dispatch/permit/environment contracts。Router dispatch、
  attempt binding、dispatch committed 在同一 Journal frame 锚定；公开 dequeue 只读，真实 dispatch 只由
  coordinator 驱动。0 个或多个 Slock、projection/credential/generation/membership/ACL 漂移均 durable
  fail-close，完整 replay 在提交锁之外，提交区只做 captured head/CAS。
- accepted attempt 最多一次调用既有 `_run_acp_session`。completed/failed/canceled/timeout/action_required
  原子写入数据面；unknown dispatch restart 后转 ACTION_REQUIRED 而不重跑。terminal/finalize CAS 冲突有界
  重试并复用 staged Blob，started attempt 必有唯一 terminal 或 action-required disposition。
- timeout/cancel 通过低依赖 ContextVar 贯穿 Sync ACP/Slock，保持 `_run_acp_session` 内部语义不变；
  provider 异常对外只暴露稳定类型，异常 context、日志、Journal 与返回文本不泄露 provider secret。

### Context、环境与权限闭包

- permit 冻结 tenant/agent/generation/BotPrincipal credential ref、persona、tool/model/profile/effort、
  permissions/capabilities、Context snapshot/watermark 与 renderer contract。Context 使用 canonical JSON
  区分 trusted instruction 和 untrusted thread/group/L1/L2，envelope 纳入 token reserve，`tokens_per_char`
  同时进入 snapshot hash，防止换算配置漂移后复用旧快照。
- 真实链路覆盖 Slock → factory → SyncACPSession → ACPSession.start → spawn boundary，并启动真实子进程
  验证 Manager/Vault/peer credential 环境变量不继承。员工 Git 与 Shell 均视为 write/shell-equivalent，
  permissions/capabilities 必须同时具备 `shell + git + file_write`；CR/LF、wrapper、危险 Git 输出/
  external-process 参数、非 canonical shape 与 safety exception 全部 fail-close。
- ACP `request_permission` 会把 canonical Git execute 分类到 Git policy 并补齐受控 cwd；真实
  `request_permission → SlockEngine filter` 回归证明能力可达，newline 注入不能借 Git policy 绕过。

### `/hire` 模型生产契约

- visible `/hire` 卡片回调现在只在 global hire 路径拆分 base model/profile/effort 并写入 typed
  `EmployeeHireRequest`；普通 `/new-role` 继续保留 composite variant，避免 legacy 运行语义回归。
- HireService 在 Journal anchor 前严格拒绝 composite employee model、未知 effort、不支持 profile；
  Projected Registry 只在锚定三元组一致时组合 provider runtime value。真实测试贯穿 cascade callback →
  ProductionEmployeeHireService → Journal projection → ProjectedAgentRegistry，Traex `max/xhigh` 只组合一次。

### 审核与验证

- 多轮 grill 审核依次封闭真实 permission bridge 不可达、permission exception fail-open、Git newline/
  路径/外部进程逃逸、卡片模型重复组合、legacy `/new-role` 回归和锚定模型字段不一致。
- 最终受影响集合 `224 passed`；fresh 全量 Autonomous
  `1686 passed, 2 skipped, 1 warning in 399.52s`。唯一 warning 来自 pinned `lark_channel` protobuf
  deprecated datetime。changed-file Ruff 与 `git diff --check` 通过。
- 独立 Spec compliance 与 Code quality 均 APPROVE，Critical/Important 为 0。`docs/goals.md` 已更新为
  Phase 3 Task 0-6 完成、Task 7 生产证据聚合为下一项；真实租户与外部 release trust 尚未完成，
  `autonomous_visible_employee_limit` 继续保持 0。

## Durable Ingress Phase 3 Task 7：生产组合、恢复与证据交接

### 生产组合与派发所有权

- `EmployeeDepartmentRuntime` 现在统一拥有 canonical data、独立 AES-GCM Inbox、附件暂存、
  `DurableEmployeeIngressRouter` 与 `EmployeeDispatchCoordinator`。`FeishuWSClient` 将既有
  `SlockEngineManager` 注入组合根，员工进程默认只继承 PATH/LANG 等运行时白名单，不继承 Manager
  Bot 或共享 provider secret；缺 Slock/environment/Context/Ingress/Gateway 时 execution readiness
  返回稳定 blocker，不能因当前没有 ACTIVE 员工而乐观放行。
- 新增 Journal polling 派发 worker：Channel callback 只等待 Inbox anchor 后 ACK，后台再从 Journal
  路由并调用唯一 coordinator。idle 轮询和持续异常使用有界指数退避；Router terminal 会先写 Inbox
  disposition，再 tombstone/GC payload。
- 组合级真实测试从 ACTIVE hire、membership、Channel authority、加密 Inbox acceptance 一直贯穿到
  owned Router queue。该测试发现 Inbox frame 推进共享 Journal 后 Router 仍读取旧 workforce projection，
  会把合法请求误终止为 `authority_stale`；registry provider 现先 synchronize projection 再授权。

### 恢复、关停与失效路径

- 启动恢复顺序固定为 Inbox/Router replay → unknown committed dispatch 转 `action_required` → terminal
  attachment/disposition/GC 收敛 → Channel/hire 恢复 → 启动新派发，恢复异常稳定阻断
  `employee_recovery`，绝不重跑未知 ACP outcome。
- 关停先停止 ingress ACK admission，再停止 hire/context admission 与新派发；dispatch thread 超过 5 秒
  grace 时保留 Channel、附件、Inbox、data、Journal 和 Vault，避免仍运行的 ACP 使用已释放依赖。
  已接受 payload 在 admission 关闭后仍可被读取派发。已有 Journal 但 release evidence 被撤销时，默认
  ChannelSupervisor 不再收到半配置 ingress/resolver，而是以 `release_evidence` dormant 恢复。

### 九门禁与 FI-29

- Phase 3 implementation manifest 的九个 frozen exact selectors 均本地通过：两项 platform、
  `EI-IPC-01`、两项 bridge、queue、ACP-once、terminal 与 recovery。严格 evaluator 要求同一 commit、
  build artifact、SDK capability artifact、exact collected node ID 与 result summary，九项齐全才为 PASSED。
- 全局 FI-29 selector 已替换为真实 spawn child + Pipe + FileAnchor 的 `EI-IPC-01`。Acceptance evaluator
  对 FI-29 使用专用 bridge：嵌入结果必须重新通过 Phase 3 strict evaluator；缺 expected commit/digest、
  selector 不符或仅 `passed=true` 全部保持 PENDING。
- 本地 evidence 仍明确不可晋级；仓库没有伪造外部 final-build attestation。外部 build/workload
  provenance、QA trust root、单调 ledger 与真实租户验收仍属后续 Phase 8/9，visible limit 保持 0。

### 审核与验证

- 三轮 `grill-me` 分别按规格、安全和运维审查，并自动采纳作用域内建议；关闭了端口缺失乐观放行、
  revoked-release restart miscomposition、stale workforce projection、忙循环/日志风暴和未排空资源释放。
- 九个 exact selectors：`9 passed, 1 warning in 23.45s`；受影响聚焦集合最终 `198 passed`；
  fresh 全量 Autonomous：`1700 passed, 2 skipped, 1 warning in 397.24s`。两项 skip 仍为未授权真实租户
  与宿主 bwrap attestation，warning 仍仅为 pinned SDK protobuf deprecated datetime。
- `docs/goals.md` 与 Phase 3 计划已更新为 Tasks 0-7 完成；下一阶段是 Employee Response Channel，
  Phase 3 完成不代表真实员工可发布或 Agent Department 最终目标完成。

## Employee Response Channel Phase 4：加密 Outbox 与员工单卡闭环

### Durable Outbox 与投递协议

- 新增 `src/autonomous/outbox/`：snapshot、binding、delivery effect 均为 frozen exact-schema；
  `(tenant, agent, attempt)` 派生稳定 `outbox_id` 与 UUIDv5 create UUID。snapshot 版本严格 +1，
  progress 单调、created_at 固定、terminal version 永久围栏；card payload 递归拒绝 secret aliases。
- snapshot 先写员工 keyring 加密 Blob并回读认证，再以 safe metadata/BlobRef 写 Journal。
  replay 会验证所有 live Blob，缺失/损坏只关闭对应员工 Outbox；并发重复 append 幂等，冲突拒绝，
  orphan 隔离与 terminal-delivered 后 superseded snapshot tombstone/GC 均可恢复。
- create/patch 作为 PREPARED → EXECUTING → COMMITTED external Effect，前两态必须先锚定。
  同一 Outbox 至多一个 active effect；未知 create 永远先用同一 UUID/旧 snapshot 重试，不能被新版本
  超车。create 后 app/message 永久绑定；generation/connection 只在当前员工 authority 证明且成功 patch
  后更新。
- employee Channel IPC 新增 bounded `UPDATE_CARD` frame；child 仅调用 pinned SDK public
  `update_card(message_id, card)`。parent 对 app/generation/connection/message 四元组回执 fail-close；
  create 使用 public `send(..., opts.uuid)`，没有 `channel.stream()` 或主 Bot fallback。

### Gateway 与生产组合

- `EmployeeDispatchCoordinator` 新增显式 attempt lifecycle port：dispatch commit 后 append queued，ACP 前
  append running，history + attempt terminal + Router terminal 原子 frame 锚定后才 append terminal。
  completed/failed/canceled/timeout/action_required 映射到同一张员工状态卡；投递失败不修改执行终态。
- `EmployeeDepartmentRuntime` 拥有独立 Outbox BlobStore、lifecycle、delivery coordinator 与恢复 worker；
  readiness 要求 outbox 与 employee update capability。恢复先 replay Outbox/terminal attempts，再重试 pending
  delivery；shutdown 先关 Outbox admission/停 worker，再关 employee Channels、Outbox、Journal/Vault。
  旧 `provisioning/response.py` 内存 outbox 及其测试已删除，生产只有 Journal-backed 实现。
- terminal frame 锚定后、Outbox append 前崩溃的窗口由 `reconcile_terminal_snapshots()` 关闭：从加密
  history payload 和 attempt projection 重建 terminal snapshot，重复调用幂等且绝不重跑 ACP。
- UI 证据在 `ux/employee-response-card.html`，覆盖 queued/running/completed/failed/canceled/
  action-required 的桌面与窄屏预览。新增独立配置根
  `AUTONOMOUS_EMPLOYEE_OUTBOX_BLOB_DIR`，默认仍保持 visible employee limit 为 0。

### Grill 审核与验证

- 多轮 `grill-me` 自动采纳建议，依次关闭通用 token/client_secret 别名、created_at 漂移、非空投影 clone、
  wrong-message receipt、重连后安全 rebinding、未知 create 被新 snapshot 超车、未投递 snapshot 提前 GC、
  terminal callback 锁内/锚定前执行等缺口。
- Phase 4/受影响聚焦集合 `212 passed`，最终 data/recovery 集合 `133 passed`；fresh Autonomous
  `1723 passed, 2 skipped, 1 warning in 397.51s`；共享 Slock/WS/ContextVar/project/docs 回归
  `193 passed in 11.89s`。`ruff check src/autonomous/`、配置校验和 `git diff --check` 全部通过。
- 两项 skip 仍为未授权真实租户验收与宿主 bwrap attestation，warning 仍仅来自 pinned SDK protobuf
  deprecated datetime。Phase 4 完成不提升 release readiness；Phase 5 团队 membership、role 与 `/stop`
  仍是下一阶段。

## Autonomous Phase 5：真实群成员关系、Role Cutover 与 Durable Stop

### 官方 API 与成员关系权威

- 修复成员关系字段/接口混用：群成员增删由 Manager Bot 通过官方
  `POST/DELETE /open-apis/im/v1/chats/:chat_id/members` 执行，固定
  `member_id_type=app_id`；同一 chat 使用串行锁，避免官方文档中的并发变更冲突。
- 普通 members list 会过滤 Bot，因此不再用于核验。每次变更与事件对账均由目标员工凭据调用
  `GET /open-apis/im/v1/chats/:chat_id/members/is_in_chat`；只有观察与目标一致时，membership effect
  COMMITTED 与 `employee.membership_changed` 才在同一 Journal frame 落盘。
- 新增 `src/autonomous/membership/` frozen exact-schema 状态机与 replay projection：
  ABSENT/ADDING/ACTIVE/REMOVING/DEGRADED，以及 PREPARED/EXECUTING/COMMITTED/ACTION_REQUIRED。
  未知远端结果不乐观成功；Router 对缺失或 DEGRADED membership 默认拒绝。
- Bot added/deleted low-level SDK 事件先走员工 Durable Inbox，事件仅触发 `is_in_chat` 观察；
  非受管 Slock 群标记 ignored，重复/乱序事件不会直接改本地成员关系。恢复不会重放外部增删：
  EXECUTING 只查询，PREPARED 直接 ACTION_REQUIRED。

### `/role add/remove` 生产切换

- `HandlerContext` 注入 runtime membership service。canonical 员工按 tenant 投影解析，handler 与
  service 双层重验管理员/团队创建者、ACTIVE visible employee、BotPrincipal/App ID 和激活团队。
- `/role add` 只有员工 Bot 观察确认在群后显示成功；`/role remove` 只移除当前 chat，保留全局员工、
  Vault credential 与其他 member_groups。canonical 路径不写 legacy registry；legacy virtual role
  继续保留旧行为。
- 补齐 `slock_role_add_select` 回调，选项使用 projected agent ID；权限检查发生在列表读取和外部变更前。

### `/stop` 唯一终态与恢复

- 新增 `employee.execution_attempt.cancel_requested` Journal 事件。精确 `/stop` 在 Inbox ACK 后进入
  dedicated control path，dispatch worker 在 Router admission 前再次识别，阻塞中的 ACP 不会饿死停止命令。
- cancel 与 terminal 共用终态串行锁。未执行 permit 被撤销且不调用 Slock；运行中调用
  `cancel_employee_session`，取消 latch 覆盖 session 尚未注册的窗口；cancel-first 强制 canceled，
  terminal-first 只报告 already terminal，迟到 success/failed 不能写第二终态。
- 权限允许配置管理员、当前团队创建者或原 attempt requester。控制结果先进入员工 Durable Outbox，
  再 disposition 命令 Inbox；崩溃重试幂等，无主 Bot fallback。重启遇到已锚定 cancel 直接收敛
  canceled，不再运行 ACP。

### Grill 审核与验证

- 多轮 grill 自动采纳并关闭：普通成员列表过滤 Bot、chat API `chat_type` 可见性误用、幂等观察超时
  重发外部副作用、未激活团队绕过、PREPARED 恢复伪造 EXECUTING、cancel/terminal 竞态、permit
  注册前取消窗口、群主解析异常、控制命令被 Router 抢先以及 outbox/disposition 崩溃窗口。
- fresh Autonomous：`1763 passed, 2 skipped, 1 warning in 401.21s`；Phase 5/共享扩大集合先后
  `302 passed`、`269 passed`。`ruff check src/autonomous/`、`python -m src.main --validate`、
  `git diff --check` 均通过。
- 两项 skip 与唯一 warning 沿用既有外部环境条件和 pinned SDK protobuf deprecated datetime。
  Phase 5 完成不代表可发布；visible limit 仍为 0，Phase 6 `/fire` durable Saga 尚未实现。

## Autonomous Phase 6：Journal-backed `/fire` 退役闭环

### 立即撤权与正式命令入口

- 新增 `EmployeeFireService`、frozen fire state/projection 与 `JournalFireAuthority`。管理员在主 Bot
  DM 发出 `/fire <员工名称或ID> [--drain]` 后，`fire.requested`、
  `employee.state_changed=retiring` 和 `employee.ingress.closed` 在同一 shared Journal frame
  锚定；单员工 Inbox closure 可重放且不影响其他员工，新消息从 ACK 边界开始 fail-close。
- `/fire` 加入全局 Slock 命令捕获，但 handler 和 authority 两层都要求配置管理员、P2P、精确 tenant
  与 ACTIVE visible Employee/BotPrincipal。相同 tenant+message 的重投先读取 durable intent，不再因员工
  已 RETIRING/ARCHIVED 而破坏幂等。
- `HirePhase` 补齐 RETIRING，runtime 注入正式 fire service；旧 `provisioning/fire.py` 只保留兼容测试，
  不再承担生产路由。

### 锚定 Effect、恢复与清理

- 六步固定顺序为 execution quiesce、Slash cleanup、Channel stop、membership cleanup、credential
  destroy、archive move。每步均先提交 PREPARED，再提交 EXECUTING 后才允许外部调用，观察确认后才
  COMMITTED；EXECUTING 恢复只查询，绝不盲目重发。任何未知结果转 ACTION_REQUIRED，随后步骤不执行。
- 默认模式通过 durable coordinator 请求取消所有当前员工 attempt 并等待可配置
  `AUTONOMOUS_FIRE_GRACE_SECONDS`；`--drain` 不取消，只在同一有界期限等待自然结束。
- Slash cleanup 使用 employee credential 删除全部命令并最终 GET 验证空集合，恢复观察是纯 GET；
  Channel 必须观察为 STOPPED/FAILED/CRASHED。membership 新增管理员专用 RETIRING remove-only 路径，
  可清理已不再激活的旧 Slock 群，但不允许 add，也不删除全局员工。
- Vault `destroy` 后用 `exists` 证明 envelope 缺失，再提交 credential fact；若崩溃发生在 effect commit
  与 workforce fact 之间，恢复会幂等补写 `credential.destroyed`，不会带着陈旧 credential 投影归档。

### 原子归档与应用处置披露

- `AtomicEmployeeArchive` 在员工目录写临时 manifest、完整处理 short write、fsync 文件与目录后，执行
  同文件系统原子 rename 到 `.archive/<agent_id>`。源目录或内容含 symlink 时拒绝；恢复会重新计算所有
  manifest 文件 hash，源/目标同时存在或内容漂移均保持 ACTION_REQUIRED。
- manifest 记录 app/credential ref 哈希、所有文件 hash、history 日期范围、逐步 cleanup disposition、
  credential destroyed 证明与归档时间。开放平台处置固定为
  `external_app_disposition=manual_deletion_required`，终态消息明确 GhostAP 未删除应用，管理员仍需手动
  停用或删除。

### Grill 审核与验证

- 多轮 grill 自动采纳并关闭：旧内存 Saga、RETIRING 与 membership 权限冲突、状态与 ingress 非原子、
  EXECUTING 恢复重放、幂等重投先查 ACTIVE、`--drain` 未等待、credential fact 崩溃窗口、manifest
  short write/缺日期范围/不校验内容 hash 等问题。
- fresh Autonomous：`1774 passed, 2 skipped, 1 warning in 399.34s`；共享 Slock/WS/docs 回归
  `277 passed in 13.23s`。`ruff check src/autonomous/`、变更文件 Ruff、
  `python -m src.main --validate` 与 `git diff --check` 全部通过。
- 两项 skip 与唯一 warning 仍来自外部租户/宿主条件和 pinned SDK protobuf deprecated datetime。
  Phase 6 只关闭本地代码合同，不提升 release readiness；visible limit 继续为 0，下一阶段是数据面真实
  producer/handler/Supervisor cutover。

## Autonomous Phase 7：Canonical Employee Data Production Cutover

### 数据权威、迁移与恢复

- 新增独立 `employee.data.authority_cutover` 严格 reducer 与生产写门禁。Runtime 在启动时先 replay，
  对所有未归档 projected employee 执行 legacy import，确认无错误后才锚定 canonical epoch/sequence；
  之后 history stage/preflight/commit、document publish 和 read audit 都在写入前重验 authority。
- `SlockDataImporter` 不再吞掉 manifest commit 失败；malformed JSON 不写完成 manifest，多个 L1 源、
  canonical 内容冲突、symlink/non-regular agent/history/reasoning/document 均 fail-close。根
  `MEMORY.md` 只有在 canonical Blob 已验证后才原子移入 `.legacy-imported/` 并 fsync 目录。
- recovery 对全部 live history/document Blob 校验 label、认证与 content hash，再从 Journal 重建 daily
  JSONL 以及 L1/summary/skill/reasoning materialization。缺失或篡改 Blob 统一阻断
  `employee_data_recovery`，不回退读取 legacy 文件。

### 真实 producer 与读权限

- canonical `employee_v1` 在 `_execute_agent`、discussion full-session 等 legacy 入口直接拒绝；真实
  Gateway 仍只调用现有 `_run_acp_session`，因此 legacy virtual role 的 history/memory 行为不变。
- terminal history 继续与 attempt terminal/Router disposition 原子锚定。completed attempt 在员工
  Outbox terminal 前，以 attempt ID 幂等发布四类 canonical 文档：L1、chat/thread memory summary、
  skill profile 和 task-scoped reasoning；任一 sink 失败不发送成功卡，restart 可从 encrypted history
  重放缺失文档且不再次执行 ACP。reasoning `source_id` 和 version/predecessor 链现严格保留。
- 新增 trusted workforce subject resolver。非空 chat ID 不再等价于 membership；同群读取必须匹配
  projected `member_groups` 与 history `chat_id`。完整 L1 仅管理员在主 Bot P2P 可读，employee Channel
  `/memory` 只读当前 chat/thread summary；权限与 durable `employee.data.read_audited` 均早于 plaintext。
- employee `/history [1..31]`、`/memory` 在 Inbox ACK 后、Router admission 前执行，并只经员工
  Durable Outbox 回复。主 Bot 增加 `/history <employee>` 与 `/employee-memory <employee>`，保留原
  chat-scoped `/memory` 和 `/discuss history` 语义。history 分片与日期范围统一使用配置时区。

### 官方字段核对、Grill 与验证

- 飞书官方文档核对确认：`im.message.receive_v1` 使用 `event.message.chat_type` 判断 p2p/group；
  Chat API 回退使用 `chat_mode`，`chat_type` 只表示 private/public。现有 DM provenance 修复保持事件优先、
  仅明确无 provenance 时查询、任意字段/绑定/查询异常 fail-close。
- 多轮 grill 自动采纳并关闭：伪 membership、`chat_id==principal_id` 错误、延迟 legacy writer、
  summary 已终态但未恢复、只发布单一 summary、reasoning source 链丢失、UTC 分片漂移、root MEMORY
  双源冲突与 migration symlink 越界。
- fresh Autonomous：`1791 passed, 2 skipped, 1 warning in 401.32s`；受影响回归 `265 passed`；共享
  Slock/memory/handler/docs `208 passed`。changed-file Ruff、`python -m src.main --validate`、
  `git diff --check` 全部通过。两项 skip 仍为未授权真实租户验收与宿主 bwrap attestation，warning
  仍来自 pinned SDK protobuf deprecated datetime。
- Phase 7 完成不等于整体发布完成。真实 main-Bot send audit、immutable build/workload provenance、
  固定 QA trust root、外部单调 ledger、生产 anchor/见证和真实 staging/production/soak 仍属 Phase 8/9；
  `autonomous_visible_employee_limit` 继续保持 0。

## Autonomous Phase 8：External Release Trust Integration

### Root-owned broker 与可续期发布能力

- 新增 `release_trust.py` 严格 Unix socket 客户端。生产启动只接受 root-owned、非 symlink、非
  group/other writable socket，并用 Linux `SO_PEERCRED` 再验证 peer UID；WS 只允许默认 root UID，
  测试专用构造参数不进入 Settings。请求/响应均为单行、64 KiB 上限、拒绝 duplicate key，所有授权
  绑定随机 nonce、PID、release binding、evidence checkpoint、workload identity/digest、ledger、
  consumption ID、witness 与有限有效期。
- `.env` 中的 evidence 路径、公钥、key ID 或开关不再可能自证通过。Runtime 先严格验证本地 23 gate
  bundle 与 attested checkpoint，再由 broker 负责固定 QA trust root、immutable build/workload
  provenance 和单调 consume。缺 provider 时 `_release_evidence_ready` 继续固定拒绝。
- release lease 在 Channel monitor 中提前 120 秒续期，monitor 在 `recover()` 前启动。续期失败仅在当前
  lease 仍有效时重试；过期立即阻断 hire/execution readiness，关闭 hire、ingress、context、outbox
  新 admission，并禁用 external resume。续期不得改变 binding/checkpoint/workload/ledger/consumption，
  witness 与 recovery deadline 必须前进。

### 外部 CAS/Witness 与真实主 Bot 审计

- 员工 Journal 和 main-Bot audit 使用不同 broker anchor scope，但共享 release session 的全局 witness
  高水位，封堵单 scope 回滚和跨 scope witness 重放。真实 broker 路径不再使用本地 `FileAnchor` 作为
  员工生产 Journal 权威；本地 anchor 只保留默认关闭态和 test injection。
- `MainBotSendAuditLog` 使用独立 HMAC Journal。每次网络发送前先把 safe hash metadata 写入 broker 的
  跨副本单调 audit ledger，再提交本地 Journal/远端 CAS；外部计数必须声明 complete，audit/witness
  不得回退，且跨副本计数不得小于本地计数。只落 tenant/target SHA-256，不保存正文或原始 chat/message ID。
- 审计覆盖 `FeishuIMClient` create/reply/file-reply/patch，以及 CardDelivery 的 card create/reply/patch、
  streaming card reference 和 element patch。tenant 上下文缺失时写全局 unknown，查询时保守计入每个
  tenant，避免后台线程/回调丢失 tenant 后形成漏报。审计本身失败不破坏既有主 Bot 服务，但将当前
  audit 标记 incomplete，使员工激活验证 fail-close。

### Grill、验证与 Phase 9 阻塞

- 多轮 grill 自动采纳并关闭：config 自证、attestation replay、lease 永久有效、Journal 仍本地可回滚、
  多 scope witness 重放、只审计 `/hire`、CardDelivery 绕过、单实例审计漏掉其他副本、恢复完成后才续期、
  raw tenant/target 落盘等问题。
- fresh Autonomous：`1811 passed, 2 skipped, 1 warning in 399.43s`；Phase 8 定向/共享集合分别
  `210 passed`、`161 passed`。`ruff check src/autonomous/`、changed-file Ruff、
  `python -m src.main --validate`、docs references 和 `git diff --check` 全部通过。
- 现场只读核查确认：`autonomous_visible_employee_limit=0`，无 broker socket、evidence bundle、release
  attestation、完整 release binding 或真实租户 opt-in，`worker_sandbox_verified=false`；`bwrap` 二进制
  存在但宿主 user namespace selector 仍 skip。故 Phase 8 应用侧完成，但 Phase 9 真实 staging/
  production、桌面/移动 Slash、附件/话题/CardAction、断线/重启/fault injection 与 1/10/50 soak 仍由
  外部租户/部署/QA 授权阻塞，不得提高 visible limit 或伪造 evidence。

## Built-in Visible Employee Runtime

### 产品语义纠正

- 用户明确要求 Visible Employee 是 GhostAP 内置能力：配置主 Bot 并启动服务后，管理员主 Bot DM
  应能直接使用 `/hire`；发起用户会打开飞书 `aregister_app()` 注册链接并按官方指引完成 Bot 应用创建，
  不要求租户管理员审批，也不需要额外 release broker、evidence、lease、远程 witness 或人工 sandbox
  attestation。
- `autonomous_visible_employee_limit` 默认从 0 调整为 8；显式 0 现在是明确的运维关闭开关。WS 不再构造
  `RootOwnedUnixReleaseTrustBroker`，composition 不再读取 release claim、续租或用 release expiry 关闭
  admission。原 broker/evidence 模块仅保留兼容与验收工具，不参与启动 readiness。

### 本地安全自举与 composition

- 新增 `local_bootstrap.py`。首次启动在 `AUTONOMOUS_STATE_DIR` 创建 0700 目录及 0600 的 versioned
  `employee-runtime-secrets.json`，分别生成 Journal HMAC、Credential Vault、Data/Ingress/Outbox 三把
  独立 256-bit key。使用 no-follow lock、owner/type/mode 校验、duplicate-key 拒绝、原子 replace、文件及
  目录 fsync；symlink、损坏、截断、权限过宽或显式/自动配置混用均以无 secret 错误 fail-close。
- 完整显式 Journal/Credential/Data key 配置继续优先使用；自动密钥只保存在本地安全文件，不写 `.env`、
  日志、卡片或 Journal metadata。Runtime 直接使用 `FileAnchor`，本地主 Bot HMAC audit 仅作诊断；audit
  打不开时不再阻断 `/hire` 或员工激活。
- Data composition、Ingress 与 Outbox 接受同一个 typed `EmployeeDataKeyring`，避免为自动密钥回写或
  伪造 Settings。恢复、membership、Context、dispatch、fire 和员工 response 均在本地 runtime enabled
  语义下继续运行。

### Channel fallback 与审核

- Employee Channel 仍优先 bwrap。默认 bwrap launch/attestation 失败时，先关闭 bootstrap pipe、停止并
  reap 失败进程，再重新创建全部 pipe，以同一绝对 worker、`python -I`、最小环境和一次性 secret pipe
  重试一次。fallback 明确记录 `verified=false, mechanism=process-fallback`，不会伪报 sandbox 已验证。
- 显式 `sandbox_prefix`、自定义 attestor 或自定义安全测试仍保持 fail-close 原语义；fallback 只适用于
  默认自动选择路径。grill 审核自动采纳了“不伪造 verified”与“不写回 .env”两项建议。

### 验证

- 本地 bootstrap 安全测试 `8 passed`；composition/unit/bootstrap `73 passed`；Channel isolation、contract、
  process `57 passed`；release/config/ingress/Slock/docs 扩大回归在修正文档路径后通过。
- 首轮 fresh `tests/autonomous/`：`1821 passed, 1 skipped, 1 warning in 403.10s`。唯一 skip 为未授权真实租户
  验收；原宿主 user namespace skip 已由真实 `process-fallback` 路径通过。warning 仍为 pinned
  `lark-channel-sdk` protobuf 的 `utcfromtimestamp` deprecation。
- 真实租户尚未在本轮执行，因此“代码已可启动体验”和“真实租户验收完成”必须继续分开表述。

### 官方智能体 preset 与员工出站补全

- 用户提供飞书官方“一键创建飞书智能体应用”“机器人 @机器人”和“智能体回复云文档评论”文档。
  对照后确认原 `preset=false` 只选择最小模板，缺少完整官方智能体预置；现改为 `preset=true`，并显式
  冻结 Bot-to-Bot mention、文档评论、Slash Command、卡片、消息及 WebSocket 事件所需 tenant/user
  scopes，包含 `drive.notice.comment_add_v1` 与 `offline_access`。
- 审计同时发现 production low-level Channel 对 SEND 固定返回 `outbound-transport-separate`，真实员工
  会在 `/status` 激活回复阶段失败。新增 employee-owned `LarkEmployeeOutbound`，只在员工子进程中用
  一次性 credential 构造官方 `lark-oapi` client；支持 text/card/post、reply、stable UUID、card patch 与
  document comment reply，成功回执绑定当前 app/generation/connection，不经主 Bot。
- 普通 SEND IPC 现在与 UPDATE_CARD 一样有 exact schema 校验；出站异常仅回传类型，不泄漏 SDK 响应或
  credential。定向 registrar/outbound/channel contract 回归 `61 passed`。
- 自动 Agent-to-Agent 接力和云文档评论执行仍不是端到端完成：前者需放开当前 bot-loop 拒绝前建立
  same-team ACTIVE membership、Bot sender identity 与循环预算，后者需 comment event normalization、
  逐文档授权、评论读取和 durable route。当前只完成官方创建配置与员工自身 transport primitive，文档
  和 goals 明确禁止把权限存在误报为工作流可用。
- 最终 fresh `tests/autonomous/`：`1834 passed, 1 skipped, 1 warning in 404.45s`；定向安全/组合/Channel
  `154 passed`，共享 Feishu 路由、配置、Slock 与文档 `280 passed`。

## 员工运行时 HOME 软链接启动修复

### 现场根因与安全修复

- 首次真实重启后主 Bot WebSocket 正常连接，但 employee execution storage 与 Context composition
  均报 `EmployeeDataConfigurationError`。现场确认 `/home/jiataorui` 是系统维护、指向
  `/data00/home/jiataorui` 的 root-owned 软链接；BlobStore、Credential Vault 与 Attachment Staging
  的逐路径 `O_NOFOLLOW` 检查因此在进入各自安全根之前就拒绝了 HOME 前缀。
- 新增 `canonicalize_user_home_path()`，只在配置路径位于当前 `Path.home()` 下时解析该受信 HOME
  前缀，然后原样追加尚未解析的子路径。它不会调用整路径 `resolve()`，因此 `.ghostap` 以下由用户
  创建的软链接仍由各存储的 no-follow 检查拒绝；HOME 之外的路径也不获得例外。
- Credential Vault、Data BlobStore、Ingress、Outbox、Attachment Staging 及 Slock legacy/agents
  composition 统一采用该边界。原先 Credential Vault 的 `resolve(strict=False)` 会顺带跟随已存在的
  子路径软链接，现一并收紧为只解析 HOME 前缀。

### 回归与验证

- 新增系统 HOME 软链接集成测试，覆盖 credential、data、ingress、outbox、attachment 与 legacy
  storage 同时位于 `~/.ghostap` 的生产组装；同时新增子目录软链接仍 fail-close 的反例。
- 定向回归 `19 passed`；员工数据/Ingress/Outbox/附件/组装扩大回归 `385 passed`；changed-source
  Ruff 与 `git diff --check` 通过。
- fresh `tests/autonomous/`：`1836 passed, 1 skipped, 1 warning in 402.12s`。唯一 skip 为未授权真实
  租户验收，warning 仍来自 pinned `lark-channel-sdk` protobuf 的 `utcfromtimestamp` deprecation。
- 后续真实重启必须同时确认主 Bot WebSocket 已连接，并且新启动窗口内不再出现 execution storage、
  Context composition 或 employee department durable composition 错误；在此之前不得宣称 `/hire`
  已可直接体验。
